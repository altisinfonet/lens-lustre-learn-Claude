# AI EVIDENCE — append-only

One line per check. **A claim is not evidence; a re-runnable command is.**

⚠ **This file is public.** Anything that would describe an *unfixed* vulnerability is
recorded as `id + PASS/FAIL` only, and the detail goes to the private claude.ai project.
A closed finding may be described in full — that is history, not a map.

Format: `date | cycle | check | command | result`

---

2026-08-14 | M1 | file integrity before apply | `git hash-object supabase/migrations/20260814042609_email_queue_authority.sql` | `c6202f77…` — matched `git rev-parse HEAD:<path>`
2026-08-14 | M1 | grant before (enqueue_email) | `select has_function_privilege('anon','public.enqueue_email(text,jsonb)','EXECUTE')` | `true`
2026-08-14 | M1 | grant before (read_email_batch) | `select has_function_privilege('anon','public.read_email_batch(text,integer,integer)','EXECUTE')` | `true`
2026-08-14 | M1 | callers that would lose access | source enumeration + `pg_proc.prosecdef` on every caller | `0` — all service_role or SECURITY DEFINER owned by postgres
2026-08-14 | M1 | grant after (both) | same `has_function_privilege` calls | `anon=false authenticated=false service_role=true postgres=true`
2026-08-14 | M1 | allow-list rejects | `select public.enqueue_email('attacker_queue_probe','{}'::jsonb)` | `42501 … is not permitted`
2026-08-14 | M1 | allow-list rejects (read) | `select public.read_email_batch('victim_queue_probe',1,30)` | `42501 … is not permitted`
2026-08-14 | M1 | allow-list accepts (read) | `select count(*) from public.read_email_batch('auth_emails',0,0)` | `0 rows, no error` — batch_size 0, no payload read, no visibility timeout set
2026-08-14 | M1 | allow-list accepts (write) | `BEGIN; select public.enqueue_email('transactional_emails', …); ROLLBACK;` | `msg_id 2662`, then `probe_rows_left=0` — rollback confirmed
2026-08-14 | M1 | side-effect table creation | `select count(*) from pg_class … nspname='pgmq' and relname ~ 'probe'` | `0`; pgmq tables `11` before and after
2026-08-14 | M1 | applied version | `list_migrations` | `20260814042609 email_queue_authority`
2026-08-14 | M1 | test suite | `npx vitest run` | `1335 passed · 1 skipped · 0 failed`
2026-08-14 | M1 | build | `npm run build` | `exit 0, built in 24.03s`
2026-08-14 | M1 | security gate | `node scripts/security-audit.mjs` | `CRITICAL 0 · HIGH 0 · MEDIUM 2 · LOW 2`
2026-08-14 | M1 | advisor delta | `get_advisors(security)` | `anon_security_definer 248 → 246`; `authenticated 269 → 267`; `0` entries name either function
2026-08-14 | M1 | typecheck | `npx tsc -b tsconfig.json` | `2 errors` — pre-existing, Phase 0 item 0.3, not a regression
2026-08-14 | M1 | VERDICT | — | `PASS` — no threshold moved
2026-08-14 | ENV | push reachability | `git push --dry-run origin main` | `403 — repository not in this session's authorised set`
2026-08-14 | ENV | repo visibility | `git ls-remote <url>` with no credential | `succeeded — repository is PUBLIC`
2026-08-14 | ENV | remote drift | `git fetch && git rev-parse origin/main` | `8259c5b → 86afb8d` mid-session; audit base was stale
2026-08-14 | SEC-01 | open finding, detail withheld (public file) | see private project doc | `OPEN`
2026-08-14 | SEC-02 | open finding, detail withheld (public file) | see private project doc | `OPEN`
2026-08-14 | M2-PRE | reachable surface | `pg_proc` where prosecdef and anon EXECUTE and prorettype<>trigger | `132` (advisor's 246 includes 114 trigger fns PostgREST cannot invoke)
2026-08-14 | M2-PRE | source scan v1 (line-based) | `grep -oP '\.rpc\(\s*["'"'"']([a-z0-9_]+)'` | `101` names
2026-08-14 | M2-PRE | source scan v2 (multiline) | python re.S over whole files | `108` names — v1 missed 7
2026-08-14 | M2-PRE | source scan v3 (cast-aware) | adds `(supabase.rpc as any)("…")` | `118` names — v1 missed 17 of 118 (14%), incl. log_app_event and log_client_error
2026-08-14 | M2-PRE | db-side call stats | `select current_setting('track_functions',true)` | `none` — database records no function usage
2026-08-14 | M2-PRE | production ground truth | `query_logs` edge_logs `/rest/v1/rpc/%`, 24h | `34` distinct RPCs; 2 of them appear nowhere in current source
2026-08-14 | M2-PRE | partition | source ∪ logs vs the 132 | `86` with evidence of use · `46` without
2026-08-14 | M2-PRE | top consumer | same query | `read_email_batch` `16952` calls/24h (~12/min, process-email-queue polling)
2026-08-14 | M2-PRE | branch cost | `get_cost(branch)` | `$0.01344/hour` — not created, read-only route sufficed
2026-08-14 | M2-PRE | VERDICT | — | `M2b BLOCKED` — cannot enumerate what installed APKs call; instrument first. M2a is separable and near-zero risk.
2026-08-14 | DRIFT | remote moved (3rd time this session) | `git fetch && git log f8c1985..origin/main` | `154d2a1` cut 1.2.5, `c7c591b` versionName 1.2.5 — Android trigger paths only
2026-08-14 | DRIFT | audit findings re-verified on new HEAD | `git show origin/main:.github/workflows/android-build.yml` | line 116 still `tsc --noEmit -p tsconfig.json` (no-op); index.html check still inside `continue-on-error: true` — both findings HOLD
2026-08-14 | SYNC | remote moved (4th time) | `git fetch && git log c7c591b..origin/main` | `6cb095d` docs only — 4 files under docs/, no source, no trigger path
2026-08-14 | SYNC | other session's test claim | `npx vitest run` after rebase onto 6cb095d | `1345 passed · 1 skipped` — claim VERIFIED
2026-08-14 | SYNC | database untouched by other session | `list_migrations` | `20260814042609` still newest — unchanged
2026-08-14 | SYNC | handover gap closed | project doc `claude/CURRENT_STATE_READ_ME_FIRST.md` | recorded the applied migration, CONTROL protocol, open findings, public-repo warning
2026-08-14 | P1 | supabase branch | `create_branch` | `PaymentRequiredException — Pro plan required`; no cost incurred; harness built locally on PG16 instead
2026-08-14 | P1 | harness seeded | local PG16, production ratios | `100k posts · 45k profiles · 471k events · 165.6k friendships · 80/15/5 public/friends/private`
2026-08-14 | P1 | BEFORE @100k | `EXPLAIN (ANALYZE,BUFFERS)` production query shape | `827766 buffers · 1603 ms · Seq Scan on posts`
2026-08-14 | P1 | AFTER-1 @100k | + is_public/partial index + maintained viewer_bucket | `207112 buffers · 941 ms` — still Seq Scan; 97% of cost was the per-row friends EXISTS
2026-08-14 | P1 | AFTER-2 @100k | + bounded candidate pool, friend ids resolved once | `577 buffers · 3.9 ms`
2026-08-14 | P1 | BEFORE @1M | same | `4836645 buffers · 9489 ms · external merge Disk: 148672kB`
2026-08-14 | P1 | AFTER-2 @1M | same | `650 buffers · 4.5 ms` — flat from 100k to 1M
2026-08-14 | P1 | GATE | pre-stated `<50ms and <2000 buffers @100k` | `FAIL` for Phase 1 as approved (941 ms / 207112). Threshold NOT moved. Design must change.
2026-08-14 | P1 | correction | re-read own earlier measurement | the production 107x was `WHERE privacy='public' … LIMIT 10` (index scan stopping at 10) — a different query than the feed runs
2026-08-14 | P1-A | option A @1M | `EXPLAIN (ANALYZE,BUFFERS)` bounded pool + uniform corpus sample | `sample200 3.8ms/854buf · sample500 6.2/1155 · sample1000 8.1/1656 · sample2000 15.2/2659`
2026-08-14 | P1-A | gate | pre-stated `<50ms and <2000 buffers` | `PASS at 1M for sample<=1000` — threshold not moved
2026-08-14 | P1-A | fairness reach | 500 simulated sessions, count distinct sampled posts | `92805 of 800000 = 11.6% corpus; 7% collision (near-uniform)`
2026-08-14 | P1-A | recency bias | avg age of reached vs corpus | `215.0 days vs 214.7 days` — no bias; bounded-window design had destroyed this
2026-08-14 | P1-A | model check | `800000*(1-(1-200/800000)^500)` | predicted `94000` vs measured `92805` — model agrees
2026-08-14 | P1-M | migration drafted | `supabase/migrations/20260814060000_feed_candidate_pool.sql` | additive only; does NOT touch get_broadcast_feed (trap #3 isolated to a later migration)
2026-08-14 | P1-M | round-trip apply | fresh db, real file | `APPLY OK` — pool 673 of 2000 posts, backfill 1600 public, trigger increments (6000 -> 6001)
2026-08-14 | P1-M | round-trip rollback | real rollback file | `ROLLBACK OK` — 0 of 4 new columns remain
2026-08-14 | P1-M | re-apply after rollback | same file again | `RE-APPLY OK` — rollback leaves a re-appliable state
2026-08-14 | P1-M | full gate | vitest / security-audit | `1345 passed · 1 skipped` · `PASS — no blocking findings`
2026-08-14 | P1-M | GO binding | `git hash-object <migration>` | `677c0cab762595390c4f84e8efd3856dc45613d0`
2026-08-14 | P1-M | PRE-FLIGHT STOP | `pg_default_acl` objtype 'f' on production | `anon=X | authenticated=X | service_role=X` — the drafted SECURITY DEFINER helper took the viewer as a parameter and would have been anon-callable with any member's uuid
2026-08-14 | P1-M | fix | viewer now read from `auth.uid()`; parameter removed; explicit REVOKE | verified `has_function_privilege('anon',...)` = `f`, `service_role` = `t`
2026-08-14 | P1-M | test-setup fault caught | first round-trip ran on an EMPTY table (`CREATE ROLE anon` fails cluster-wide, ON_ERROR_STOP aborted the seed) | re-run on 2001 posts: pool 698, own+private present, trigger 6003->6004
2026-08-14 | P1-M | round-trip (populated) | apply / verify / rollback / re-apply | all OK; 0 of 4 columns remain after rollback
2026-08-14 | P1-M | GO VOIDED | approval was for `677c0cab...`; file changed | new hash `5ccba5fb83aaf80b448983526ad33b6640933f4e` — requires a fresh GO
2026-08-14 | LOCK | invariant test added | `src/__tests__/securityDefinerGrants.test.ts` | asserts every SECURITY DEFINER fn a migration creates is REVOKEd from anon by name, gates on auth.uid()/has_role, or delegates to one that does
2026-08-14 | LOCK | mutation-tested (can it fail?) | removed the anon revoke from the M1 migration | `2 failed` — then restored, `12 passed`
2026-08-14 | LOCK | mutation-tested | removed the queue allow-list | `1 failed` — then restored, `12 passed`
2026-08-14 | LOCK | vacuous-pass guard | asserts `inScope.length > 0` | deleting every migration cannot make the suite green
2026-08-14 | P1-APPLY | approval binding | `git hash-object` vs approved | `5ccba5fb...` MATCH — GO valid
2026-08-14 | P1-APPLY | final pre-flight | colliding columns / functions / newest migration | `0 / 0 / 20260814042609`
2026-08-14 | P1-APPLY | applied | `apply_migration` | version `20260814070357 feed_candidate_pool`
2026-08-14 | P1-APPLY | is_public backfill | `count(*) where is_public` | `210 of 210` — production is 100% public, as measured earlier
2026-08-14 | P1-APPLY | viewer_count backfill | `sum(viewer_count)` vs distinct (post,user) view/skip pairs | `57` vs `57` — exact match
2026-08-14 | P1-APPLY | rand_key | `count(*) where rand_key is null` | `0`
2026-08-14 | P1-APPLY | indexes | `pg_indexes` for the two new names | `2`
2026-08-14 | P1-APPLY | grants | `has_function_privilege` | feed_candidates anon=`false` service_role=`true`; refresh_viewer_buckets anon=`false`
2026-08-14 | P1-APPLY | live call | `explain analyze select count(*) from feed_candidates('gate-report-seed')` | `210 rows · 1403 buffers · 12.4 ms`
2026-08-14 | P1-APPLY | filename drift | applied version 20260814070357 vs drafted 20260814060000 | repo files renamed to match
2026-08-14 | P1-APPLY | gate | vitest / security-audit | `1357 passed · 1 skipped` · `PASS`
2026-08-14 | P1-RPC | drafted | `20260814080000_feed_rpc_candidate_pool.sql` + rollback | all 3 overloads DROP/CREATE in one txn, explicit re-GRANT (trap #3)
2026-08-14 | P1-RPC | guard fired on the ranking change | `feedFreshness.test.ts` | 2 assertions pinned to `random() * 6.0` failed — the guard doing its job
2026-08-14 | P1-RPC | guards RE-POINTED, not relaxed | now assert feed_rank_score + seeded jitter + no recency term + bounded pool + no count(DISTINCT) | strictly more than before
2026-08-14 | P1-RPC | bug found IN MY OWN TEST | `indexOf("seen_ranked AS")` matched inside `unseen_ranked AS` | slice was empty, assertion checked nothing; fixed + length guard added
2026-08-14 | P1-RPC | mutation: recency term into unseen tier | re-run guards | `1 failed` ✔
2026-08-14 | P1-RPC | mutation: drop the per-viewer seed | re-run guards | `1 failed` ✔
2026-08-14 | P1-RPC | mutation: reinstate count(DISTINCT) LATERAL in real SQL | re-run guards | `1 failed` ✔
2026-08-14 | P1-RPC | FALSE PASS caught in my own harness | rollback reported OK while psql had errored (exit code masked by a pipe; test db lacked can_view_post) | re-run with exit codes captured
2026-08-14 | P1-RPC | round-trip (valid) | apply / rollback / re-apply, exit codes checked | `0 / 0 / 0`; forward uses feed_candidates, rollback uses can_view_post, both return 10 rows
2026-08-14 | P1-RPC | grants across DROP | count of PUBLIC entries in proacl | forward `0/0/0`; rollback `1/1/0` — exactly production's prior state
2026-08-14 | P1-RPC | gate | vitest / security-audit | `1361 passed · 1 skipped` · `PASS`
2026-08-14 | P1-RPC | GO binding | `git hash-object` | `11661da4f7526a8c1d466e8bce0e5e98116d6b76`
2026-08-14 | A4 | CI typecheck was vacuous | `npx tsc --noEmit -p tsconfig.json --listFiles \| wc -l` | `0` files checked, exit 0
2026-08-14 | A4 | fixed | android-build.yml now runs `npx tsc -b tsconfig.json` | `tsc -b` exit `0` after fixing the 2 errors it exposed
2026-08-14 | A4 | error 1 | `FeedCardWindow.test.tsx:113` TS2578 unused `@ts-expect-error` | the directive was itself the error; replaced with a typed `delete`
2026-08-14 | A4 | error 2 | `profileMapCache.ts:344` TS2339 `.data` missing | `withRetry<T extends {error:unknown}>` narrowed away `data`; constraint widened to `{data:unknown; error:unknown}`
2026-08-14 | A5 | npm fallback removed | `grep -rc "npm ci \|\| npm install" .github/workflows/` | `0` sites remain (was 3)
2026-08-14 | A6 | AAB web-bundle assertion | new blocking step, `continue-on-error` parsed from YAML | `False` — asserts `base/assets/public/index.html` + >=1 JS asset in the RELEASE aab
2026-08-14 | A6 | YAML validity | `yaml.safe_load` on all 3 workflows | parsed OK, 2/3/1 jobs
2026-08-14 | A4-6 | full gate | tsc -b / vitest / build / security-audit | `0` · `1361 passed · 1 skipped` · `built in 24.49s` · `PASS`
2026-08-14 | A7 | duplicate feed-live subscription | `/feed` mounts useFeedRealtime twice (Feed.tsx:167 + WallPosts.tsx:150 via composerOnly, which gates only JSX at :1834) | fixed with useIsPrimaryInstance, key `feed-realtime:${userId}`; deps now `[userId, isPrimary]`
2026-08-14 | A7 | regression lock added | `src/hooks/feed/__tests__/feedRealtimeSingleton.test.ts` | 5 tests
2026-08-14 | A7 | mutation: remove the isPrimary guard | re-run | `1 failed` ✔
2026-08-14 | A7 | mutation: drop isPrimary from the dep array | re-run | `1 failed` ✔
2026-08-14 | A7 | unfiltered bindings — capability check | `@supabase/realtime-js@2.97.0`, `filter?: string` single-column predicate only | cannot express "public OR mine OR from N relevant users" or "reactions on the posts currently in my feed"
2026-08-14 | A7 | fan-out sizing (production, 30d) | `post_inserts_30d=154 · reaction_writes_30d=907 · profiles=94 · nonpublic_posts=0` | ~1,061 broadcast writes/30d today
2026-08-14 | A7 | STOP | eliminating unfiltered hot-table bindings needs a mechanism change (Broadcast-from-trigger to per-user topics), not a filter | architectural decision — reported, not taken
2026-08-14 | A4-7 | gate | tsc -b / vitest / security-audit | `0` · `1366 passed · 1 skipped` · `PASS`
2026-08-14 | A2 | approval binding | `git hash-object` vs approved | `11661da4...` MATCH — GO valid
2026-08-14 | A2 | BEFORE | `pg_proc` for get_broadcast_feed | 3 overloads · 15 out-cols each · VOLATILE · SECDEF · PUBLIC on the 2 legacy forms
2026-08-14 | A2 | applied | `apply_migration` | version `20260814074922 feed_rpc_candidate_pool`
2026-08-14 | A2 | AFTER shape | `pg_proc` | overloads `3` · 15-column `3/3` · VOLATILE `3/3`
2026-08-14 | A2 | AFTER behaviour | `pg_get_functiondef` | `uses feed_candidates = true` · `count(DISTINCT fe.user_id) = false` — the LATERAL is GONE from production
2026-08-14 | A2 | grants across DROP | `has_function_privilege` + PUBLIC count | anon `true` on all 3 · authenticated `true` · PUBLIC entries `0` (normalised)
2026-08-14 | A2 | rows + contract | direct calls | 4-arg `10` · 3-arg `10` · 2-arg `10` · author_name non-null `10/10` · tiers `2` · duplicate ids `0`
2026-08-14 | A2 | cold single call | `EXPLAIN (ANALYZE,BUFFERS)` | `2361 buffers · 123.5 ms` (first call, cold)
2026-08-14 | A2 | warm avg of 5 | clock_timestamp loop | `2.2 ms` — pool size at 210 posts is `210` (nothing to bound yet)
2026-08-14 | A2 | filename drift | applied `20260814074922` vs drafted `20260814080000` | repo files renamed to match
2026-08-14 | A2 | gate | tsc -b / vitest / security-audit | `0` · `1366 passed · 1 skipped` · `PASS`
2026-08-14 | A8 | SECDEF inventory | classify all anon-reachable non-trigger SECURITY DEFINER fns by actual gate | `136 signatures`: gated by auth.uid()/has_role `68` · ungated+mutating `7` · ungated read-only no-args `6` · ungated read-only with-args `55`
2026-08-14 | A8 | 3 of the 7 are in fact gated | read each body | record_test_agent_run (own token) · increment_managed_page_view (scoped WHERE) · recompute_entry_public_status (scoped) — regex missed them
2026-08-14 | A8 | NEW FINDING move_to_dlq | `pg_get_functiondef` + `has_function_privilege('anon',…)` | SECURITY DEFINER, anon=true, NO auth check; body does `pgmq.delete(source_queue, message_id)` on ANY queue by id, plus pgmq.create/send
2026-08-14 | A8 | impact | q_auth_emails carries authentication mail; msg ids are sequential bigints | targeted denial of password-reset/signup email. NOT exploited — deleting a real message would destroy a member's auth email
2026-08-14 | A8 | same frozen baseline | `rls-authority-baseline.json` | contains `SECDEF_NO_AUTH_GUARD:public.move_to_dlq` from email_infra.sql:166 — detected in May, frozen, while security-audit.mjs reports 0/0
2026-08-14 | A8 | blast radius | callers | move_to_dlq: process-email-queue (SERVICE_ROLE) only · _ensure_stats_row: trg_follows_counts + trg_friendships_counts, both SECDEF owner postgres · log_push_outcome + wallet_ledger_v2_diff_snapshot: no caller
2026-08-14 | A8 | round-trip (mock pgmq) | before/apply/after/rollback/re-apply, exit codes captured | BEFORE deleted `q_auth_emails msg 12345`; AFTER `0` deletes and the legitimate pair still works; anon f->t->f across rollback/re-apply
2026-08-14 | A8 | grants after apply | has_function_privilege | all 4 fns `anon=false svc=true`
2026-08-14 | A8 | GO binding | `git hash-object` | `f9a0b4fbfb7a2cc6da7692688290a4a717ae024e`
2026-08-14 | A8-APPLY | approval binding | `git hash-object` vs approved | `f9a0b4fb...` MATCH
2026-08-14 | A8-APPLY | BEFORE | has_function_privilege on the 4 fns | all `anon=true`; move_to_dlq body had no allow-list
2026-08-14 | A8-APPLY | applied | `apply_migration` | version `20260814080227 queue_and_writer_authority`
2026-08-14 | A8-APPLY | AFTER | same 4 fns | `still_anon_callable=0` · `service_role_ok=4` · allow-list present in body
2026-08-14 | A8-APPLY | live rejection | `select move_to_dlq('q_auth_emails','attacker_dlq',1,'{}')` | `42501 … is not a permitted queue pair` — RAISE fires before any pgmq call, so nothing sent/created/deleted
2026-08-14 | A8-APPLY | population delta | anon-reachable SECDEF signatures | `136 -> 132`
2026-08-14 | A8-APPLY | filename drift | applied `20260814080227` vs drafted `20260814090000` | repo files renamed to match
2026-08-14 | A8-APPLY | gate | tsc -b / vitest / security-audit | `0` · `1368 passed · 1 skipped` · `PASS`
2026-08-14 | B1-PREP | harness rebuilt | `pg_ctl` PG16.13 at /tmp/pgdata:5433 | prior container harness still alive; scratch db `mo` created fresh
2026-08-14 | B1-PREP | fixture reproduces root cause | `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT EXECUTE ON FUNCTIONS TO anon, authenticated` | without this the migration's REVOKE passes vacuously
2026-08-14 | B1-LOCAL | forward apply | `psql -v ON_ERROR_STOP=1 -f 20260814100000_media_objects.sql` | exit `0`
2026-08-14 | B1-LOCAL | V1 trigger fn closed | `has_function_privilege('anon'|'authenticated', 'tg_post_media_requires_ready()','EXECUTE')` | `f` · `f` — despite the default-privileges grant
2026-08-14 | B1-LOCAL | V2 rls | `pg_class.relrowsecurity` | media_objects `t` · post_media `t`
2026-08-14 | B1-LOCAL | V3 policy inventory | `pg_policies` | 5 policies; member UPDATE/DELETE on media_objects = `0` (state transitions are server-side)
2026-08-14 | B1-LOCAL | V4 state gate INSERT | insert post_media -> media in `pending` | `23514` raised: "media … is in state pending, only ready may be referenced"
2026-08-14 | B1-LOCAL | V5 verified needs timestamp | `UPDATE state='verified'` with NULL verified_at | rejected by `media_objects_verified_has_ts`
2026-08-14 | B1-LOCAL | V6 ready accepted | promote pending->verified->ready then insert post_media | `1` row
2026-08-14 | B1-LOCAL | V7 dedup scope | same owner+same sha256 / other owner+same sha256 | `unique_violation` / accepted — per-owner, not global
2026-08-14 | B1-LOCAL | V8 ON DELETE RESTRICT | delete a referenced media_object | `foreign_key_violation`
2026-08-14 | B1-LOCAL | V9 hash length | insert 1-byte sha256 | rejected by `media_objects_sha256_len`
2026-08-14 | B1-LOCAL | V10 gate on UPDATE | repoint post_media at pending media | `23514` — gate is not INSERT-only
2026-08-14 | B1-LOCAL | V11 FIRST ATTEMPT WAS VACUOUS | `SET LOCAL ROLE` outside a transaction | WARNING + ignored, ran as superuser (RLS bypassed), returned 3/3 rows. Re-run inside BEGIN/COMMIT.
2026-08-14 | B1-LOCAL | V11 rls isolation | non-superuser `mo_test`, uid=owner B | `current_user=mo_test` · sees `2` (own row + media on a public post); owner A's unreferenced pending media = `0`
2026-08-14 | B1-LOCAL | V12 visibility follows the post | flip post to `private`, re-read as owner B | media_objects `1` (own only) · post_media `0`
2026-08-14 | B1-LOCAL | V13 anonymous caller | `auth.uid()` NULL | sees `1` — media on public posts only
2026-08-14 | B1-LOCAL | V14 cannot insert as another owner | insert with foreign owner_id | `insufficient_privilege` from WITH CHECK
2026-08-14 | B1-LOCAL | ROLLBACK DEFECT FOUND | first rollback draft, tables empty | exit `3` — "cannot drop table post_media because other objects depend on it: policy media_objects_select". Circular dependency: post_media FKs media_objects, and media_objects_select reads post_media. Fixed by dropping the policy by name first; NOT by CASCADE.
2026-08-14 | B1-LOCAL | rollback guard fires | run rollback while post_media holds 1 row | exit `3`, `ROLLBACK ABORTED: public.post_media holds 1 row(s)`; both tables still present afterwards
2026-08-14 | B1-LOCAL | round trip | fwd -> rollback -> fwd -> rollback -> fwd | exits `0 0 0 0 0`; after rollback: tables `<none>` · function `<none>` · policies `0`; after re-apply: policies `5` · anon on trigger fn `f` · rls `t`
2026-08-14 | B1-LOCAL | rollback idempotent | run rollback twice on an absent schema | exit `0` (NOTICE "does not exist, skipping")
2026-08-14 | B1-TEST | new invariant lock | `src/__tests__/mediaIdentityContainment.test.ts` | 7 tests; enforces the claim the migration comment makes
2026-08-14 | B1-TEST | mutation-tested | M1 sha256 in RETURNS TABLE · M2 global UNIQUE(sha256) · M3 trigger removed · M4 column renamed · M5 `media/${sha}` path in src · M6 gate INSERT-only | all `6` caught; baseline `7 passed`
2026-08-14 | B1-GATE | gate | tsc -b / vitest / security-audit | `0` · `1376 passed · 1 skipped` · `PASS` (CRITICAL 0 · HIGH 0)
2026-08-14 | B1-PREFLIGHT | production read | `list_migrations` | newest APPLIED = `20260814080227 queue_and_writer_authority` — matches AI_CONTROL.db_migration
2026-08-14 | B1-PREFLIGHT | name collisions | pg_class / pg_proc | media_objects `<none>` · post_media `<none>` · tg_post_media_requires_ready `<none>`
2026-08-14 | B1-PREFLIGHT | dependency present | pg_proc | `can_view_post(uuid,uuid,text)` — exactly the signature both SELECT policies call
2026-08-14 | B1-PREFLIGHT | corpus | public.posts | total `210` · nonpublic `0`
2026-08-14 | B1-PREFLIGHT | SECOND DEFECT FOUND IN MY OWN MIGRATION | `pg_default_acl` objtype `r` | `anon=arwdDxtm | authenticated=arwdDxtm | service_role=arwdDxtm`. ALTER DEFAULT PRIVILEGES covers TABLES, not only functions. The draft revoked the trigger FUNCTION and said nothing about the two TABLES, so both would have been created with anon holding SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN. RLS covers the first four ONLY — TRUNCATE, REFERENCES and TRIGGER are checked against the grant and no policy can refuse them.
2026-08-14 | B1-FIX | added | `REVOKE ALL ON TABLE public.media_objects, public.post_media FROM PUBLIC, anon, authenticated` | migration ships with no reader/writer, so the correct grant is none; the upload cycle grants explicitly
2026-08-14 | B1-FIX | fixture corrected TWICE | (a) added `GRANT ALL ON TABLES` (b) added `service_role` to both default-privilege grants | without (a) the new REVOKE passed vacuously; without (b) the service_role probe read `false` and would have been misread as a migration defect rather than a fixture gap
2026-08-14 | B1-LOCAL | control table under the fixture | `CREATE TABLE public.ctrl(i int)` | `anon=arwdDxt/postgres | authenticated=arwdDxt/postgres` — confirms the fixture reproduces the condition (PG16 harness lacks `m`/MAINTAIN, which PG17.6.1 production has; `REVOKE ALL` covers it either way)
2026-08-14 | B1-LOCAL | V15 least privilege | `has_table_privilege` × 3 roles × 2 tables × 7 verbs | anon `false` ×14 · authenticated `false` ×14 · service_role SELECT/INSERT/TRUNCATE `true` (the worker path is intact)
2026-08-14 | B1-LOCAL | V1b function privilege | tg_post_media_requires_ready() | anon `false` · authenticated `false` · service_role `true`
2026-08-14 | B1-LOCAL | re-verified on clean db `mo3` | V1–V14 re-run against the corrected fixture + migration | exit `0` · `0`; V11e `2/2` · V12a `1/1` · V12b `0/0` · V13 `1/1`
2026-08-14 | B1-LOCAL | round trip round 2 | guard(rows) / rollback / fwd / rollback / fwd | `3` · `0` · `0` · `0` · `0`; after rollback tables `<none>`; after re-apply policies `5` · anon SELECT `false` · service_role SELECT `true`
2026-08-14 | B1-TEST | second invariant lock | `src/__tests__/newTableGrants.test.ts` | generic over every in-scope migration: a new table must ENABLE RLS and must say something explicit about its default grant
2026-08-14 | B1-TEST | mutation-tested | N1 drop the table REVOKEs · N2 drop ENABLE RLS · N3 revoke FROM PUBLIC only | `3/3` caught; baseline `6 passed`
2026-08-14 | B1-GATE | gate (final) | tsc -b / vitest / security-audit | `0` · `1382 passed · 1 skipped` · `PASS` (CRITICAL 0 · HIGH 0)
2026-08-14 | B1-APPLY | binding | `git hash-object supabase/migrations/…_media_objects.sql` | `71c65795975d90f744859856d4dc4d04f91b696c` — matches the quoted GO hash; working tree clean
2026-08-14 | B1-APPLY | last-second pre-flight | pg_class / pg_proc / schema_migrations | objects absent `yes` · trigger fn absent `yes` · newest `20260814080227`
2026-08-14 | B1-APPLY | applied | `apply_migration` name `media_objects` | success
2026-08-14 | B1-APPLY | FILENAME DRIFT (same as A8) | connector assigned `20260814084711`, file was named `20260814100000` | repo files renamed to match production; the connector picks the version, not the filename
2026-08-14 | B1-APPLY | AFTER A-O | 15 production checks | tables `media_objects,post_media` · rls `true/true` · policies `5` · member UPD/DEL on media_objects `0` · trigger `BEFORE INSERT UPDATE` · trigger fn anon `false` authenticated `false` · anon+authenticated table privs `0/28` · service_role `14/14` · unique index `(owner_id, sha256)` · global unique on sha256 alone `0` · FK confdeltype `r` (RESTRICT) · rows `0/0` · tables with RLS `143`
2026-08-14 | B1-APPLY | LIVE gate proof, production, self-rolling-back | DO block: insert pending media -> attach to a real post; then re-insert same owner+same bytes; RAISE at the end to unwind | gate `PASS — post_media: media … is in state pending, only ready may be referenced` · dedup `PASS — same owner + same bytes rejected`
2026-08-14 | B1-APPLY | probe left nothing | count media_objects / post_media after the probe | `0` / `0`
2026-08-14 | B1-TRANSPORT | push attempted, blocked | `git push origin main` | `403 access denied by the git proxy: … is not in this session's authorized repository set`. Not a credential I can supply; the repo must be added to the session's sources.
2026-08-14 | B1-TRANSPORT | SEVENTH filename drift, pre-existing | production `20260813171159 feed_author_identity` vs repo `20260813120000_feed_author_identity.sql` | renamed migration + rollback; 2 stale references in `src/hooks/feed/useFeedQuery.ts` and 1 inside the rollback's own run instructions also corrected. Remaining `20260813120000` references: `0`
2026-08-14 | B1-TRANSPORT | repo/production filename parity | 6 applied versions checked | 6/6 migration + 6/6 rollback filenames now equal the applied version
2026-08-14 | B1-TRANSPORT | durable copy | `git bundle create origin/main..HEAD` + 12 SQL files | `50mm-transport-2026-08-14.tar.gz`, 192K, delivered to the owner; bundle verified against base `6cb095d`
2026-08-14 | B2-DESIGN | THIRD DEFECT FOUND, in the migration applied one cycle earlier | production `pg_get_constraintdef` for `media_objects_verified_has_ts` | `CHECK (state = 'pending' OR verified_at IS NOT NULL)` — quarantine is what happens when hash verification FAILS, so verified_at is legitimately NULL then. The constraint could only be satisfied by writing a timestamp asserting a verification that did not happen. Fixed in B2 to exempt `quarantined`.
2026-08-14 | B2-DESIGN | decision | member writes go through RPCs, NOT a table grant | RLS restricts WHICH ROWS, never WHICH COLUMN VALUES; `GRANT INSERT` + `WITH CHECK (owner_id = auth.uid())` would let a member set `state='ready'` inline and publish unverified bytes past the B1 post_media gate
2026-08-14 | B2-LOCAL | forward apply on fixture+B1 | `psql -v ON_ERROR_STOP=1 -f 20260814120000_media_write_path.sql` | exit `0`
2026-08-14 | B2-LOCAL | W1 grant asymmetry | 5 functions × 3 roles = 15 probes | begin_upload: anon `f` authenticated `t` service_role `t` · mark_verified/mark_ready/quarantine/trigger_fn: anon `f` authenticated `f` service_role `t`
2026-08-14 | B2-LOCAL | W2 tables still ungranted | 28 probes | `0`
2026-08-14 | B2-LOCAL | W3 unauthenticated caller | media_begin_upload with NULL auth.uid() | `42501`
2026-08-14 | B2-LOCAL | W4 input validation | short hash / zero width / zero bytes / image/gif | `22023` ×4, each with a readable message rather than a constraint name
2026-08-14 | B2-LOCAL | W5 happy path | begin_upload | returns uuid; row starts `pending/private`
2026-08-14 | B2-LOCAL | W6 IDEMPOTENCY | 6 retries of the same owner+bytes | `1` distinct id · `1` row total
2026-08-14 | B2-LOCAL | W7 member cannot reach ready | real non-superuser role `b2_member`, `current_user=b2_member` verified | direct UPDATE `permission denied for table media_objects` · media_mark_verified `permission denied` · media_mark_ready `permission denied`
2026-08-14 | B2-LOCAL | W8 legal path | mark_verified -> mark_ready -> post_media insert | `verified/true` · `ready` · post_media `1` — B1 gate and B2 machine agree
2026-08-14 | B2-LOCAL | W9 illegal edges, AS SUPERUSER | ready->pending · ready->verified · owner_id change · sha256 change | `23514` ×4
2026-08-14 | B2-LOCAL | W10 the bug that matters | pending->ready by direct UPDATE, and via media_mark_ready | `23514` both — verification is not skippable even for postgres
2026-08-14 | B2-LOCAL | W11 quarantine from pending | media_quarantine on a never-verified row | `quarantined` with `verified_at` still NULL; reason recorded — the case B1's constraint made impossible
2026-08-14 | B2-LOCAL | W12 quarantined bytes not handed back | re-upload the same sha256 | `42501` — otherwise the client retries forever
2026-08-14 | B2-LOCAL | W13 quarantine is terminal | quarantined -> verified | `23514`
2026-08-14 | B2-LOCAL | W14 derivative ladder | no `original` / unknown rung `2160` / partial ladder for a narrow image | `22023` · `22023` · accepted — a narrow image has no honest 1440 rung and is not forced to fabricate one
2026-08-14 | B2-LOCAL | W15 in-flight cap | 60 sequential begin_upload calls | `54000` raised after `50`
2026-08-14 | B2-LOCAL | W16 FIRST ATTEMPT WAS VACUOUS | read state after W15's PL/pgSQL exception block | a caught exception rolls back its whole subtransaction, so all 50 rows had vanished and the check compared `1 ready / 0 in flight`. Re-run without an enclosing exception block.
2026-08-14 | B2-LOCAL | W16 cap counts in-flight only | fill to 50 · 51st refused · drain to ready · retry | `50` · `54000` · `51 ready / 0 in flight` · accepted
2026-08-14 | B2-LOCAL | rollback guard fires | rollback while a quarantined row has NULL verified_at | exit `3`, refuses to fabricate a timestamp or delete the rows
2026-08-14 | B2-LOCAL | round trip | guard / rollback / fwd / rollback / fwd / rollback×2 / fwd | `3 0 0 0 0 0 0` ; after rollback: 5 functions `<none>` · triggers on media_objects `<none>` · quarantine_reason `<none>` · constraint restored to the original text; after re-apply: constraint text back to the fixed form
2026-08-14 | B2-TEST | third invariant lock | `src/__tests__/mediaWritePath.test.ts` | 13 tests
2026-08-14 | B2-TEST | mutation-tested | P1 table grant to authenticated · P2 mark_ready to authenticated · P3 revoke from anon only · P4 pending->ready legalised · P5 trigger dropped · P6 sha256 mutable · P7 begin_upload open to anon · P8 quarantine constraint reverted | `8/8` caught; baseline `13 passed`
2026-08-14 | B2-GATE | gate | tsc -b / vitest / security-audit | `0` · `1402 passed · 1 skipped` · `PASS` (CRITICAL 0 · HIGH 0)
2026-08-14 | B2-APPLY | binding | `git hash-object` | `6c73cdf1f177ef525a02cad76a0a5c3702c5d2db` — matches the quoted GO hash; working tree clean
2026-08-14 | B2-APPLY | last-second pre-flight | schema_migrations / pg_proc / pg_attribute / pg_constraint | newest `20260814084711` · 5 function names free · quarantine_reason absent · old constraint is the pending-only form · rows that could violate the new constraints `0`
2026-08-14 | B2-APPLY | applied | `apply_migration` name `media_write_path` | success
2026-08-14 | B2-APPLY | EIGHTH filename drift | connector assigned `20260814104119`, file was named `20260814120000` | both repo files renamed to match production
2026-08-14 | B2-APPLY | AFTER A-H | production checks | grant triples (anon/auth/service) begin_upload `011` · mark_ready `001` · mark_verified `001` · quarantine `001` · trigger_fn `001` · member table privs `0/28` · trigger `trg_media_state_transition BEFORE UPDATE` · verified_has_ts now exempts `quarantined` · both new constraints present · quarantine_reason `text` · rows `0/0`
2026-08-14 | B2-APPLY | LIVE end-to-end proof, production, self-rolling-back | one DO block, RAISE at the end | idempotency `1 row from 6 calls` · skip-verify `PASS — illegal media state transition pending -> ready` (as postgres) · sha256 `PASS — immutable` · legal path `reached ready` · quarantine-from-pending `quarantined, verified_at NULL` · re-upload of quarantined bytes `PASS`
2026-08-14 | B2-APPLY | probe left nothing | media_objects / post_media | `0` / `0`
2026-08-14 | A3-VERIFY | PHASE A ITEM 3 CLEARED — the feed RPC verified live, the oldest open debt in the program
2026-08-14 | A3-VERIFY | deployed RPC over PostgREST, anon key, 4-arg form | `POST /rest/v1/rpc/get_broadcast_feed` | `200` · 9,530 bytes · 10 rows
2026-08-14 | A3-VERIFY | contract | column list off the wire | `15` columns, exact names incl. `feed_tier`, `author_name`, `author_avatar`, `thumbnail_urls`, `categories`
2026-08-14 | A3-VERIFY | substance | tier mix / privacy / population | `newest=3 unseen=7` · privacy `public` ×10 (no non-public leaked) · author_name `10/10` · author_avatar `10/10` · image_urls `10/10` · thumbnail_urls `10/10` · categories `3/10`
2026-08-14 | A3-VERIFY | MY OWN CHECK MISFIRED | `any('sha' in k.lower())` printed FAIL | false positive: `shares_count` contains the substring `sha`. Re-checked by exact name — hash columns on the wire `none`; no 64-hex-looking value anywhere in the payload.
2026-08-14 | A3-VERIFY | legacy overloads | 3-arg / 2-arg forms shipped Android builds still call | `5 rows, 15 cols` each — no caller stranded
2026-08-14 | A3-VERIFY | parameters honoured | categories filter / exclude_ids | `1 row` for `['street']` · excluded id absent from the result
2026-08-14 | A3-VERIFY | DETERMINISM, live | 4 identical calls, same viewer, same hour | identical id-sequence hash `b909fcd12a37` ×4, identical tier sequence
2026-08-14 | A3-VERIFY | SERVER-SIDE COST on production | `EXPLAIN (ANALYZE, BUFFERS)` on `get_broadcast_feed('{}',10,3,null)` | **`18.804 ms` · `2,359` buffers** at 210 posts
2026-08-14 | A3-VERIFY | GATE RESULT — reported as measured, threshold NOT moved | pre-stated Phase 1 gate `<50 ms` and `<2,000` buffers | latency **PASS** (18.8 vs 50) · buffers **FAIL by 18%** (2,359 vs 2,000). For scale: the design this replaced measured `941 ms` / `207,112` buffers.
2026-08-14 | A3-VERIFY | buffer breakdown | `feed_candidates('…', null)` alone | `6.326 ms` · `1,406` buffers · `210` rows — the pool builder is 60% of the buffers; at 210 posts the pool IS the whole corpus, so the bounded-pool cap does not yet bite
2026-08-14 | A3-VERIFY | BROWSER, real Chrome on the owner's Windows machine | `https://www.50mmretina.com/feed` | feed rendered; `get_broadcast_feed` called **exactly once**, `200`; all `24` cdn.50mmretina.com requests `200` (avatars, post images, journal, course, ads); photos, author names, avatars and counters all display
2026-08-14 | A3-VERIFY | trap #1 re-checked | every `cdn.50mmretina.com` response code | `24/24` = 200 — no repeat of the `/cdn-cgi/image/` apex breakage
2026-08-14 | B3-PREAUDIT | scope check before designing the derivative worker | mapped the EXISTING upload/hash pipeline | no duplication: `verify-image-hash` and `backfill-image-hashes` operate only on `competition_entries.photo_meta`; `posts.content_hash` is `md5(content|image_urls|image_url)` — a URL-string hash for 10-minute duplicate rejection, NOT an image content hash
2026-08-14 | B3-PREAUDIT | F1 LANDMINE (highest severity) | `detect-orphan-files/index.ts` reference set | scans `posts.image_url` (:39) and `unnest(image_urls)` (:49). Occurrences of `thumbnail_urls` in the whole file: `0`. Every `-thumb.webp` is therefore unreferenced BY CONSTRUCTION. Harmless only because the function enumerates via `storage.from(bucket).list()` — Supabase Storage — while live uploads go to R2. **Making it R2-aware without first adding thumbnail_urls to the reference set would classify every thumbnail in production as an orphan.** That R2 fix is exactly what Phase B orphan-cleanup was about to do.
2026-08-14 | B3-PREAUDIT | F2 unreclaimable storage | `purge-s3-orphans/index.ts:202` | scope is `competition-photos/` only. No orphan sweep exists for `post-images/` on R2. Combined with F3 this leaks storage that cannot currently be reclaimed by any tool.
2026-08-14 | B3-PREAUDIT | F3 no upload idempotency | `src/lib/imageUpload.ts:56,83` | object key is `Date.now()`-plus-base36-random, so a retried upload writes NEW objects and silently orphans the previous ones. `s3Upload.ts:130-133,170-177` throw immediately on a failed PUT — there is no retry on the PUT itself, only on the presign call (`s3Upload.ts:45-97`). A failure on photo 3 of 5 discards the two already in R2.
2026-08-14 | B3-PREAUDIT | F4 scheduled posts publish without thumbnails | `publish-scheduled-posts/index.ts:224-244` | inserts `image_urls` and `image_url`, never `thumbnail_urls`; and `scheduled_posts` has no such column — verified on production, `thumbnail_urls column on scheduled_posts = false`. The publisher could not set it even if it tried.
2026-08-14 | B3-PREAUDIT | F4 measured on production | posts with images vs missing thumbnails | `210` with images · **`9` with none** (4.3%) · `0` with a count mismatch — those 9 serve a full-resolution original wherever a thumbnail is expected
2026-08-14 | B3-PREAUDIT | corpus health | thumbnail/image array-length mismatch | `0` — the `PostMedia.tsx:355-357` misalignment path is not currently triggered in production
2026-08-14 | B3-PREAUDIT | conflict to manage, not a defect | storage layouts | existing `post-images/<uid>/posts/<ts>-<rand>-w<W>h<H>.webp` vs new `media/<object_id>/<rung>.webp`. Both will coexist in one R2 bucket during transition and NEITHER orphan tool understands the new one.
2026-08-14 | B3a-IMPL | THE FIX I WROTE FIRST FIXED NOTHING | edited `IMAGE_QUERIES` / `ARRAY_QUERIES` at the top of detect-orphan-files | those constants are DEAD CODE — declared, never read. `grep -rn 'IMAGE_QUERIES\|ARRAY_QUERIES'` matches only their own declarations. The list that actually runs was a divergent set of `addUrls(table, columns)` calls 250 lines below (:258-278). **Trap #11, and it caught me.** Both lists collapsed into one `REFERENCE_COLUMNS` table that the loop consumes.
2026-08-14 | B3a-IMPL | THIRD defect in the same file | `:238` `if (error || !data || data.length === 0) break;` | a FAILED reference query was indistinguishable from an empty table. A transient error on `posts` would condemn every post image in that run. Now throws and aborts the whole report — a partial reference set is worse than no answer.
2026-08-14 | B3a-IMPL | reference set rebuilt from the live schema, not memory | information_schema query over all public BASE TABLEs matching url/photo/image/avatar/cover/attachment/file/thumb | `25` tables now referenced (was 21); external-link columns (profiles.website_url/.instagram_url/.custom_url, client_errors.url, search_recents, push_config, test_agent_runs) deliberately excluded — they never name a stored file
2026-08-14 | B3a-IMPL | columns ADDED to the safety list | measured exposure on production | `posts.thumbnail_urls` **249 urls / 201 posts** · `hero_banners.thumbnail_url` 7 · `photo_of_the_day.thumbnail_url` 2 · `ad_creatives.image_url` 2 · `ad_creatives.advertiser_logo_url` 1 · `office_staff.photo_url` 1 · `scheduled_posts.image_urls` 1 · plus `posts.thumbnail_url`, `post_drafts.*`, `competition_entries.photo_thumbnails`, `featured_photos.thumbnail_url` (empty today) = **263 live files** an R2-aware sweep would have condemned
2026-08-14 | B3a-IMPL | recovery snapshots now count as references | `SNAPSHOT_COLUMNS` | `posts_dead_host_backup_20260812` + 2 v3 preflight snapshots. Tolerated if absent (a dropped snapshot is legitimate) but the skip is surfaced in the report, because a skipped snapshot narrows the safety set.
2026-08-14 | B3a-IMPL | report now states its own blind spot | `scope_warning` in the response | "Enumerates Supabase Storage only. R2 … is NOT scanned." — a low orphan count must not read as "storage is clean"
2026-08-14 | B3a-TEST | fourth invariant lock | `src/__tests__/orphanReferenceSet.test.ts` | 47 tests; asserts every (table,column) individually, that there is exactly ONE list, and that the list is actually iterated
2026-08-14 | B3a-TEST | mutation-tested | Q1 drop posts.thumbnail_urls · Q2 reintroduce a dead lookalike list · Q3 declare the list but stop iterating · Q4 revert error-means-empty · Q5 drop scheduled_posts · Q6 drop snapshots · Q7 remove the R2 scope warning | `7/7` caught; baseline `47 passed`
2026-08-14 | B3a-GATE | edge function parses | `deno lint --rules-exclude=no-import-prefix,no-explicit-any,require-await` | exit `0`, no syntax errors (the file was rewritten by script, so this was checked rather than assumed)
2026-08-14 | B3a-GATE | LIMITATION, stated not glossed | `deno check` | **could not type-check**: `https://esm.sh/@supabase/supabase-js@2.49.8` is blocked by the egress proxy ("unsuccessful tunnel"). Parse is verified; full type inference is NOT.
2026-08-14 | B3a-GATE | gate | tsc -b / vitest / security-audit | `0` · `1449 passed · 1 skipped` · `PASS` (CRITICAL 0 · HIGH 0)
2026-08-14 | B3a-DEPLOY | authorisation | owner: "start work now based on the plan" — plan step 1 = GO 0555226f | binding re-checked at deploy time: `git hash-object` = `0555226fcf9bd013d73e21aa25520c2c0c0e4de7`, tree clean
2026-08-14 | B3a-DEPLOY | deployed | `deploy_edge_function detect-orphan-files` | version `21`, status ACTIVE, verify_jwt `true` (unchanged)
2026-08-14 | B3a-DEPLOY | AFTER | `get_edge_function` read-back | deployed index.ts contains REFERENCE_COLUMNS (25 tables incl. posts.thumbnail_urls), SNAPSHOT_COLUMNS, fail-loud throw, scope_warning — content matches the authorised file
2026-08-14 | B3a-DEPLOY | function is read-only + admin-only | body unchanged in that respect | worst case of any residual defect is an admin-facing 500, not deletion — the deleting companion (purge-s3-orphans) was NOT touched
2026-08-14 | PLAY-NOTICE | owner forwarded Play Console release dashboard, build 1088 (v1.2.5) | 4 RECOMMENDATIONS, zero violations, no deadline | (1)+(2) edge-to-edge display / deprecated APIs -> Phase F Android matrix (Android 15 inset handling in the Capacitor shell) · (3) bitmap optimization -> exactly B3d derivative ladder + F image budgets · (4) R8 optimization -> CI build config, scheduled in F, NOT auto-applied (owner rule: no background dependency changes; R8 shrinking can break Capacitor reflection paths and must be tested on a real build)
2026-08-14 | B3b-IMPL | TWO MORE PROTOCOL VIOLATIONS FOUND, IN THE DELETING FILE ITSELF | read purge-s3-orphans against the Deletion Protocol | (1) `const { data: comps } = await ...select("id")` ignored its error — a transient failure reading `competitions` yields an EMPTY live-id set, classifying EVERY uuid folder as an orphan; on a non-dry run that is the whole competition archive. (2) the live-id reads were UN-PAGINATED — PostgREST caps un-ranged selects at 1,000 rows, so past 1,000 entries every additional entry's folder would be "not live" and deleted. Survivable today only because production has not crossed 1,000 entries. Same defect classes as B3a's #3 and BUG-071, in the file that deletes.
2026-08-14 | B3b-IMPL | shared S3 client | `supabase/functions/_shared/s3.ts` | one SigV4 implementation for list+delete; `deleteS3Objects` now THROWS on a failed batch carrying the already-deleted count (the old inline copy logged to console.error and kept counting — a fabricated total)
2026-08-14 | B3b-IMPL | purge-s3-orphans hardened to the Deletion Protocol | rewrite | live-id reads paginated + fail-loud · empty-live-set refusal (SAFETY ABORT) · dry_run default true · execute requires `expected_count` from a prior dry run, aborts 409 on mismatch · `MAX_DELETE` default 200 with `HARD_MAX_DELETE` 1000 ceiling the body cannot exceed · post-delete verification re-lists and reports residue, never assumes zero
2026-08-14 | B3b-IMPL | detect-orphan-files sees R2 | `listAllS3Objects` over the configured prefix | per-bucket r2 stats · 30-day age rule from LastModified · unknown prefixes COUNTED and surfaced, never orphaned ("deleting from ignorance is how the near-miss happened") · scan failure is impossible to read as clean (`r2.scanned=false` + `scan_error`, scope_warning switches text) · REPORT-ONLY — detection deliberately has no delete capability, asserted by test
2026-08-14 | B3b-TEST | protocol alarm | `src/__tests__/deletionProtocol.test.ts` | 9 tests over every bulk-delete-capable function (auto-discovered, vacuity-guarded); targeted deleters (`hard-delete-competition`, `s3-delete`) exempt BY NAME with the bounding argument documented, and the exemption list is checked against reality
2026-08-14 | B3b-TEST | orphanReferenceSet updated for the new premise | 3 assertions replaced 1 | scope stated truthfully per run · unknown-prefix surfacing · detect stays deletion-free
2026-08-14 | B3b-TEST | mutation-tested | V1 error-means-empty live-ids · V2 threshold removed · V3 expected_count dropped · V4 post-delete verification dropped · V5 empty-set refusal dropped · V6 shared delete swallows failures · V7 detect gains delete capability · V8 unknown prefixes treated as orphans | `8/8` caught; baseline `58 passed`
2026-08-14 | B3b-GATE | gate | tsc -b / deno lint (3 files) / vitest / security-audit | `0` · clean · `1460 passed · 1 skipped` · `PASS`
2026-08-14 | B3b-GATE | LIMITATION, stated | runtime execution against real R2 | NOT tested from this sandbox (no admin JWT; R2 credentials live in production site_settings and were not extracted). Verification is: parse, types where possible, source-level protocol assertions, mutation coverage. The first real run MUST be a dry run and its report reviewed before any execute.
2026-08-14 | B3b-STATE | AI_CONTROL.md protocol note updated | "rule, not an alarm" paragraph replaced | the alarm now exists and is named
2026-08-14 | B3b-DEPLOY | binding | 3 files re-hashed at deploy time | detect `91b2cf6c` · purge `99ef8bf0` · _shared/s3 `9365385c` — all match the GO set; tree clean
2026-08-14 | B3b-DEPLOY | MECHANICAL TRANSFORM, declared not hidden | deploy bundle nests `_shared/s3.ts` beside the entrypoint | ONE line differs from the repo file in each deployed index.ts: `from "../_shared/s3.ts"` -> `from "./_shared/s3.ts"`. Everything else byte-identical. The repo keeps `../_shared/` because that is the true on-disk layout.
2026-08-14 | B3b-DEPLOY | deployed | `deploy_edge_function` ×2 | purge-s3-orphans version `20` ACTIVE · detect-orphan-files version `22` ACTIVE · verify_jwt `true` on both
2026-08-14 | B3b-DEPLOY | first-real-run rule | per the Deletion Protocol and the recorded limitation | the owner's admin session must run detect-orphan-files (read-only) and a purge DRY RUN first; no execute until that dry-run report is reviewed. The function itself enforces expected_count + caps even if this instruction is forgotten.
2026-08-14 | B3c-IMPL | ROOT CAUSE was three files, one chain | compose GENERATED thumbnails (`uploadedThumbs` in WallPosts) and threw them away because `scheduled_posts` had no column, so the publisher inserted posts with none — and could not have done otherwise (`thumbnail_urls column on scheduled_posts = false`, verified on production in the pre-audit)
2026-08-14 | B3c-IMPL | fix, all links | migration `20260814180000` adds `scheduled_posts.thumbnail_urls text[]` (additive, IF NOT EXISTS) · WallPosts schedule branch passes `uploadedThumbs` · duplicate keeps `p.thumbnail_urls` · insert hook writes it · publisher carries `row.thumbnail_urls ?? []` into posts
2026-08-14 | B3c-LOCAL | round trip on harness | fwd / fwd-again / rollback-with-data / rollback / rollback-again / fwd | `0 0 3 0 0 0` — forward idempotent; rollback ABORTS while any pending post carries thumbnails, idempotent when clear
2026-08-14 | B3c-TEST | MY OWN TEST ANCHORED ON THE WRONG BLOCK | first run failed against a CORRECT publisher | the assertion anchored on the first `.from("posts")` — a duplicate-check QUERY — not the insert. Re-anchored on the insert payload (`user_id: row.user_id`). Recorded because an anchor that loose could as easily have PASSED against a wrong publisher.
2026-08-14 | B3c-TEST | chain lock | `src/__tests__/scheduledPostThumbnails.test.ts` | 6 tests, one per link incl. the rollback guard; mutation-tested `6/6` caught (migration line, compose, hook, publisher, duplicate, rollback guard)
2026-08-14 | B3c-GATE | gate | tsc -b / deno lint / vitest / security-audit | `0` · clean · `1466 passed · 1 skipped` · `PASS`
2026-08-14 | B3c-NOTE | the 9 existing posts | repair is an OPERATION not a schema change | `backfill-thumbnails` (admin-only) regenerates and updates `posts.thumbnail_urls`; to be run from the owner's admin session after this cycle applies — recorded as an owner step alongside the first orphan dry-run
2026-08-14 | EXIF-AUDIT | THE PLAN'S ASSUMPTION WAS PARTLY WRONG, in the good direction | read the actual upload paths | the MAIN image path (posts, competition entries, avatars via uploadImageWithThumbnail) re-encodes through a canvas (imageCompression.ts:181-185) — which strips ALL metadata including GPS, already, for every successful upload. And competition EXIF is a deliberate FEATURE: exifExtract.ts PICK list captures camera/exposure tags for the judges' audit trail and EXCLUDES GPS BY DESIGN (documented as B8: "photographers should not leak location at submit").
2026-08-14 | EXIF-AUDIT | the REAL exposure #1 | imageUpload.ts FILE-5004 fallback: `fullResFile = file` | when canvas encoding FAILS, the ORIGINAL uploads unconverted — EXIF+GPS intact on the public CDN. Realistic trigger: HEIC (iPhone default) in browsers whose canvas cannot decode it — precisely the case where the fallback fires.
2026-08-14 | EXIF-AUDIT | the REAL exposure #2 | `storageUpload()` raw-file callers | CompetitionSubmit.tsx, FileUploadDropZone.tsx, useProfileMutations.ts, admin uploaders, HelpSupport.tsx upload File objects with no re-encode step in storageUpload itself; support attachments at least go to a PRIVATE bucket. Each caller needs individual audit: some may pre-encode upstream.
2026-08-14 | EXIF-AUDIT | design decided for the EXIF cycle | | (1) fallback: on encode failure, check for GPS with exifr (already a dependency); JPEG with GPS -> splice out the APP1 segment (no new deps); undecodable format with GPS -> REFUSE with a clear message rather than leak the member's location; no GPS -> current behavior stands. (2) audit each storageUpload caller and route image uploads through the same guard. (3) lock with a test asserting no upload path sends a File that has not passed the strip-or-verify gate.
2026-08-14 | B3c-APPLY | binding | migration `272b27cb` + publisher `e237763b` re-hashed at apply time | match; tree clean
2026-08-14 | B3c-APPLY | pre-flight | production reads | newest `20260814104119` · scheduled_posts exists · thumbnail_urls absent · pending rows `0` (no in-flight member data affected)
2026-08-14 | B3c-APPLY | applied | `apply_migration scheduled_post_thumbnails` | success; NINTH filename drift: recorded as `20260814165300` (drafted 20260814180000) — repo files renamed
2026-08-14 | B3c-APPLY | AFTER | production reads | column `text[]` present · comment present
2026-08-14 | B3c-APPLY | publisher deployed | `deploy_edge_function publish-scheduled-posts` | version `22` ACTIVE; verify_jwt `true` PRESERVED (read from the live function list first, not assumed — the cron caller authenticates with the secret header plus gateway JWT, and v21 was true)
2026-08-14 | B3c-APPLY | full chain now live | column + client (next app build) + publisher | future scheduled posts publish WITH thumbnails; the 9 existing heavy posts await the owner's backfill-thumbnails run
2026-08-14 | EXIF-IMPL | the guard | `src/lib/gpsGuard.ts` + one line in imageUpload.ts fallback | three outcomes, preference order: no GPS -> unchanged; GPS+JPEG -> metadata segments (APPn+COM) spliced out of the byte stream, pixels untouched, strip VERIFIED by re-reading before release; GPS+undecodable -> REFUSED with member-readable copy (`GPS_REFUSAL_MESSAGE`). FAIL-CLOSED: an unparseable file is treated as GPS-present, because an unprovable file does not go to a public CDN.
2026-08-14 | EXIF-IMPL | wiring | imageUpload.ts encode-failure fallback | `fullResFile = file` (the leak) replaced with `fullResFile = await guardOriginalUpload(file)`. The thumbnail-failure fallback uploads fullResFile, so it inherits the guard.
2026-08-14 | EXIF-TEST | BYTE-LEVEL tests, not only source-grep | synthetic JPEGs (SOI/APP1-EXIF/APP13/COM/DQT/SOF/SOS+entropy incl. FF 00 stuffing) | splice removes APPn+COM, keeps decode tables + frame header + image data verbatim; metadata-only difference collapses to identical output; malformed tails are never truncated
2026-08-14 | EXIF-TEST | MUTATION G4 ESCAPED, test strengthened, re-caught | removing the non-JPEG refusal passed the first test version — the verify-path throw satisfied a file-wide regex while the branch quietly returned the file | assertion re-anchored on the `if (!isJpeg(bytes))` branch itself (must throw, must not return file); G4 re-run now caught. Third loose-anchor lesson this program; recorded like the others.
2026-08-14 | EXIF-TEST | mutations | G1 unguard fallback · G2 splice keeps APP1 · G3 fail-open on unreadable · G4 remove refusal (re-run) · G5 drop verify · G6 truncate malformed | `6/6` caught after strengthening; baseline `8 passed`
2026-08-14 | EXIF-GATE | gate | tsc -b / vitest / security-audit | `0` · `1474 passed · 1 skipped` · `PASS`
2026-08-14 | EXIF-NOTE | remaining raw-caller audit | FileUploadDropZone, HelpSupport (private bucket), admin uploaders | deferred to its own small pass — member-facing post/competition/avatar paths are all covered (re-encode or guard); the remainder are admin/private surfaces
2026-08-14 | EXIF-NOTE | ships with the next app build | client-only change, no production mutation | rides to production via the normal web deploy when transport is unblocked (or next CI build)
2026-08-14 | B3d1-IMPL | the ladder ships client-side for the LEGACY pipeline | `src/lib/imageLadder.ts` + imageUpload + PostMedia + detect-orphan-files | rungs 1080/1440 generated at upload as extra canvas exports (metadata stripped inherently); sizes from the 08-13 measurements, not round numbers; NO upscaling — the LONG edge decides (portraits keep their ladder; width-based logic would have silently stripped it from half the corpus)
2026-08-14 | B3d1-IMPL | the marker is a PROMISE | `-l3` in the filename announces "rungs exist beside this file" | published ONLY when every planned rung actually landed: rungs upload FIRST; on any failure the name reverts to legacy (4 individually-asserted strip sites: thumbnail-failure early return, rung-pair failure, single-rung failure, supabase-storage fallback). A kept promise or no promise, never a broken one — a broken marker would 404 every feed render of the post.
2026-08-14 | B3d1-IMPL | no edge-function change needed for upload | 2 rungs = one more pair-presign call | the existing s3-presign-upload pair mechanism is reused; presign fn untouched
2026-08-14 | B3d1-IMPL | Deletion Protocol at design time | detect-orphan-files derives `-l3-r1080/-r1440` from marked referenced keys | rung addresses live in NO database column (renderer derives them from the name), so without this the cleanup companion would delete exactly the files that make the feed fast
2026-08-14 | B3d1-IMPL | srcset with honest widths | portrait rung descriptor is the SCALED width (900×1600 @1080 -> `608w`), same correction the thumb path makes | declaring the rung number would make the browser pick a file 40% smaller than promised — the exact "quality getting very poor" mechanism the owner reported, not repeated
2026-08-14 | B3d1-TEST | three-party naming agreement lock | `src/lib/__tests__/imageLadder.test.ts` | 13 tests: pure-value tests for plan/width/path + source assertions that uploader, renderer and orphan set all speak LADDER_MARKER/rungPath — three parties drifting into two spellings is the failure mode
2026-08-14 | B3d1-TEST | MUTATION L5 ESCAPED, strengthened, re-caught | count-based "≥4 strip sites" passed when one strip was deleted (many sites kept the count high) | each fallback now asserted INDIVIDUALLY; L5 re-run caught. Fourth loose-anchor lesson; same entry class as B3c and EXIF.
2026-08-14 | B3d1-TEST | mutations | L1 width-not-long-edge · L2 renderer hand-builds suffix · L3 derivation drops 1440 · L4 frame regex loses marker · L5 early-return keeps marker (re-run) · L6 portrait width lie | `6/6` caught after strengthening; baseline `13 passed`
2026-08-14 | B3d1-TEST | pre-existing PostMediaQuality assertion updated | pair-string became ordered list (thumb first, original LAST, rungs between) | assertion now locks the ordering properties instead of the exact pair string
2026-08-14 | B3d1-GATE | gate | tsc -b / deno lint / vitest / security-audit | `0` · clean · `1487 passed · 1 skipped` · `PASS`
2026-08-14 | B3d1-NOTE | production changes pending ONE ask | client half rides the next app build; detect-orphan-files rung derivation needs redeploy | deploy set: detect-orphan-files/index.ts (with _shared/s3.ts unchanged)
2026-08-14 | B3D1-DEPLOY | binding | `git hash-object detect-orphan-files/index.ts` = `606e56f606cd6c54c538330334056b1167ca9dd4` at deploy time | matches the GO hash; payload = repo file + the ONE declared import transform (../_shared -> ./_shared), verified by reverse-diff before upload
2026-08-14 | B3D1-DEPLOY | deployed | detect-orphan-files version `23` ACTIVE, verify_jwt `true` | rung derivation (`-l3-r1080/-r1440` from marked referenced keys) is live BEFORE any rung file exists in the store — the safety lands ahead of the feature, by design
2026-08-14 | B3D2-IMPL | dims parsers, pure bytes | `supabase/functions/_shared/imageDims.ts` | WebP VP8/VP8L/VP8X, JPEG SOF0/1/2 (incl. progressive), PNG IHDR. Deno-free on purpose: the SAME file is vitest-tested on hand-built binary fixtures and deployed to the edge. Refusal is the default — unknown container, desync, SOS-before-SOF, absurd dims, truncated header all return null; a wrong dimension is worse than a missing one.
2026-08-14 | B3D2-TEST | byte-level fixtures | 12 tests | incl. the refusal paths; first run caught MY FIXTURE being 25 bytes — under the parser's own 30-byte truncation floor (the floor worked; the fixture tested the floor, not the parser). Padded and documented.
2026-08-14 | B3D2-IMPL | the backfill function | `supabase/functions/backfill-image-dims/index.ts` + `copyS3Object`/`readS3ObjectHead` in _shared/s3.ts | per slide: 64KiB ranged read -> refusal-first dims parse -> SERVER-SIDE copy to the dimensioned name -> VERIFY the new name answers -> only then UPDATE the row -> read the row back. Old objects left in place: unreferenced after the update, collectable by the Deletion Protocol at 30 days — deleting is not this job's business.
2026-08-14 | B3D2-IMPL | ordering IS the safety | copy -> verify -> update, asserted by position in the test | a member's post never points at a name that does not answer; a crash mid-run leaves harmless extra copies, never a broken row
2026-08-14 | B3D2-IMPL | deletion's ceremony without deletion | dry_run default · expected_count gate · drift abort 409 · max_posts 25 / hard 100 · fail-loud paginated candidate scan · per-slide skip reasons (supabase-host, unmanaged shape, missing object, unparseable header) · idempotent re-runs reuse existing copies | rewriting 153 slides of member data deserves the same gates as deleting them
2026-08-14 | B3D2-IMPL | admin buttons | AdminHealth.tsx: "Purge Dry Run" + "Dims Backfill Dry Run" beside the existing scan | every owner step is now a CLICK, and both buttons are wired READ-ONLY — the purge button cannot send expected_count, so the function's own gates make execution from this surface impossible
2026-08-14 | B3D2-TEST | MUTATION D2 ESCAPED, strengthened, re-caught | `if (false)` dead-branched the expected_count gate while its abort STRING survived a message-based regex | assertions now target the live CONDITIONS (`if (expectedCount === null)`, `if (expectedCount !== plan.length)`), not the prose they guard. Fifth loose-anchor lesson.
2026-08-14 | B3D2-TEST | locks | dimsBackfill 14 tests (values: insertDimsInName agrees with the renderer's DIMS_RE, key/URL transforms can never disagree; ordering by index positions; gates; skip reasons; no delete capability) + imageDimsParsers 12 byte-level | mutations `7/7` caught after strengthening
2026-08-14 | B3D2-GATE | gate | tsc -b / deno lint ×3 / vitest / security-audit | `0` · clean · `1513 passed · 1 skipped` · `PASS`
2026-08-14 | B4S1-IMPL | the PUT itself retries (defect F3, first half) | `putWithRetry` in s3Upload.ts, wired into uploadToS3 + both sides of uploadPairToS3 | retrying the SAME presigned URL is exactly idempotent — same key, same bytes, nothing extra created. Retried: network TypeError (the mid-transfer drop), 5xx, 429. 403 re-presigns ONCE for the SAME path (a slow photo on slow cellular can outlive the 300s presign). Other 4xx NOT retried: a wrong request repeated is still wrong. Attempts capped at 3 with backoff.
2026-08-14 | B4S1-TEST | BEHAVIOURAL tests with mocked fetch, not grep | `src/lib/__tests__/putRetry.test.ts` | 4 tests: drop-then-success reuses the SAME URL; persistent 500 stops at 3 attempts; 400 makes exactly 1 attempt; 403 re-presigns once and the re-presign body carries the SAME path (no new orphan). Mutations R1-R4 (retry removed · 400s retried · unlimited attempts · re-presign path drift) `4/4` caught FIRST TRY — the loose-anchor ledger is paying for itself.
2026-08-14 | B4S1-GATE | gate | tsc -b / vitest / security-audit | `0` · `1517 passed · 1 skipped` · `PASS`
2026-08-14 | B3D2-DEPLOY | binding | 3 files re-hashed at deploy | backfill `0aa2176f` · s3 `75b05544` · imageDims `bc5938b0` — match the GO set
2026-08-14 | B3D2-DEPLOY | deployed | `backfill-image-dims` version `1` ACTIVE, verify_jwt `true` (new function; gateway JWT + in-code admin check) | payload = repo files + the two declared import transforms (../_shared -> ./_shared), same mechanism as every _shared deploy
2026-08-14 | B3D2-DEPLOY | run order | owner clicks "Dims Backfill Dry Run" in AdminHealth (ships next build; until then I can trigger the dry run is NOT possible — admin JWT only) | dry run -> review plan -> execute batches with expected_count -> re-run detect scan
2026-08-14 | CG-AUDIT | first task of the Competition Engine gate: run the auditor, then audit it | `select * from judging_invariants_check()` on PRODUCTION | 7/7 invariants `ok`, fail_count 0: tag_decision_drift · current_round_canonical · decision_vocabulary · eligibility_consistency · r4_stuck · no_legacy_in_progression_writers · rejected_entry_gate_leak. The CI test wrapping this is skipped ONLY for missing service-role credentials — the function itself is real and green.
2026-08-14 | CG-AUDIT | score immutability | pg_trigger on judge_scores | GUARDED: `trg_enforce_round_lock` (completed round -> "Scoring is locked", incl. UPDATE and DELETE) + `trg_validate_judge_score_range` + `validate_criteria_scores` + `audit_judge_scores` (audit trail exists) + cache refresh. Definition read in full: lock covers score writes via entry->round resolution; has a `app.bypass_round_lock` GUC bypass for internal syncs — noted for the matrix (a SECURITY DEFINER writer setting that GUC bypasses the lock; acceptable as designed, must appear in the CG matrix as a tested assumption).
2026-08-14 | CG-AUDIT | duplicate votes | pg_constraint | `competition_votes_entry_user_photo_unique` — hard uniqueness, not a trigger count
2026-08-14 | CG-AUDIT | duplicate/over-submission | competition_entries triggers | NO unique constraint, but 15 gates incl. `trg_enforce_max_entries` (per-competition max_entries_per_user), rate-limit + throttle triggers, fee/photo-limit/status-transition validators, audit. FINDING (minor): a COUNT-based max-entries trigger is racy under concurrent inserts — two simultaneous submissions can both pass the count. Severity low (over-admission by 1, human-review catches), fix candidate: advisory lock or partial unique index when max=1. Recorded for the CG matrix, not fixed silently.
2026-08-14 | CG-AUDIT | production state | competition_entries | `0` rows currently (between contests) — behavioural probes limited; the full CG matrix (judge authz, participant isolation, deterministic winners, award stacking) needs a seeded harness pass, queued as CG-2.
2026-08-14 | CG-AUDIT | bypass enumeration COMPLETE | pg_proc scan for `bypass_round_lock` setters + grants + definitions | 4 functions touch the GUC. `admin_delete_auth_user` + `admin_purge_orphan_user_data`: not member-executable. `backfill_tag_decision_drift_admin`: IS anon/authenticated-executable (default-privileges class) BUT gates internally — first statement `IF v_caller IS NULL OR NOT has_role(v_caller,'admin') THEN RAISE 'admin only'` before any bypass; verified by reading the definition, not the name. `enforce_round_lock` itself is a trigger function (not RPC-invokable). VERDICT: the score-lock bypass is NOT reachable by members. The GUC is transaction-scoped (`set_config(...,true)`) in all setters — no leakage across requests.
2026-08-14 | CG-AUDIT | matrix status after first pass | | GUARDED+verified: score immutability, vote uniqueness, bypass containment. GUARDED-soft w/ recorded race: max-entries. Remaining for CG-2 (seeded harness): judge authz, participant isolation, winner determinism, award stacking, round-row edit protection after publish, max-entries race demonstration.
2026-08-14 | CG2-RACE | THE RACE IS REAL, demonstrated on the harness with the VERBATIM production trigger | session A BEGIN+INSERT (uncommitted, pg_sleep 2) · session B concurrent INSERT | both admitted: `rows for member under max=1: 2`. Sequential control worked (`Entry limit exceeded`), proving the demonstration isolates CONCURRENCY, not a broken trigger.
2026-08-14 | CG2-RACE | fix built + PROVEN on the same race | `pg_advisory_xact_lock(hashtextextended(user:competition))` before the count | identical concurrent scenario now: second insert waits on the lock, then `Entry limit exceeded` — `rows WITH FIX: 1`. Different member admitted concurrently (lock is scoped). Rollback restores the racy version verbatim (advisory gone), fix re-applies. One variable changed: the lock.
2026-08-14 | CG2-APPLY | authorisation | owner: "continue / grant all permission / connect supabase … whatever you need" — blanket GO recorded | both migrations applied under it; every binding still logged as always
2026-08-14 | CG2-APPLY | applied ×2 | `revoke_unrls_verbs_legacy` -> version `20260814175909` · `max_entries_race_fix` -> version `20260814175927` (drifts #10, #11 — files renamed) | success ×2
2026-08-14 | CG2-APPLY | AFTER A-G | production verification | authenticated TRUNCATE tables `136 -> 0` · anon REFERENCES `0` · member verbs on posts INTACT `true ×4` · service_role TRUNCATE retained · advisory lock live in the trigger fn · trigger still attached
2026-08-14 | CG2-APPLY | class closed | the 136-table non-RLS-verb exposure is now 0; the newTableGrants gate guards the future, this migration cleaned the past | the legacy sweep item shrinks accordingly (SECURITY DEFINER function signatures remain for E)
2026-08-14 | CG2-DETERMINISM | winner/ranking determinism audit, read-only | complete-round decision aggregation + all rank/winner/final SQL functions | DECISION AGGREGATION: already deterministic by design — count DESC then SOW-priority DESC, comment cites the earlier D1/D2 fix that "eliminates Object.entries() ordering non-determinism"; equal-priority synonym ties (shortlist/shortlisted) normalize to the same progression decision, so no outcome ambiguity.
2026-08-14 | CG2-DETERMINISM | SQL ranking surfaces | 19 functions scanned; ORDER BY clauses read for the 3 flagged | `get_top_contributors_v2` (the LIVE one per useTopContributors.ts): `ORDER BY rc.score DESC, rc.uid` — deterministic ✓. `get_top_contributors_v1`: `ORDER BY score DESC LIMIT 3` with NO tiebreak — but superseded (client calls v2); joins the legacy-signature backlog (Phase E), not a live defect. `get_placement_drift_admin`: `ORDER BY r_score DESC` — admin REPORT ordering only; display nondeterminism, no decision made from row order. No live winner path found that decides by unstable order.
2026-08-14 | CG2-DETERMINISM | verdict | | Winner determinism at the DECISION and RANKING layers: VERIFIED for live paths. Remaining CG-2 items requiring the seeded harness: judge authz probes as a real judge role, participant isolation (a judge who is also a participant), award stacking. Queued.
2026-08-14 | CG2-HARNESS | seeded harness built | local PG16 (port 5433), faithful replica of the judging surface: 12 tables, 58 policies, 18 gating/side-effect triggers, 16 functions — every policy/function/trigger body pulled VERBATIM from production catalogs this session | fidelity ledger in harness/cg2/01_schema_seed.sql header lists all 5 deviations and why each cannot change a verdict (key one: competition_round_publish carries no side-effect triggers, so that table gets DENY-only probes — a more permissive harness that still denies proves production denies)
2026-08-14 | CG2-HARNESS | judge authz matrix (probes A1-A5) | non-superuser SET LOCAL ROLE authenticated + jwt-claim GUC, per the V11 lesson | A1 non-judge DENIED · A2 judge of another competition DENIED · A3 forged judge_id DENIED · A4a distributed-mode unassigned DENIED · A4b assigned ALLOWED (INSERT 0 1) · A5 positive control ALLOWED+COMMITTED — the denials are not vacuous. VERIFIED
2026-08-14 | CG2-HARNESS | round-row protection (probes D1-D5, E1) | | D1 judge UPDATE judging_rounds → UPDATE 0 · D2 judge INSERT → RLS denial · D3 member UPDATE round_publish → UPDATE 0 · D4a post-completion score UPDATE → UPDATE 0 (policy layer) · D4b post-completion INSERT → 'Scoring is locked.' · D4c SUPERUSER update → trigger still raises (defense in depth beyond RLS) · D5a member self-setting app.bypass_round_lock=on STILL denied (policies do not honor the GUC) · D5b bypass works only for trusted/definer paths · E1 needs_review at R2 blocked even for superuser. VERIFIED
2026-08-14 | CG2-HARNESS | award write path (probes C0, C3) | | C0 direct INSERT into judge_award_tags by a judge → RLS denial (no member INSERT policy exists; the SECURITY DEFINER mirror is the ONLY write path) · C3 uncatalogued tag → mirror writes NOTHING and logs noop_alias_miss (fail-loud honored, BUG-052 re-raise intact). VERIFIED
2026-08-14 | CG2-HARNESS | FINDING F1 (demonstrated) | probes B1/B2 | a judge who is also a participant CAN score (B1: INSERT 0 1) and Winner-award-tag (B2: award row written by the mirror) THEIR OWN entry. No layer checks entry ownership: not judge_can_access_entry, not any policy, not any trigger. Mitigation today: competition_judges inserts are admin-only, so exposure requires assigning a participant as judge — nothing prevents that assignment. Production pre-flight: 0 judges currently hold an own entry in a judged competition → latent landmine, no active conflict.
2026-08-14 | CG2-HARNESS | F1 fix PROVEN on harness, NOT applied | harness/cg2/03_fix_proof.sql + transcript | one line — ce.user_id <> _judge_id in judge_can_access_entry(), the choke point every judge-write policy calls. P1 self-score DENIED · P2 self-award DENIED · P3 normal judging ALLOWED · P4 distributed+assigned ALLOWED. First P1 run was DISCARDED as evidence: it failed on the round lock (round 1 completed by probe D4), not on ownership — re-run on the open round 4 so the denial is attributable. Candidate migration + verbatim rollback staged in harness/cg2/ (NOT supabase/migrations/ — that directory holds only applied files). AWAITING OWNER GO because it changes judging policy, not because it is risky.
2026-08-14 | CG2-HARNESS | FINDING F2 (demonstrated) | probe C1 | award STACKING: same judge, same entry, same photo — Winner then 1st Runner-Up both persist as judge_award_tags rows (unique key includes stage_key BY DESIGN: Winner+Top-100 must legitimately coexist, so the DB cannot distinguish sensible from contradictory pairs). judge_decisions converges to last-write-wins (runner_up_1) — so final PLACEMENT stays single and deterministic (CG2-DETERMINISM verified); only the award-tag display/cert surface can carry contradictory pairs. Severity: LOW-MEDIUM, UI/ops discipline currently the only guard. Fix option (mutually-exclusive award families check in the mirror) queued as a design question for the owner, not implemented.
2026-08-14 | CG2-HARNESS | FINDING F3 (demonstrated) | probe C2 | two judges awarded Winner to two different entries in ONE competition; both award rows stand — no DB constraint enforces one-Winner-per-competition. Bounded: final placement is computed by complete-round aggregation (deterministic, SOW-priority — separately verified), so the award-tag layer disagreeing with itself cannot split the actual title; it can only mislabel certificates/display. Same design question as F2.
2026-08-14 | CG2-HARNESS | cycle verdict | | CG-2 seeded-harness matrix COMPLETE: judge authz VERIFIED, round protection VERIFIED (both layers), award write path VERIFIED, participant isolation FAILED-AS-PREDICTED (F1, fix proven+staged), stacking demonstrated (F2/F3, design questions). Harness assets + transcripts committed under harness/cg2/.
2026-08-14 | CG2-F1-DECISION | OWNER DECISION recorded | AskUserQuestion 2026-08-14 | Owner chose "No, leave it as is": judges MAY judge their own entries — the F1 fix is intentionally NOT applied. This is a product-rules decision by the owner, recorded here so no future cycle re-flags F1 as an oversight. The proven candidate + rollback REMAIN staged in harness/cg2/ should the rule ever change. F2/F3 likewise stay design-accepted (UI/ops discipline). CG-2 cycle CLOSED.
2026-08-14 | B3D4 | encoder heaviness — root cause pinned | src/lib/imageUpload.ts rung branch | The B3d-1 rungs were encoded through compressImageToFiles with NO quality option, inheriting the 0.92 master default. The ladder's byte targets (~56 KB @1080, ~83 KB @1440) were measured at q82 (IMAGE_DERIVATIVE_MEASUREMENTS_2026-08-13 §5) — so every rung shipped ~2× the bytes its own design assumed. Measurement-anchored, not a taste call.
2026-08-14 | B3D4 | fix applied (client, rides next build) | imageLadder.ts + imageUpload.ts | RUNG_QUALITY=0.82 exported from imageLadder.ts (the ladder's single source of truth) with the WHY; rung encode passes it. The FULL-RES master DELIBERATELY keeps 0.92: post-ladder it serves only lightbox zoom and download — the two moments a photography platform must not soften; feed bytes come from rungs+thumb. Scope note: this is the POST ladder path only; avatar (0.85) and thumbnail (0.7) unchanged.
2026-08-14 | B3D4 | tests + mutations | imageLadder.test.ts (16 tests) | Three assertions: RUNG_QUALITY VALUE = 0.82 (not grep); rung branch passes RUNG_QUALITY (branch-scoped regex — a file-wide grep would pass on the import line, loose-anchor lesson applied up front); full-res call shape unchanged (guards the master against a quality override sneaking in). Mutations: M1 value 0.92 CAUGHT · M2 override removed CAUGHT · M3 master softened to 0.7 CAUGHT. Suite 1521 passed. security-audit PASS.
2026-08-14 | EXIF-RAW | full raw-caller inventory closed | every storageUpload importer read | AdminGallery, EmailRichTextToolbar, CompetitionSubmit: ALWAYS canvas re-encode (strips metadata) ✓ · AdminBanners, useProfileMutations: storageRemove only ✓ · InlineImageDropZone: encode failure is a HARD STOP, no raw fallback ✓ · imageUpload FILE-5004 fallback: gated (prior cycle) ✓ · FileUploadDropZone: was the LAST open door — two raw modes, one branch: compressImages={false} (AdminSEO → PUBLIC site-assets; HelpSupport → private support-attachments, bucket visibility read from storage.buckets, not assumed) and the compression-failure fallback.
2026-08-14 | EXIF-RAW | gate applied at the one choke point | FileUploadDropZone.tsx raw branch | `if (isImage) toStore = await guardOriginalUpload(file)` before uploadImage — same fail-closed guard as the post pipeline: clean passes, strippable JPEG spliced, uncleanable GPS refused with member-readable copy (GpsPrivacyError toast). Non-images (PDF/doc) out of guard scope, unchanged. Wiring locked by 2 branch-anchored assertions in gpsGuard.test.ts (9 tests); mutations M1 guard-removed CAUGHT, M2 guarded-file-discarded CAUGHT. tsc clean, suite green.
2026-08-14 | B4-MATRIX | Multi-media Post Atomicity Matrix BUILT and EXECUTED | harness/b4/ — faithful local replica of BOTH regimes (today's posts.image_urls array + the applied-but-empty media engine); every function/trigger/constraint verbatim from production catalogs | 20 rows executed, not asserted on paper. Fidelity ledger in 01_media_seed.sql lists 4 deviations, each shown to be MORE permissive than production so no DENY verdict depends on them (rate_limit_posts deliberately unattached; can_view_post stubbed — reads are not what this matrix tests).
2026-08-14 | B4-MATRIX | REGIME 2 protections HELD (R7-R14, R18) | | reference-before-ready refused · pending->ready skip refused · ready->verified backwards refused · legal chain accepted (control) · same bytes = one object · concurrent begin_upload converges · quarantined never re-handed · referenced media undeletable · post delete cascades refs but keeps the media row for the sweep · in-flight cap 50 enforced · concurrent delete-vs-publish: FK RESTRICT wins, 0 dangling. VERIFIED
2026-08-14 | B4-MATRIX | REGIME 1 protections HELD (R1, R4, R4b, R6, R6b) | | partial upload = no post row at all (no half-publish) · identical repost inside 10 min refused · same caption + different photos allowed (guard not over-broad) · author cannot swap a published post's photos · caption edit still allowed (not vacuous). VERIFIED
2026-08-14 | B4-MATRIX | DEFECT R3 (LIVE, member-visible) | two concurrent sessions, harness/b4/race_transcript.txt | detect_duplicate_post reads EXISTS then raises, with nothing serialising the two. Two simultaneous identical submits BOTH committed -> 2 identical posts. The sequential control was correctly refused, proving the overlap is the cause and the harness is not broken. Trigger in the wild: a member double-tapping Post on a slow connection, or one composition submitted from two devices. Not adversarial.
2026-08-14 | B4-MATRIX | DEFECT R17 (latent until B5) | harness/b4/race_transcript.txt | a quarantine committing while a publish was in flight left 1 LIVE post reference pointing at quarantined bytes. Also R16: the same photo could occupy two carousel positions (the gate's "no duplicate reference" clause was assumed, never enforced).
2026-08-14 | B4-MATRIX | FIXES APPLIED to production | 20260814234152 dup_post_race_fix + 20260814234206 media_takedown_consistency (repo files renamed to recorded versions — drifts #12, #13) | R3: pg_advisory_xact_lock on (user_id, content_hash) BEFORE the read, same remedy as the max-entries race. Re-ran the identical race: 1 post, second correctly refused. Different member concurrently: completed t+1152ms against a lock held to t+3000ms — no cross-member serialisation. R17/R16: FOR UPDATE in the publish gate + quarantine detaches references in the same transaction + UNIQUE(post_id, media_id). Re-ran: live refs to quarantined media 0 (was 1), duplicate reference refused, every other row unregressed. Pre-flight: media tables 0 rows, posts 210 rows unchanged after.
2026-08-14 | B4-MATRIX | GAP R15 recorded, NOT hidden | | a partial reference set (ords 0 and 2, hole at 1) is accepted: posts has no status column, so the design's step-7 publish gate ("requires every ord present") is unbuilt. Harmless today — nothing writes post_media until B5 — and recorded as a B5 REQUIREMENT rather than quietly passed.
2026-08-14 | B4-MATRIX | MY OWN ALARM CAUGHT ME | securityDefinerGrants.test.ts failed on 20260814234206 | the migration redefined a SECURITY DEFINER function without STATING its grant posture. Verified live first: anon false / authenticated false both before and after — CREATE OR REPLACE preserves the ACL, so NO hole was opened. But the rule exists precisely so nobody relies on that; added explicit REVOKEs to the migration and executed them against production (idempotent, state unchanged). Recorded as a self-caught mistake, not silently patched.
2026-08-14 | B4-MATRIX | SECOND SELF-CAUGHT DEFECT: error-code collision | audit of FILE-50xx usage vs the catalog | the B3d-1 ladder cycle reused FILE-5008 (registered: EXIF read failure) for rung-encode fallback and FILE-5009 (registered: stale-handle rebuild) for rung-upload fallback — two codes each meaning two different things, which makes "I'm getting FILE-5008" undiagnosable, the exact failure the owner's catalog directive exists to prevent. Renumbered to FILE-5011/5012, registered with actionable resolutions, catalog regenerated, drift test green.
2026-08-14 | B4-MATRIX | B4 client resume implemented | WallPosts.tsx uploadedPhotoCache | a post failing on photo 3 of 5 threw the successful uploads away; the retry re-uploaded them under fresh names, orphaning the first copies and spending the member's data twice. Cache is keyed by the File OBJECT, never by index — the composer appends/removes/crops, so index 2 is not a stable identity; object identity self-invalidates exactly right (crop replaces the File, remove leaves others untouched). Keyed on the ORIGINAL File so a retry after the stale-handle rebuild still hits. Cleared in closeComposer. New code FILE-5010 (info) so the resume is observable.
2026-08-14 | B4-MATRIX | tests + mutations | postAtomicity.test.ts (8) | assertions resolve the LAST definition of each function across migrations, because CREATE OR REPLACE means a later migration silently wins and a corpus-wide grep would keep passing against a superseded file. Ordering asserted by index comparison (lock BEFORE read; detach AFTER state change) — a lock taken after the read protects nothing. Mutations: lock removed CAUGHT · lock moved after the read CAUGHT · lock key widened to user-only CAUGHT · FOR UPDATE dropped CAUGHT · detach removed CAUGHT · detach reordered CAUGHT · later migration drops the unique index CAUGHT · rollback file missing CAUGHT. Suite 1533 passed, tsc clean, security-audit PASS.
2026-08-14 | B4-MATRIX | THIRD SELF-CAUGHT MISTAKE: invented rollback location | reconciler DRIFT "no rollback for 20260814234152, 20260814234206" | I wrote the rollbacks to supabase/migrations/rollback/<name>.rollback.sql — a directory and spelling I invented — while the project's real location, the one scripts/ai-control.mjs reads, is supabase/rollback/<name>_ROLLBACK.sql. Worse: my own test asserted MY convention and passed, so the test was an echo of the mistake rather than a check on it. Moved both files to the canonical location, added an assertion that the invented directory does not exist at all (two rollback directories is the "two catalogs drift" failure with a new face), and re-mutated: rollback returned to the invented path -> 2 tests fail. Reconciler now reads 12/12 rollback coverage.
2026-08-14 | TRANSPORT | EXHAUSTIVE route audit, owner instruction "establish the connection properly so it never fails" | every reachable path probed, not argued | (1) git push -> 403, proxy states the repo "is not in this session's authorized repository set"; (2) GitHub REST /repos/... -> 403 for EVERY repo tried including public ones (anthropics/claude-code, octocat/Hello-World) — so the gate is the SESSION, not repo permissions; (3) GET /user -> 200, authenticating as altisinfonet — the credential is real and is the owner's own account, only the repo PATHS are sealed; (4) POST /graphql -> 403, "only the pinned set of PR-review operations is served"; (5) the add_repo tool the error message names is NOT in this session's tool registry (searched, not assumed) — it is a Claude Code CLI capability, absent in Cowork; (6) device bridge is file-transfer ONLY, no device_bash in this session, so git cannot be run on the owner's machine from here either; (7) SSH is force-rewritten to HTTPS by GIT_CONFIG url.insteadOf and would meet the same gate. CONCLUSION: no code path exists from this container to the repository. Not a misconfiguration to fix — a session boundary.
2026-08-14 | TRANSPORT | delivery route BUILT and REHEARSED instead of reported | tools/PUSH-TO-GITHUB.bat + 50mm-57-commits.bundle (344 KB, git bundle verify OK, base 6cb095d -> tip 5d5ce34) | REHEARSED end-to-end in a throwaway clone: reset to 6cb095d, fetch bundle, merge --ff-only, landed 5d5ce34, 57 commits gained — the script's git sequence is proven, not assumed. The .bat is written for a non-technical owner: checks git is installed (with a download link if not), clones fresh so no existing clone is required, ff-only so it REFUSES rather than mangles if origin moved, and every failure prints what happened, what was NOT changed, and what to tell me. CRLF line endings (cmd.exe mis-parses LF blocks). Desktop placement was offered twice via the device bridge; the owner was not at the machine to approve the folder dialog, so both files were delivered to the conversation instead, which works from any device.
2026-08-15 | B5-1 | design correction BEFORE building | investigated posts/post_drafts/scheduled_posts before writing any SQL | The Phase-0 sketch called for posts.status moving pending_media -> published. That assumed posts holds unpublished rows. It does not: drafts live in post_drafts, scheduled posts in scheduled_posts, and posts has only ever meant "published". A status column would have imposed a filter obligation on 13 server functions and every client query in order to represent a state no row would ever occupy. Rejected the sketch and built the property a different way rather than following the plan off a cliff.
2026-08-15 | B5-1 | atomic publish RPC APPLIED | 20260815041256_atomic_publish_with_media (repo file renamed to the recorded version) | post_publish_with_media() inserts the post AND every media reference in ONE transaction, then verifies the count before commit. Half-publish is not expressible: ords come from the array's own WITH ORDINALITY so a gap cannot be constructed, and any refusal rolls the post back with it. Also enforces: every photo owned by the caller, every photo state='ready', no repeated photo, 1..10 photos, authenticated only. idempotency_key column + PARTIAL unique index (WHERE NOT NULL) so the 210 legacy posts are untouched and a retry returns the FIRST post instead of creating a second.
2026-08-15 | B5-1 | harness proof, 8 clauses | harness/b5/publish_transcript.txt | P1 positive control 3 photos -> refs {0,1,2} ✓ · P2 one photo still pending -> REFUSED, 0 posts left behind ✓ · P3 another member's photo -> REFUSED ✓ · P4 same photo twice -> REFUSED ✓ · P5 empty list -> REFUSED ✓ · P6 unauthenticated -> REFUSED ✓ · P7 same idempotency key twice -> SAME id, 1 post ✓ · P8 nonexistent media -> REFUSED ✓.
2026-08-15 | B5-1 | production verification | post-apply | idempotency_key column ✓ · partial unique index ✓ · function SECURITY DEFINER ✓ · anon EXECUTE false ✓ · authenticated EXECUTE true ✓ · posts row count UNCHANGED at 210 ✓ · existing posts carrying a key: 0 ✓. Inert until the client switch — nothing calls it yet and post_media holds 0 rows; it ships first so the switch inherits a proven write path.
2026-08-15 | B5-1 | tests + mutations | publishAtomicity.test.ts (8) | assertions resolve the LAST definition across migrations (CREATE OR REPLACE means a later file silently wins). Mutations: hand-built ords replacing ORDINALITY CAUGHT · completeness gate moved before the insert CAUGHT · ownership check dropped CAUGHT · anon revoke removed CAUGHT · idempotency index made non-partial CAUGHT · rollback file missing CAUGHT. Suite 1546 passed. security-audit PASS.
2026-08-15 | B5-2 | media backfill BUILT (not yet deployed) | supabase/functions/backfill-media-objects/index.ts + readS3Object added to _shared/s3.ts | Gives every existing post a real media_objects row and a post_media reference. ADDITIVE ONLY: posts.image_urls untouched, nothing deleted, nothing published; the rows it writes are read by no one until the client switch, so a mistake cannot reach the live site. Hashes the WHOLE file — a fingerprint over a ranged read is identical for every photograph sharing a container header, which would silently collapse different photographs into one identity and mis-target a future takedown. Walks pending->verified->ready through the real state machine rather than inserting 'ready' directly (which the service role could do) so the backfill is not the one writer that never proves it read the bytes. WHOLE-POST-OR-NOTHING: a post is written only when every slide is plannable, because a partial reference set is exactly the half-published state B5-1 refuses at the front door — the backfill must not be the back door. Deletion-grade ceremony: dry-run default, expected_count gate with drift abort, per-run cap + hard ceiling, fail-loud scans, per-slide skip reasons, post-write read-back verification, failed posts left untouched for retry.
2026-08-15 | B5-2 | REAL dry-run numbers, computed against production data | SQL mirroring the planner's exact classification | 210 posts carry photographs. 194 posts / 230 slides are FULLY PLANNABLE. 0 posts are partial — every post is entirely plannable or entirely not, so the whole-post gate will never have to reject one. 16 posts / 28 slides are wholly unplannable: all of them are hosted on Supabase storage rather than R2, the documented out-of-scope class that needs its own pass. Average 1.23 slides per post.
2026-08-15 | B5-2 | tests + mutations | mediaBackfill.test.ts (18) | Mutations: ranged head instead of whole read CAUGHT · whole-post gate loosened to allow partials CAUGHT · media inserted directly as 'ready' bypassing the state machine CAUGHT · job starts writing to posts CAUGHT · drift gate dead-branched CAUGHT · post scan treats an error as 'done' CAUGHT · readS3Object gains a Range header CAUGHT · dedup lookup removed CAUGHT.
2026-08-15 | B5-2 | HONEST LIMITATION, recorded not glossed | | The function is NOT deployed and has NOT been executed in the Deno runtime. Deploying through the connector requires the entrypoint and both shared modules to be inlined into the tool call by hand (31 KB); hand-transcribing them risks the deployed copy silently differing from the repo, which is precisely the drift this program's protocol exists to prevent. By this project's own standard ("no row goes green from reasoning alone") B5-2 is BUILT AND LOCKED, not PROVEN. It goes green when it is deployed from disk and its dry run is run for real.
