# FINAL-CONTROL — 50mm Retina World, complete state handoff

Generated 2026-08-14 by running the commands quoted. **Report only — nothing was changed, no
production mutation, no push.** Every claim is labelled **VERIFIED** (I ran it and read the
output), **PARTIALLY COMPLETE**, or **NOT VERIFIED**.

> **The single most important line in this document:** the Phase 1 performance work is applied
> to the *schema* but the feed RPC still runs the old whole-corpus scan. **No user-visible
> performance change has shipped.** `pg_get_functiondef(get_broadcast_feed) ~ 'feed_candidates'`
> → **false**; `~ 'count(DISTINCT fe.user_id)'` → **true**.

---

# SECTION 1 — PROJECT IDENTITY · VERIFIED

| | |
|---|---|
| Project | 50mm Retina World (web + Android) |
| Repository | `github.com/altisinfonet/lens-lustre-learn-Claude` — **PUBLIC** (`git ls-remote` succeeds with no credential) |
| Local path | `/home/claude/work/repo` (ephemeral container clone) |
| Branch | `main` |
| HEAD | `4a33a58ab4c60ded1f8028917eabfdb6e389023b` |
| Latest commit | `feat(feed): RPC rewrite drafted — NOT applied` |
| `origin/main` | `6cb095d` |
| Ahead / behind | **19 ahead**, 0 behind |
| Working tree | **clean** (`git status --porcelain` empty) |
| Uncommitted | none |
| Pushed | **NO — 19 commits exist only in this container** |
| Push blocked | **YES.** `403 — repository is not in this session's authorised set, so the proxy will not inject a credential` |
| Production project | Supabase `jtdtehuqtinjxropkkcn` (`50mmretinaworld`, ap-northeast-2, PG 17.6.1) |
| **Production migration** | **`20260814070357 feed_candidate_pool`** |
| **Latest local migration** | **`20260814080000_feed_rpc_candidate_pool.sql` — NOT APPLIED** |

⚠ **The repo is one migration behind production and one migration ahead of it, simultaneously.**
`20260814042609` and `20260814070357` are applied but exist only in unpushed local commits;
`20260814080000` is committed locally and not applied. Anyone cloning from GitHub sees neither.

---

# SECTION 2 — TECHNOLOGY STACK · VERIFIED

Resolved from `package-lock.json`, not from the declared ranges.

| Package | Version |
|---|---|
| react / react-dom | **18.3.1** |
| typescript | **5.8.3** |
| vite | **7.3.1** |
| @tanstack/react-query | **5.62.0** (only exact-pinned dep in the manifest) |
| react-router-dom | **6.30.1** |
| @supabase/supabase-js | **2.97.0** |
| tailwindcss | **3.4.17** |
| framer-motion | **12.34.3** |
| @radix-ui/* | 27 declared, e.g. react-dialog 1.1.14, react-toast 1.2.14 |
| Node / npm (this run) | **v22.22.2 / 10.9.7** |

**`@capacitor/*` count in `package-lock.json`: 0.** Capacitor is installed by CI only,
exact-pinned in `.github/workflows/android-build.yml`:

`@capacitor/core|cli|android@8.5.0` · `share@8.0.1` · `splash-screen@8.0.2` ·
`filesystem@8.1.2` · `camera@8.2.2` · `app@8.1.1` · `browser@8.0.4` ·
`@capacitor-firebase/messaging@8.4.0` · `@capawesome/capacitor-app-update@8.0.3` ·
`@capacitor/assets@3.0.5`

**`@capacitor/ios`: does not exist anywhere.** `@capacitor-community/*`: **zero references in
the entire repository.**

Java **21** · AGP **8.12.3** · Gradle wrapper **8.13** · minSdk **24** · compileSdk/targetSdk
**36** — all injected by `sed` at build time; **no Gradle file is committed** (`git ls-files |
grep gradle` → 0).

| Question | Answer |
|---|---|
| `android/` present? | **NO** — regenerated by CI each build |
| `ios/` present? | **NO** |
| Capacitor bundled locally? | **YES** — `webDir: 'dist'` |
| `server.url`? | **ABSENT** |
| `server.hostname`? | **ABSENT** |

### ARCHITECTURE: **React Web + Capacitor (bundled, Android-only, zero hand-written native code).**
Not React Native. Not Flutter. Not native.

---

# SECTION 3 — CONTROL SYSTEM · VERIFIED EXECUTABLE AND ENFORCING

Files: `AI_CONTROL.md` (2,829 B) · `AI_EVIDENCE.md` (13,122 B, **102 evidence lines**) ·
`scripts/ai-control.mjs` (6,746 B). No CONTROL skill was installed — the protocol lives in
these three files.

**Live run:**
```
CONTROL — 50mm Retina — phase 0 — READY_FOR_REVIEW
  OK                 base_commit        6cb095d
  OK                 working_tree       clean
  INFO               unpushed_commits   19 local commit(s) not on origin/main
  INFO               approved_hash      no production write authorised
  OK                 rollback_coverage  4/4 since 20260813000000 (598 older out of scope)
  REQUIRES_CONNECTOR db_migration       claimed 20260814070357; verify with list_migrations
  OK                 state              READY_FOR_REVIEW
  VERDICT: READY   NEXT: AWAIT_GO
```

**Is it enforcement or documentation? Enforcement — proved by mutation, then reverted:**

| Injected fault | Output |
|---|---|
| `approved_hash: deadbeef…` | `BLOCKED — approval was for deadbeef…, file is now 11661da4… — GO is VOID` · VERDICT **BLOCKED** |
| `base_commit: 0000000` | `DRIFT — claimed 0000000, origin/main is 6cb095d — re-measure before trusting any prior result` · VERDICT **DRIFT** |

Working tree confirmed clean afterwards.

**Rules as implemented.** Commands `CONTROL` / `STATUS` / `GO <hash>` / `STOP` / `REVIEW` /
`ROLLBACK`. State machine DESIGN → APPROVED → IMPLEMENTING → LOCAL_VERIFICATION →
READY_FOR_REVIEW → APPROVED_FOR_PRODUCTION → DEPLOYED → POST_DEPLOY_VERIFICATION → CLOSED, plus
BLOCKED. **Approval is bound to `git hash-object`** — verified live twice this session: `GO
677c0cab` was **voided** when the file changed, and `GO 5ccba5fb` was matched before applying.
Drift detection: `git fetch` is check #1. Database reconciliation: the script has no DB
credential and prints `REQUIRES_CONNECTOR` rather than guessing — the agent verifies with
`list_migrations` and records it. One production migration per cycle; hard ceiling on
`DROP TABLE`, destructive data ops, credential rotation, the keystore, and publishing anything
describing an unfixed vulnerability.

**Standing rule, and it fired three times today:** *discovering something adjacent is not
authorisation to fix it.*

---

# SECTION 4 — COMPLETE WORK HISTORY

### 4.1 Before this program (from the repo's own docs, **NOT independently re-verified**)
`get_broadcast_feed` 11 → 15 columns carrying `author_name`/`author_avatar`/`thumbnail_urls`/
`categories` (migration `20260813171159`) · `COALESCE(_exclude_ids)` guard restored · feed
windowing · 14.7 MB backdrop removed · `maxPages: 5` · realtime 9 → 5 bindings · Capacitor
pinning · drafts/slow-network error handling · two in-render components hoisted.
Builds 1086/v1.2.4 **halted**, 1088/v1.2.5 **green and awaiting owner upload**, 1073/v1.2.2 live.

### 4.2 This program — every commit, chronological · VERIFIED from `git log`

| Commit | What | Applied to prod? |
|---|---|---|
| `8a2459b` | `image_meta` jsonb migration | **NO** |
| `bfa4e16` | restore explicit VOLATILE; forensic audit doc | n/a |
| `933f4a7` | **withdraw** `image_meta` — superseded by `media_objects` | n/a |
| `01fc86c` | engineering plan v1 → v2 → Phase 0 design review | n/a |
| `0f2dacc` → `28e0b06` | **email queue authority** — `enqueue_email` + `read_email_batch` to service_role, queue allow-list | **YES — `20260814042609`** |
| `dd41a5d` | ignore tsbuildinfo | n/a |
| `f489ad4` | **CONTROL protocol** | n/a |
| `07c7fbb` | M2 preflight — **M2b BLOCKED** | no write |
| `8f62b6a`, `83cb8db` | rebase + sync onto moved remote; handover gap closed | n/a |
| `e4d904d` | harness built; **approved Phase 1 design FAILS its own gate** | no write |
| `daf19e7` | Option A measured — passes gate, fairness restored | no write |
| `9fdea4e` | **R1–R7**; bucketing decision reversed on adversarial evidence | no write |
| `b4c4777`, `f2ad318` | candidate-pool schema drafted; **pre-flight stop** (viewer was a parameter) | **NO** |
| `93cbd42` | SECURITY DEFINER grant invariant test, mutation-tested | n/a |
| `84eba29` | **candidate pool schema APPLIED** | **YES — `20260814070357`** |
| `4a33a58` | **RPC rewrite drafted** | **NO** |

Interleaved commits from the other session (now stopped): `28beb81`, `86afb8d`, `f8c1985`,
`154d2a1`, `c7c591b`, `6cb095d`.

---

# SECTION 5 — DATABASE / MIGRATIONS · VERIFIED

**PRODUCTION: `20260814070357` · LATEST LOCAL: `20260814080000` (not applied).**

| Migration | Purpose | On GitHub? | Applied? | Verification |
|---|---|---|---|---|
| `20260813171159 feed_author_identity` | 15-column feed contract | yes | **YES** | pre-existing |
| `20260814042609 email_queue_authority` | email RPCs → service_role + allow-list | **NO (local only)** | **YES** | anon=false on both; allow-list rejects with 42501; rollback round-tripped |
| `20260814070357 feed_candidate_pool` | `is_public`, `rand_key`, `viewer_count`, `viewer_bucket_stable`, trigger, refresh fn, pool fns | **NO (local only)** | **YES** | 4/4 columns, 4/4 functions, backfill exact, anon=false on all new fns |
| `20260814080000 feed_rpc_candidate_pool` | point the RPC at the pool | **NO** | **NO** | round-tripped locally only |

**Live production checks, run now:**

```
prod_migration           20260814070357
p1_columns_present       4          (is_public, rand_key, viewer_count, viewer_bucket_stable)
p1_functions             4
rpc_uses_pool            false      ← the RPC is still the OLD definition
rpc_still_has_lateral    true       ← count(DISTINCT fe.user_id) still runs per post
media_tables             0          ← media_objects / post_media DO NOT EXIST
tables_total             141
rls_enabled              141        (100%)
policies                 679
anon_secdef_callable     136 signatures / 132 distinct names
anon_enqueue_email       false      ✔ fixed
anon_read_email_batch    false      ✔ fixed
anon_feed_candidates     false      ✔ never exposed
posts                    210
nonpublic_posts          0
track_functions          none
```

**NOT VERIFIED:** whether the 132 distinct-name figure moved as a result of my work. My earlier
baseline was also 132, and the two email functions have since been removed from the set — so at
least two other names entered it, from a source I did not capture. The population is not
tracked, so it can drift silently. That is itself an argument for M2.

Storage policies, per-bucket: **NOT VERIFIED** — not inspected this session.

---

# SECTION 6 — FEED PERFORMANCE (current implementation) · VERIFIED

| Item | Current state |
|---|---|
| Virtualization | `FeedCardWindow`, hand-rolled IntersectionObserver, `WINDOW_MARGIN = 1600px 0px` |
| Mounted PostCards on `/feed` | **7–15** (from 50 retained) — derived from the constants, not device-measured |
| `maxPages` on feed | **5** → 50 posts retained |
| `maxPages` on `/profile` | **0 occurrences — still unbounded** |
| `/profile` windowed | **NO — `FeedCardWindow` appears 0 times in `WallPosts.tsx`** |
| `cdnImage.ts` non-test importers | **0 — still dead code** |
| `isTransformable` requires Supabase path | **still 1 occurrence** → always false for CDN URLs |
| feed srcset | **still 2 candidates** (`thumb 600w, original Xw`) |
| `media_objects` referenced in `src/` | **0** |
| Route code-splitting | **50 `lazyRetry(` — 57/57 routes lazy** |
| Feed requests for 10 posts | **7–9**, batched, **no N+1** (unchanged; enrichment is one 7-way `Promise.all`) |

**Live production feed RPC, measured now:**
```
select count(*) from get_broadcast_feed(null,10,3,null)
→ 2,943 buffers · 91.8 ms · 210 posts
```

**Largest remaining bottleneck: the feed RPC itself** — it still scans every visible post and
runs the `count(DISTINCT)` LATERAL per post. The fix is written and round-tripped but **not
applied**.

Feed p50/p95 in production: **NOT VERIFIED** — no latency instrumentation exists
(`logger.timed()` has **0 call sites**).

---

# SECTION 7 — DATABASE SCALE

### VERIFIED CURRENT BEHAVIOUR (production, 210 posts)
Feed RPC: 2,943 buffers, 91.8 ms. Still `Seq Scan` + per-post `count(DISTINCT)`.
100% of posts are `privacy='public'`, so `are_friends()` is never exercised in production.

### VERIFIED ON A SEEDED HARNESS (local PG16, production ratios, 80/15/5 privacy mix)

| Design | 100k posts | 1M posts |
|---|---|---|
| Current production shape | 827,766 buf · 1,603 ms | **4,836,645 buf · 9,489 ms · external merge, 148 MB to disk** |
| `is_public` + partial index + maintained bucket | 207,112 buf · 941 ms | — |
| **Bounded pool (the drafted RPC)** | 577 buf · 3.9 ms | **1,656 buf · 8 ms** |

Ranking tests at 1M, pool 1,530: **R1** 0 duplicates / 0 omissions over 153 pages · **R2**
deterministic · **R3** Spearman(bucket, position) old `+0.9231` vs new `+0.9192`, delta
**0.0039** · **R4** Kendall τ `−0.0020` · **R7** stable · **R1c adversarial** 0 duplicates under
20,100 concurrent pool updates (it produced **2 duplicates** before the stable-bucket fix).

Reach fairness: 500 simulated sessions reached **92,805 of 800,000** distinct posts (11.6%),
average age of reached posts **215.0 days** vs corpus **214.7** — no recency bias.

### FUTURE EXPECTED BOTTLENECK (prediction, not measurement)
Exclusion-array pagination still grows: **990 UUIDs / ~38 KB at page 100**. Keyset cursor is
designed but **not implemented** — the drafted RPC keeps the exclusion array for compatibility.

---

# SECTION 8 — MEDIA PIPELINE · LARGELY NOT STARTED

`UPLOAD → client canvas → R2 (presigned PUT) → CDN → feed`. No verification step, no
server-side derivative generation, no `media_objects`.

| Derivative | Exists? |
|---|---|
| 600 thumbnail | **YES** — client-side, WebP q0.70 |
| 1080 | **NO** |
| 1440 | **NO** |
| original ≤2560 | **YES** — client-side, WebP q0.92 |

**Can a 2560px original still reach a phone for a normal feed card? YES.** `isTransformable()`
requires `/storage/v1/object/public/`; every stored URL is `cdn.50mmretina.com`, so the whole
transform branch is dead. 153 of 258 slides (59%) have no `srcset` at all.

**CDN transforms work and are never called** — verified from a `www` page: apex fails, `www`
and `cdn` return 200. `cdnImage.ts` has **0 non-test importers**, and its own test *forbids*
adoption (`expect(code).not.toContain("cdn-cgi")`).

`/cdn-cgi/image/` responses carry **no CORS headers** — a transformed URL can never serve the
download/canvas path (`imageCompression.ts:150-157` sets `crossOrigin="anonymous"`).

Public/private media, friends-only media, signed URLs, object authorization, public→private
transition: **NOT IMPLEMENTED.** All post media is publicly readable on the CDN regardless of
`posts.privacy` — harmless *today only* because `nonpublic_posts = 0`, verified.

Orphan cleanup: exists for `competition-photos/` only, **manually invoked, no schedule**.
EXIF/GPS: `exifr` used on the competition path only; **no GPS-stripping policy** — NOT VERIFIED
whether GPS survives upload.

---

# SECTION 9 — UPLOAD RELIABILITY · NOT STARTED

| Capability | State |
|---|---|
| Retry on presign call | **YES** (`[300, 800]` + one auth refresh) |
| **Retry on the actual PUT** | **NO** — bare `fetch` |
| Resumability / chunking | **NO** |
| `AbortController` in the upload path | **NO** — 3 exist in `src/`, all elsewhere (AdminSEO, supabase client, PDF gen) |
| Progress | **NO** — `XMLHttpRequest` occurrences: **0**; `fetch` cannot report upload progress |
| Persistent pending state | **NO** |
| Idempotency | **NO** |
| Duplicate prevention | DB trigger hashes `md5(content ‖ image_urls)`; a retry generates new URLs → **guard never fires** |
| Orphan cleanup for `post-images/` | **NO** |
| Encode off main thread | **NO** — `new Worker(`: **0** |

Failure behaviour: network loss mid-upload → toast, photos retained, retry **from byte zero**;
photos 1–2 of 5 already in R2 become **permanent orphans**. App killed → total loss. Presign TTL
**300 s**, shared `amzDate` across both PUTs; a 50 MB competition entry exceeds it below
~1.4 Mbps with **no re-presign**. Token expiry is handled for the presign only.

---

# SECTION 10 — REALTIME · VERIFIED

**26 `supabase.channel()` occurrences across 23 files; 25 `removeChannel` calls.** 6 module-level
singletons never removed (deliberate, guarded).

`/feed`: **8 channel objects, ~25 postgres_changes bindings.**

- **5 unfiltered bindings on the hottest tables**: `posts` INSERT/UPDATE/DELETE, `post_reactions`
  INSERT/DELETE — every write platform-wide reaches every client.
- **`feed-live` is subscribed twice on `/feed`** (`Feed.tsx:167` and `WallPosts.tsx:150` via the
  `composerOnly` mount). `useIsPrimaryInstance` appears **3 times in `useRealtimeFeed.ts`** but
  guards the notification bell, **not** `useFeedRealtime` — **NOT FIXED**.
- `user_roles` subscribed twice by two always-on global singletons.
- 25 of 26 `.subscribe()` ignore the status callback.
- 1 latent listener leak (`usePwaInstall.ts:34`), currently unmounted.

---

# SECTION 11 — SECURITY · CURRENT

**FIXED AND VERIFIED**
- `enqueue_email` / `read_email_batch`: `anon=false, authenticated=false, service_role=true`;
  queue allow-list rejects with `42501`. **`read_email_batch` returned the whole pgmq payload and
  `q_auth_emails` carries authentication mail** — this was the most serious exposure found.
- New Phase 1 functions: `feed_candidates`, `feed_rank_score`, `refresh_viewer_buckets` all
  `anon=false`. The viewer is read from `auth.uid()`, not a parameter — caught in pre-flight
  *before* apply.
- RLS: **141/141 tables, 679 policies.**

**OPEN**

| id | Finding | Severity | Prod-exposed |
|---|---|---|---|
| SEC-01 | `ALTER DEFAULT PRIVILEGES` grants EXECUTE on **every** new `public` function to `anon`+`authenticated`. **132 distinct SECURITY DEFINER names reachable at `/rest/v1/rpc/`**. Spot-checked `admin_*` gate internally; 132 is too many to assert from a spot check. | **P1** | YES |
| SEC-02 | `scripts/audits/baselines/rls-authority-baseline.json` — frozen 2026-05-12, **259 HIGH** findings (258 `SECDEF_NO_AUTH_GUARD`), no owner, no expiry. `security-audit.mjs` reports 0/0 because it is a different scanner that never reads this file. | **P1** | n/a |
| SEC-03 | Post media publicly readable on the CDN regardless of `posts.privacy`. Harmless today only because `nonpublic_posts = 0`. | **P1** (P0 the day a friends-only post exists) | YES |
| SEC-04 | 4 advisor `ERROR security_definer_view`; 8 mutable `search_path`; leaked-password protection **OFF**. | P2 | YES |
| SEC-05 | `npm audit`: **1 critical, 19 high**, 29 total. `security-audit.mjs` does not scan dependencies. | **P1** | build-time |

I did **not** read any queue payload or member token to prove SEC-01/03. Safe verification:
enable `track_functions`, gather 30 days, then revoke what has zero calls; and for SEC-03, an
anonymous HTTP GET against a friends-only object once one exists.

---

# SECTION 12 — CI / BUILD / RELEASE · VERIFIED BY RUNNING

| Gate | Result |
|---|---|
| `npx vitest run` | **1,361 passed · 1 skipped · 0 failed** (110 files) |
| `npm run build` | **exit 0**, built in 27.0 s; `svgo` missing so 11 SVGs unoptimised |
| `node scripts/security-audit.mjs` | **PASS — 0 critical / 0 high** |
| `npx tsc -b tsconfig.json` | **FAIL — exit 2, 2 errors** (`FeedCardWindow.test.tsx:113`, `profileMapCache.ts:344`) |
| **`npx tsc --noEmit -p tsconfig.json` (what CI actually runs)** | **passes while checking `0` files** — proved with `--listFiles` |
| `npm audit` | 1 critical, 19 high, 6 moderate, 3 low |

**Can CI silently resolve different versions? YES.** `npm ci || npm install --no-audit
--no-fund` appears in three workflows — any `npm ci` failure degrades to range re-resolution
with a green step. Capacitor's *transitive* graph is in no lockfile. `node-version: 22` floats
by patch. The 11 named Capacitor packages **are** protected by a real blocking pin check.

Android: `android/` regenerated per build; the only assertion that the web bundle is inside the
artifact sits in a `continue-on-error: true` step; the one blocking artifact check verifies the
**notification icon**. Signing verifies alias and key password before Gradle and hard-fails an
unsigned AAB when a keystore secret is present. Play upload targets `track: production` with
`status: draft`.

---

# SECTION 13 — OBSERVABILITY

**Measured:** JS errors and route crashes (`AppErrorBoundary` → `log_app_event` →
`public.client_errors`, 75-code catalog with mandatory resolutions) · image-load failures
(capturing listener, records `naturalWidth`) · post/comment failures · upload *duration*.

**NOT measured:** feed latency · RPC latency (`logger.timed()` — **0 call sites**) · network
timings (`networkTracer` is dev-gated twice) · **native crashes / Android OOM — no signal at
all** · `unhandledrejection` · `window.onerror` · realtime channel health · CDN egress · upload
failure rate directly (the `"upload"` error kind is declared and never emitted).

**Single sink.** Everything goes to the project's own Postgres. Both senders swallow their own
rejection by design, so a Supabase outage is invisible **by construction**. No alerting; 30-day
retention.

---

# SECTION 14 — SCALE READINESS

| Scale | Verified current capability | Expected bottleneck (prediction) | Required before |
|---|---|---|---|
| **10k users** | 210 posts, 91.8 ms feed. Nothing failing. | Feed RPC shape — O(total posts); realtime 5 unfiltered bindings ×  concurrency | apply `20260814080000`; realtime filters |
| **100k users** | Harness: current shape 827,766 buf / 1,603 ms at 100k; drafted RPC 577 buf / 3.9 ms | CDN egress (2560px originals, ~6–8× multiplier); upload orphans | media pipeline; upload reliability |
| **1M users** | Harness: current shape **9,489 ms + 148 MB disk spill**; drafted RPC **8 ms** at 1M | pagination payload; realtime fan-out; no observability to diagnose with | keyset cursor; realtime filters; latency + crash telemetry; second alerting sink |
| **10M users** | **NOT VERIFIED — never modelled** | feed materialisation; read replica; server-side media | `feed_items` fan-out; read replica; a different operating org |

---

# SECTION 15 — REGRESSION CHECK vs the 2026-08-13 audit

| Finding | Status |
|---|---|
| `enqueue_email` anon-executable | **FIXED** (verified) |
| `read_email_batch` anon-executable (found during this program) | **FIXED** (verified) |
| Feed SQL: Seq Scan + `count(DISTINCT)` LATERAL | **PARTIALLY FIXED** — schema applied, **RPC not switched**; fix drafted, not applied |
| Pagination: unbounded exclusion array | **NOT FIXED** |
| 2560px originals reaching phones | **NOT FIXED** |
| Derivative ladder (1080/1440) | **NOT FIXED** |
| CDN transform unused | **NOT FIXED** — `cdnImage.ts` still 0 importers |
| Upload reliability | **NOT FIXED** |
| Realtime: 5 unfiltered hot-table bindings | **NOT FIXED** |
| Realtime: `feed-live` double subscription | **NOT FIXED** |
| Capacitor pinning | **FIXED** (pre-existing, re-verified) |
| CI typecheck checks 0 files | **NOT FIXED** |
| 2 real TypeScript errors | **NOT FIXED** |
| iOS | **NOT STARTED** (by design) |
| `/profile` unwindowed + no `maxPages` | **NOT FIXED** |
| Feed N+1 | **FIXED** (7–9 batched requests) |
| Route lazy loading | **FIXED** (57/57) |
| Memory: feed windowing | **FIXED** for `/feed` only |
| 248→ anon SECURITY DEFINER population | **NOT FIXED** — 132 distinct names remain |
| Frozen security baseline hiding 259 HIGH | **NOT FIXED** |
| **REGRESSED** | **none identified** |

---

# SECTION 16 — OPEN ISSUES

**P0 — none currently exposed.** SEC-03 becomes P0 the moment one friends-only post exists.

**P1**
1. Feed RPC still scans the whole corpus — fix drafted, awaiting GO
2. SEC-01 — 132 anon-reachable SECURITY DEFINER names
3. SEC-03 — private media publicly fetchable by design
4. SEC-05 — 1 critical / 19 high npm advisories
5. Upload reliability — no retry/resume/abort/progress; silent orphans
6. 2560px originals to phones; 59% of slides with no `srcset`
7. `/profile` unbounded retention — the OOM condition the feed fixed
8. CI typecheck checks 0 files, hiding 2 real errors
9. No latency, crash or OOM telemetry; single observability sink
10. SEC-02 — frozen baseline hiding 259 HIGH findings

**P2** — realtime unfiltered bindings and double subscription · exclusion-array pagination ·
`viewer_bucket_stable` refresh job **not scheduled** (rank input will freeze) · 4 advisor
ERRORs · leaked-password protection off · orphan purge unscheduled and post-images-blind ·
19 unpushed commits / repo out of sync with production.

**P3** — `sharp` (33 MB) and unused `vite-plugin-pwa` in `dependencies` · `qrcode` 80 KB in the
entry chunk · initial JS 468 KB gzip vs the project's own 350 KB target · duplicate indexes ·
`usePwaInstall` listener leak · `installStaleChunkReload` dead.

---

# SECTION 17 — FINAL GATES

| Gate | Verdict | Evidence |
|---|---|---|
| SECURITY | **PARTIAL** | 2 severe exposures closed and verified; RLS 141/141; 5 findings open incl. 1 critical npm |
| RELIABILITY | **FAIL** | no upload retry/resume/idempotency; orphans by design |
| PERFORMANCE | **PARTIAL** | schema applied, RPC not switched — **no user-visible gain shipped** |
| DATABASE | **PARTIAL** | Phase 1 half-landed; fix drafted and round-tripped |
| MEDIA | **FAIL** | no derivatives beyond 600; 2560px to phones; CDN unused |
| UPLOAD | **FAIL** | see §9 |
| REALTIME | **FAIL** | 5 unfiltered hot-table bindings; double subscription |
| MOBILE | **PARTIAL** | bundled Capacitor correct; no iOS; artifact assertion non-blocking |
| SCALABILITY | **PARTIAL** | 1M-post path measured on a harness, not shipped |
| OBSERVABILITY | **FAIL** | no latency, no crash, no alerting |
| ECONOMICS | **NOT VERIFIED** | CDN egress and cost per 1,000 feed views never measured |
| CI / RELEASE | **FAIL** | blocking typecheck checks 0 files; `npm ci \|\| npm install` |

---

# SECTION 18 — FINAL VERDICT

```
CURRENT STATUS:                 PARTIALLY DONE
ARCHITECTURE:                   YELLOW
REACT + CAPACITOR:              KEEP
INSTAGRAM-LEVEL:                NOT YET VERIFIED
STRONGER-THAN-INSTAGRAM:        CONDITIONAL
PRODUCTION READY:               CONDITIONAL (it is live and stable at current scale;
                                it is not ready for the scale being planned)
```

**TOP 10 REMAINING RISKS**
1. The feed performance fix is drafted, verified and **not applied** — all measured gains are theoretical in production
2. 19 commits and 2 applied migrations exist **only in an ephemeral container**; loss of this container loses the migration source, though not the applied schema
3. SEC-01 — 132 anon-reachable SECURITY DEFINER functions, unenumerable from source because installed APKs call RPCs today's code does not
4. SEC-03 — private media publicly fetchable the day the feature is used
5. Upload path loses data and leaks storage on any interruption
6. Media delivery still sends 2560px originals to phones; 59% of slides have no `srcset`
7. CI's blocking typecheck checks zero files; two real errors have been shipping
8. No latency, crash or OOM telemetry — a production regression would be invisible
9. `viewer_bucket_stable` has **no scheduled refresh**; reach fairness will silently freeze
10. `/profile` retains and mounts every page ever fetched — the OOM condition the feed fixed

**NEXT REQUIRED ACTION**
`GO 11661da4` — apply `20260814080000_feed_rpc_candidate_pool.sql`, then verify a real feed load
on web **and** on the 1088 debug APK. Immediately after: schedule
`refresh_viewer_buckets()`, and transport the three applied/pending migrations to GitHub.

---

# SECTION 19 — THE INVESTMENT QUESTION

**YES, BUT.**

The foundations that are expensive to retrofit are right, and I can point at each: RLS on
141/141 tables with 679 policies, 57/57 routes lazy, no N+1 in the feed, a 75-code error catalog
with mandatory resolutions, 1,361 passing tests, and a codebase that argues with itself in
writing so past incidents cannot be repeated silently. The framework is not the limiting factor
— every bottleneck I measured is a query plan, an index, an `<img>` attribute, a missing
`filter:` or an absent retry. Keep React + Capacitor.

The "but" is that the four things a photography platform is judged on — image delivery, upload
reliability, feed query cost and pagination — are still largely unbuilt, and the feed fix that
*is* built has not been applied. What I would price in is not the remaining work, which is
known, scoped and measured; it is the pattern this session kept surfacing: a green gate that was
green because it was looking at nothing. A typecheck checking zero files, a frozen baseline
hiding 259 HIGH findings, two of my own tests passing vacuously, my own rollback test reporting
OK after psql had errored. Every one was caught, but only by deliberately trying to make each
check fail. Fund it — and make "prove this test can fail" a standing requirement.

---

```
FINAL_CONTROL
STATUS=PARTIALLY_DONE
COMMIT=4a33a58ab4c60ded1f8028917eabfdb6e389023b
PRODUCTION_MIGRATION=20260814070357
LOCAL_MIGRATION=20260814080000
WORKTREE=CLEAN
TESTS=PASS_1361_1_SKIPPED
TYPECHECK=FAIL_2_ERRORS_CI_CHECKS_ZERO_FILES
BUILD=PASS
SECURITY=PARTIAL
DATABASE=PARTIAL
MEDIA=FAIL
UPLOAD=FAIL
REALTIME=FAIL
PERFORMANCE=PARTIAL
SCALE=PARTIAL
OPEN_P0=0
OPEN_P1=10
NEXT_ACTION=GO_11661da4_APPLY_20260814080000_FEED_RPC
ARCHITECTURE=YELLOW
INSTAGRAM_GRADE=NOT_YET_VERIFIED
PRODUCTION_READY=CONDITIONAL
```
