# WORK ORDER WO-3 — repo-enabled measurement

**Nothing in WO-3 closes a §25 row.** §25.4 is unchanged by repo access: every figure below is
compiler-generated.

**Access discipline.** Cloned read-only; the push URL was set to `DISABLED_NO_PUSH_AUTHORITY`
before any other command. All work in detached checkouts and two `git worktree` checkouts. No push,
branch, commit, PR, merge, tag, workflow dispatch or release. `docs/PROMOTION_LEDGER.md` was read,
never modified — its sha256 is unchanged at `f00f612a057477146f4047f5af9235337f857d31887bb10e5bb56c98a9cd5943`,
182,502 bytes.

**Carried forward, still open, not superseded by WO-3: F-21, F-24, F-25.**

---

## STEP 1 — identity and scope, re-measured

| # | Command | Measured | Expected | |
|---|---|---|---|---|
| 1 | `git rev-parse origin/main` | `b671e1fb0c5bcf145d442076c229eca888afd674` | same | **MATCH** |
| 2 | `git rev-parse origin/staging` | `9ac4524d703035e6d2debd9e97ab9a0e73de3bc9` | REV-16 recorded `9ac4524d70…` | **MATCH** |
| 3 | last non-docs commit on staging | `a42b209e4f70a6efed4f3dcdb654e0f994416594` | same | **MATCH** |
| 4 | `git diff --name-only b671e1fb...a42b209e \| wc -l` | **138** | 138 | **MATCH** |
| 5 | numstat totals for that range | **+9,060 / −1,293** | +9,060 / −1,293 | **MATCH** |
| 6 | commit counts, with / without merges | **39 / 37** | 45 / 43 | **✗ MISMATCH** |
| 7 | every path changed after `a42b209e` on staging | `docs/PROMOTION_LEDGER.md` **only** (10 commits) | ledger only | **MATCH** |

Step 3 used `git log origin/staging --format=%H -- . ':(exclude)docs/**' | head -1`.

### FINDING W3-1 — the commit-count expectation is measured over a different range, at an earlier time

Not adjusted to fit. Diagnosed:

- `main..a42b209e` = **39** with merges, **37** without. Stable; this is the promotion range, and it
  is the range that produces the 138 files and the +9,060/−1,293 that both matched exactly.
- `main..origin/staging` = **49 / 47** today.
- The 10 commits after `a42b209e` are REV-7 … REV-16, **every one touching `docs/PROMOTION_LEDGER.md`
  only, zero merges**.
- **39 + 6 = 45 and 37 + 6 = 43.** The expectation is `main…staging` measured when **6** ledger
  revisions existed after the RC — i.e. at REV-12, the revision that ruled B13. Four further §28-
  permitted ledger revisions (REV-13…REV-16) have landed since, so the same command now returns 49/47.
  The merge count is consistent across all three readings (2 merges: 39−37, 45−43, 49−47).

**This is not an identity mismatch.** All three SHA anchors matched exactly, as did files-changed,
line counts, and the docs-only-after-RC property. The deviation is a *label* defect of exactly the
class `POST_PROMOTION_PLAN` C1 names — "never label a count `main…staging` in a document that lives
on `staging`" — and it goes stale on every ledger revision by construction. **Ledger-guard LG-05
independently reports the same thing** (step 2), which is corroboration from a second instrument.

**On the stop condition.** The work order says at step 1 "report it and continue", and in STOP
CONDITIONS "any identity mismatch in step 1". Read together: the stop condition is scoped to
**identity**, and identity is clean on all three anchors. Continued, with the conflict declared
here rather than resolved silently.

---

## STEP 2 — ledger-guard against the live repo

**Hash verification first (F-11).** Reconstructed from the project and verified before execution:

| File | sha256 | Bytes | |
|---|---|---|---|
| `ledger_guard.py` | `a9629fcf2d18d0ce7a3a2567e4471d261f2f963b503d3dcb6629c08274b79a42` | 29,338 | **required hash, MATCH** |
| `test_ledger_guard.sh` | `39e5bd950f9dd6884e25ef6e3a0e3a6e57593a5f9226ae94ac5e48f7b7560e91` | 17,913 | MATCH |
| `test_ci_propagation.sh` | `429eaa172305309b1723cabcf0a8f38d74d381134a5c2ae473f70bcd0e00c289` | 4,647 | MATCH |

**Self-tests, before trusting it:** `test_ledger_guard.sh` **PASS=66 FAIL=0** (per-check coverage OK
for LG-01…LG-10, adversarial, exit codes); `test_ci_propagation.sh` **PASS=14 FAIL=0**. Both stop
conditions clear.

**Live run** — `python3 ledger_guard.py --repo <checkout> --ledger docs/PROMOTION_LEDGER.md --base origin/main`,
from a detached checkout at `origin/staging`:

```
  fact source: MEASURED
LG-01  WARN  [MEASURED] canonical code-RC field holds 4 live SHAs; only the first (a42b209e4f70) is treated as the RC
LG-02  INFO  [MEASURED] code-frozen claim holds: only docs/ changed after a42b209e4f70
LG-04  INFO  [MEASURED] 1 SHA(s) recognised as void/superseded from structured fields
LG-05  FAIL  line 120  claims 43 commits for main…staging; endpoint value is 49 (47 no-merges)
LG-05  FAIL  line 120  claims 45 commits for main…staging; endpoint value is 49 (47 no-merges)
LG-05  FAIL  line 122  claims +9494/-1293 for main…staging; endpoint value is +10159/-1293
LG-05  FAIL  line 426  claims +9680/-1293 for main…staging; endpoint value is +10159/-1293
LG-05  FAIL  line 428  claims 44 commits for main…staging; endpoint value is 49 (47 no-merges)
LG-05  FAIL  line 428  claims 46 commits for main…staging; endpoint value is 49 (47 no-merges)
LG-06  INFO  [MEASURED] header REV-16 matches newest table row
LG-08  FAIL  line 46   §16.4 does not resolve to that exact subsection
LG-08  FAIL  line 790  §8.6 does not resolve to that exact subsection
LG-09  INFO  [MEASURED] no-tags claim confirmed for local AND remote
FATAL=0 FAIL=8 WARN=1 INFO=4     (exit 1)
```

**Your prediction — `FATAL=0 FAIL=8 WARN=1 INFO=4` — is exactly right.** The two abbreviation WARNs
vanished under live git; the LG-01 multi-SHA WARN remains. **Every finding now reads `[MEASURED]`.
B-2 is closed** — the guard's `--repo` form has been run and no finding rests on `facts_rev16.json`
any more.

The six LG-05 FAILs are W3-1 seen from the other side: four different `main…staging` figures
(43, 44, 45, 46 commits; +9,494 and +9,680 lines) recorded at four different moments, all now stale
against 49/47 and +10,159/−1,293. **A single class of defect, six instances, one fix: state figures
against the frozen endpoint.**

### FINDING W3-2 — the ledger-guard pack in the project is incomplete against its own MANIFEST

`claude/LEDGER_GUARD_V3_PACK/` holds **3 of the 12** files its own `MANIFEST.sha256` lists:
`ledger-guard.yml`, `test_ci_propagation.sh`, `test_ledger_guard.sh` (plus the manifest). Missing:
`00_README_V3.md`, `BLOCKED_ITEMS_V3.md`, `REV16_FINDING_CLASSIFICATION_V3.md`, `REV16_REPORT_V3.txt`,
`TRANSCRIPT_ci_linux.txt`, `TRANSCRIPT_selftest_linux.txt`, `V3_FIX_EVIDENCE.md`, `facts_rev16.json`,
and `ledger_guard.py` itself. The guard source exists only at the separate path
`claude/LEDGER_GUARD_V3_SOURCE_ledger_guard.py.md`. Not blocking — the three files needed were
recovered and hash-verified — but a pack that cannot satisfy its own coverage check is the same
class of defect as F-7.

---

## STEP 3 — B13 15b, the real drift re-measurement

Gates first: pack **25/25 files hash-verify**; `10_verify_pack.sh` **PASS=30 FAIL=0**;
`11_mutation_control.sh` **DETECTED=12 UNDETECTED=0 SKIPPED=0 EQUIVALENT=3 FALSE-EQUIV-CLAIMS=0
NO-OP-HERE=1**. All clear. Classification performed by hash-verified
`07_ws4_reference_impl.py classify`, per §4.5. Capture root `supabase/functions/` on both sides per
§4.4. Smoke-tested on two functions first per §4.8.

### (a) production vs repo @ `702e5ce` — against the 2026-08-26 baseline

| | MATCH | HEADER-ONLY | DRIFT | UNKNOWN | Σ |
|---|---|---|---|---|---|
| 2026-08-26 baseline | 21 | 21 | 29 | 0 | 71 |
| **strict protocol, as written** | **19** | **21** | **31** | **0** | **71** |
| **import-map-excluded** | **21** | **21** | **29** | **0** | **71** |

`classified + unknown = 71` on both readings. **UNKNOWN is 0 and nothing was folded into MATCH.**

### FINDING W3-3 — the baseline is reproducible, but only under one reading of the protocol; the difference is the import map

The entire 2-function gap is `handle-email-suppression` and `handle-email-unsubscribe`. Both deploy
exactly two files — `index.ts` and `deno.json` — while the RC-side closure emits only `index.ts`,
because `07_ws4_reference_impl.py closure` returns files reachable through import specifiers and
does **not** emit the import map itself. §4.5 rule 2 says "path sets differ → DRIFT", so strictly
applied the protocol calls them DRIFT. **Tested, not assumed:** excluding `deno.json` from the
deployed side turns both to **MATCH** at both RCs, giving 21/21/29 — the baseline exactly.

Both numbers are reported. I am not picking the one that matches. The protocol as written and the
2026-08-26 baseline disagree about whether a function's import map is part of its comparison set,
and that ambiguity should be ruled on before either figure is cited.

### (b) production vs repo @ `a42b209e` — the CURRENT frozen RC, what 15b requires

| | MATCH | HEADER-ONLY | DRIFT | UNKNOWN | Σ |
|---|---|---|---|---|---|
| **strict protocol** | **19** | **22** | **30** | **0** | **71** |
| **import-map-excluded** | **21** | **22** | **28** | **0** | **71** |

**Exactly one function changes verdict between the two RCs: `send-gift-credit`, DRIFT @ `702e5ce`
→ HEADER-ONLY @ `a42b209e`.** Materially: the indexed-RPC fix that F-17 identified as
production-ahead-of-*staging-deployment* **is present in the frozen RC**. Production is not ahead of
`a42b209e` for that function; it differs only in `secureHeaders.ts`. That refines F-17 without
contradicting it — the two comparisons have different reference points, which is F-16's whole point.

DRIFT causes @ `a42b209e`: 12 content-differs-outside-secureHeaders; 6 RC has `_shared/laneConfig.ts`
the deployment lacks; 6 RC has `_shared/s3.ts` the deployment lacks; 2 RC has
`account-deleted.tsx`+`laneConfig.ts`; 2 deployment has `deno.json`; 1 mixed; 1 RC has
`secureHeaders.ts` the deployment lacks (`submit-judge-decision` — see C-4).

**DIRECTION: not recorded.** §4.6 permits it only on a digest match to an attributable revision or
deployment metadata naming a SHA. Neither was established per-function here. `updated_at` is
explicitly non-probative and is not used. **DIRECTION = UNKNOWN**, and the 2026-08-26 "three
functions in the opposite direction" finding is **not carried forward**.

---

## STEP 4 — the 138-file review

**Scope declaration, §3.5 rule 6.**

- **What I read:** all **138** files, in full, at `a42b209e`, from a `git worktree` checkout.
  Instrument: `git diff --name-only b671e1fb...a42b209e` for the list, then `grep -n 'docs/'` over
  each **complete file**, never over diff hunks. All 138 were present at `a42b209e`; 0 deleted.
- **What I did NOT do:** I did not review the 138 files for correctness, security, or logic. I ran
  one content-wide search — the `docs/` reference question that C-10 concerns — plus the group
  totals below. A general code review of 138 files is not claimed and was not performed.

**Group totals, measured:** 31 A · 107 M · 0 D · **2 docs / 136 non-docs**. Matches the ledger's
recorded "138 files: 31 A, 107 M, 0 D — 2 docs, 136 non-docs" exactly.

**C-10 re-test — the auditor's figure is corroborated, the compiler's is not.** Searching full file
contents, **10** of the 138 reference `docs/`:
`.github/workflows/android-build.yml`, `.gitleaks.toml`, `docs/DECISIONS.md`,
`docs/PROMOTION_LEDGER.md`, `src/__tests__/candidatePatternWidening.test.ts`,
`src/__tests__/onePostFunnel.test.ts`, `src/components/__tests__/PrivacyGapDisclosed.test.ts`,
`src/components/post/PrivacyGapNotice.tsx`, `supabase/functions/_shared/manifestPlan.ts`,
`supabase/functions/measure-post-media/index.ts`.

**The executable consumer is confirmed by direct reading**, in the file C-10 names:

```
src/__tests__/candidatePatternWidening.test.ts:251
    const audit = readFileSync(join(ROOT, "docs/CANDIDATE_PATTERN_AUDIT.md"), "utf8");
src/__tests__/candidatePatternWidening.test.ts:252
    expect(existsSync(join(ROOT, "docs/CANDIDATE_PATTERN_AUDIT.md"))).toBe(true);
```

That is a test which reads a `docs/` file at runtime and asserts its existence — executable, not a
comment. **C-10 CORROBORATED: 10 files, and the executable consumer is real.** The earlier "four
files, all comments, a precaution" was produced by searching hunks and is false. Every other
reference I read is prose or comment; that classification came from reading each line, and I note
that a keyword classifier alone would have mislabelled at least two of them.

### The sixteen Track R defects — BLOCKED, cannot be tested

**The list is not available to me.** It is in neither the project (searched) nor my context (it
predates a context compaction in this session). I will not invent, infer, or partially reconstruct
sixteen defect statements in order to appear to have tested them — a fabricated CORROBORATED/NOT
CORROBORATED table would be worse than an empty one.

**BLOCKED. Closable by:** the owner (Neil Basu) pasting the sixteen items. The repo access needed to
test them is now in place, so this is the only missing input. **Track R remains not started** — this
is a request for the list as evidence to test, not a step into remediation.

---

## STEP 5 — the independent test-suite run

**Credential rule enforced and proven before the suite started.** Run under `env -i` with an
explicit scrub proof printed first: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`,
`SUPABASE_DB_URL`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `R2_ACCESS_KEY_ID`,
`R2_SECRET_ACCESS_KEY`, `R2_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `AWS_ACCESS_KEY_ID`,
`AWS_SECRET_ACCESS_KEY`, `GH_TOKEN`, `GITHUB_TOKEN`, `CRON_SECRET` — **all unset**, and a regex sweep
of the whole environment for `SUPABASE|R2_|CLOUDFLARE|AWS_|GITHUB|GH_|TOKEN|SECRET|_KEY` returned
**nothing**. Dependencies installed with `npm ci` from the committed lockfile.

| | |
|---|---|
| Test files | **179** (178 passed, 1 not run) |
| Total tests | **2,476** |
| **Passed** | **2,475** |
| **Failed** | **0** |
| **BLOCKED — requires live provider credentials** | **1** |
| Skipped for any other reason | **0** |

**The one BLOCKED test, named:**

> `src/test/judging-invariants.test.ts` → *"Phase R3 — judging data invariants › judging_invariants_check returns all 'ok'"*

It is gated by `describe.skipIf(!canRun)` where `canRun = Boolean(URL && SERVICE_KEY)` reading
`VITE_SUPABASE_URL`/`SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_SERVICE_KEY`. With them
it constructs a **service-role** Supabase client and calls `judging_invariants_check()` **against the
live database**. Under your rule it is **BLOCKED, not skipped, and was not run with real
credentials**. Your instruction was load-bearing: this is precisely the test whose silent execution
would have been a live production reach.

### FINDING W3-4 (severity: MEDIUM) — a credential-gated live-database test self-skips into a green result

The suite reports this as "1 skipped" inside an otherwise all-green run. Nothing in the summary line
distinguishes "deliberately not applicable" from "the one test that touches production data did not
run". A reader of `Tests 2475 passed | 1 skipped` would reasonably conclude the suite is complete.
The count of tests never exercised without secrets should be surfaced as its own gate, not folded
into "skipped".

---

## STEP 6 — C-1's cause, answered

**The repository holds exactly ONE copy of each shared module.** Measured at `a42b209e`:

| Module | Copies in repo | Path | sha256 (12) | Bytes |
|---|---|---|---|---|
| `s3.ts` | **1** | `supabase/functions/_shared/s3.ts` | `cb6dd4ae8d13` | 15,400 |
| `imageDims.ts` | **1** | `supabase/functions/_shared/imageDims.ts` | `29499a56d639` | 5,156 |
| `registry.ts` | **1** | `supabase/functions/_shared/transactional-email-templates/registry.ts` | `ef52c267d31c` | 2,315 |
| `secureHeaders.ts` | **1** | `supabase/functions/_shared/secureHeaders.ts` | `bec30cb9dd39` | 6,963 |
| `laneConfig.ts` | **1** | `supabase/functions/_shared/laneConfig.ts` | `66d014481b1f` | 4,909 |

**C-1's "Bounded" clause is hereby updated. The question it left open is now answered:**

> **Previously:** "Whether these variants are intentional (per-function forks) or accidental drift is
> not decidable from deployment state — that needs the repository."
>
> **Now measured:** the repository holds **one** copy of each. There are **no per-function forks in
> source**. Production's 4 `s3.ts` variants and 4 `imageDims.ts` variants are therefore
> **accumulated deployment drift** — each function carries a snapshot of the shared module as it
> stood when *that function* was last deployed. **C-1's cause is DETERMINED: drift, not design.**

Sharper still: **not one of production's four `s3.ts` variants (7,590 / 7,777 / 9,143 / 9,980 bytes)
matches the repo's 15,400-byte copy.** All four are stale. For `imageDims.ts`, exactly one —
`backfill-image-dims`, `29499a56d639` — matches the repo; the other three are stale. This raises
C-1's practical severity: the destructive-path functions `purge-s3-orphans` and `detect-orphan-files`
are both running storage-layer code that no longer exists in the source tree.

---

## STEP 7 — F-26 against the repository → FINDING C-4

Every one of the 71 production slugs classified by CORS mechanism on **both** sides.

| Mechanism | Production, deployed | Repo @ `a42b209e` |
|---|---|---|
| Bundles `_shared/secureHeaders.ts` (origin allowlist) | **27** | **28** |
| **Local `corsHeaders` object — wildcard `*`** | **39** | **38** |
| Imports `corsHeaders` from `npm:@supabase/supabase-js@2/cors` | 2 | 2 |
| No CORS handling at all | 3 | 3 |
| | **71** | **71** |

**Deployed mechanism vs repo mechanism: 70 of 71 agree. Exactly one differs:**

> `submit-judge-decision` — **deployed** carries a local wildcard `corsHeaders` object; the **repo at
> `a42b209e` calls `getSecureHeaders`**. The remediation exists in the frozen RC and is not deployed.
> This is the same function the step-3 drift run independently flags as DRIFT for the reason
> "RC has `_shared/secureHeaders.ts` the deployment lacks" — two instruments, one conclusion.

### FINDING C-4 (severity: HIGH) — B13's CORS residual risk does not hold in the frozen RC either, and promotion does not fix it

C-3 established that the *deployed* population splits four ways against B13's "ALL 71 functions …
`secureHeaders.ts` byte-identical". With repo access the finding hardens considerably:

1. **The split is in the source of truth, not just in deployment.** The frozen RC `a42b209e` itself
   contains **38 functions with a local wildcard-`*` CORS object** and 3 with none. B13's risk
   describes a uniform condition that **the candidate tree does not satisfy**.
2. **Promotion does not close it.** Deploying `a42b209e` would change the CORS mechanism of exactly
   **one** function (`submit-judge-decision`, local wildcard → shared allowlist). The other 38
   wildcard functions are wildcard in the RC and would remain so. Any remediation reasoned about as
   "the shared helper is fixed, so promotion fixes CORS" reaches **1 of 39**.
3. **What B13's inner clause does hold.** `secureHeaders.ts` is byte-identical across all 27
   deployed functions that bundle it (one variant, 1,507 bytes), and all 27 pass a request so the
   helper's `"*"` no-argument default is never exercised. The clause is true *as written*; the defect
   is that it is scoped to "every function that bundles it" while the headline is scoped to "ALL 71".

**Bounded.** Deployed source and repository source, read on 2026-08-30. No endpoint was called, so
nothing here establishes runtime response headers. Whether the 38 wildcards are intended is a design
question the source cannot answer.

---

## Standing items

- **Hard gate:** cleared for **this platform and this run only**. `11_mutation_control.sh` returned
  `UNDETECTED=0` with **`NO-OP-HERE=1`** — **M8c has still never executed anywhere it is a defect.
  BLOCKED, not coverage.** Windows figures in `BLOCKED_ITEMS_revision6.md` (B3) remain
  **AUDITOR-ATTESTED**, not witnessed by this session. B4 (symlink resolution) untested on every
  platform by construction.
- **F-11 applied:** nothing was executed before it hash-verified — the WS4 pack (25/25) and all three
  ledger-guard files.
- **F-21, F-24, F-25:** carried forward, still open.
- **Track R:** not started. The sixteen-defect list is BLOCKED for want of the list itself.
- **No provider mutation of any kind occurred.**

**Nothing above closes a §25 row.**
