# WORKSTREAM 1 — THE EIGHT §25.3 INFRASTRUCTURE ROWS

> **Execution provenance: `SPECIFIED, NOT EXECUTED` (§0.11).** No query here has been run by anyone.

**All eight are `BLOCKED` for the compiler.** Access per ledger §25.7 (owner ruling **D-13**).
Fill each block completely. **An unfilled block is BLOCKED, never a pass.**

Template — copy per row:

    REQUIREMENT   :
    INSTRUMENT    :  (exact read-only command/query, verbatim)
    TARGET        :  (lane / project ref / bucket / repo+ref)
    GENERATED BY  :  auditor | owner | compiler        <-- decides the class, §0.5
    IDENTITY CTRL :  (§0.6a — proof the output came from the target it claims)
    TIMESTAMP UTC :
    SANITIZED OUT :  (no credentials in any form, §0.3)
    COMPLETENESS  :  (§0.6 control and its result)
    NEG CONTROL   :  (§0.7 — the stated discriminating result; record output + exit status
                       + which of §0.7's three rows it is: discriminating / non-discriminating / BLOCKED)
    RESULT        :  PASS | FAIL | BLOCKED
    CLASS         :  VERIFIED | OWNER-ATTESTED | INFERRED | BLOCKED

---

## 1.1 — Production database policy state  *(AF-17, highest-severity live finding)*

**Requirement.** Production is missing two RLS policies staging has: banned users can comment on ads,
and hidden ads' threads are readable. Ledger claims production **7**, staging **9**.

**Instrument** — read-only, `jtdtehuqtinjxropkkcn`:

```sql
SELECT schemaname, tablename, policyname, permissive, roles, cmd
FROM pg_policies
WHERE tablename = 'ad_creative_comments'
ORDER BY policyname;
```

**Completeness control.** Also run, and record, the **unfiltered** count so a narrow filter cannot
hide rows:

```sql
SELECT tablename, count(*) FROM pg_policies
WHERE schemaname = 'public' GROUP BY tablename ORDER BY tablename;
```

**Identity control — corrected at revision 3.** A correct answer read from the wrong lane is the
easiest possible error here, so the result must be bound to the **project**, not to the database name.

> ⚠ **Do NOT use `current_database()` as the discriminator.** Both Supabase projects legitimately
> return **`postgres`**. Revision 2 required these values to **differ** — a control that would have
> raised a false BLOCKED on entirely correct data.

**Bind the evidence to the project instead:**

1. **Authenticated console / SQL-editor project ref**, captured from the session that ran the query —
   production `jtdtehuqtinjxropkkcn`, staging `ztzutckwdhetphwghuzj`.
   **The two project refs MUST differ.** That is the discriminator.
2. Alongside, record for context (never as the discriminator):

```sql
SELECT current_database(), current_user, version(),
       current_setting('server_version_num'),
       inet_server_addr()::text AS server_addr;
```

3. Record a **sanitized target identity**: project ref, region if shown, and the console URL path
   with any token or session parameter **removed**. No connection string, ever.

**Negative control — filtering. Must produce the stated discriminating result (§0.7); an
expected 404/empty result is discriminating even though the command exits non-zero.** Same query for a table that does not exist —
`WHERE tablename = 'zzz_does_not_exist'` — **must return 0 rows.** If it returns rows, the query is
not filtering and 1.1's result is void.

**Expected.** 7 policies **before** D-10; **9 after**. ⚠ **D-10 has NOT been executed.** If this
returns 9 now, that is an **unauthorised change** — stop and record under §21.

## 1.2 — Staging database policy state

Identical to 1.1 against `ztzutckwdhetphwghuzj`, **including the identity control**. The two
**project refs must differ** (`jtdtehuqtinjxropkkcn` vs `ztzutckwdhetphwghuzj`); if they match, one
of the two runs hit the wrong lane and **both** rows are void. **`current_database()` may read
`postgres` in both — that is expected and is not a failure.** **Expected: 9.**
**Cross-check:** the two policies present in staging and absent in production must be **named**, not
counted — a count match can hide a name mismatch.

## 1.3 — Deployed edge-function state  *(B13 risk 2)*

**Requirement.** Ledger claims production `submit-judge-decision` is **v23, ACTIVE**, serving
`Access-Control-Allow-Origin: *`. Also: **71** functions deployed, `_shared/secureHeaders.ts`
byte-identical at md5 `58b9f45d…`.

**Instrument.** Supabase dashboard → Edge Functions (read-only), or `supabase functions list`
executed **by the auditor under their own login**. Record **name, version, status, updated_at** for
all 71 — see workstream 4 for the full protocol.

**Identity control.** Record the **project ref** in the same command output (`--project-ref` echoed,
or the console's project selector) so the inventory is provably production and not staging.

**Completeness control.** **Discover the live count `N`. Do not assume 71.** Record `N`, and record
whether `N = 71`. **`N ≠ 71` is a FINDING, not a void result** — it means functions were added or
removed since 2026-08-26 and §23.5.3's re-measurement is triggered. Carry `N` into workstream 4.

**Negative control — filtering. Must produce the stated discriminating result (§0.7); an
expected 404/empty result is discriminating even though the command exits non-zero.** Read a function name known to be **absent** — it must 404. A
listing or reader that answers for everything is not filtering.

⚠ **Read only. Do not deploy, do not redeploy, do not "refresh".** B13 condition 2 excludes all
function deployment from this release.

## 1.4 — R2 token policy / bucket scope  *(G8, D-12)*

**Requirement.** Token `staging-upload`, id `73a7920647481fd93553f9c1f68bf5a3`, has **exactly one**
permission policy — **R2 › `50mm-staging`, Bucket Item Write** — and **no policy naming `50mm`**.

**Instrument.** Cloudflare dashboard → Manage Account → API Tokens → that token → **read the policy
list**. Read-only; **do not roll, do not edit, do not reveal the secret.**

**Identity control.** Record the **token id shown on the page** and confirm it equals
`73a7920647481fd93553f9c1f68bf5a3`, and the **account id** `a7810011a99de537a210130f86306785`. The
page must be the one for this token, not a template.

**Completeness control — the token inventory.** List **every** API token in the account and record
`id`, `name`, `status`, and **whether each has any R2 policy naming `50mm`**. The conclusion is not
"there is one token" — it is **"no token in this account grants write to `50mm` to the staging
credential"**, which needs the whole list. Record the count as observed, whatever it is.

> **Corrected after round-5 review.** The previous control required comparing against *a second
> token*, which **may not exist** — an unsatisfiable control that would have forced a false BLOCKED.

**Negative control — filtering. Must produce the stated discriminating result (§0.7); an
expected 404/empty result is discriminating even though the command exits non-zero.** Open the token list filtered to a status or name that matches
nothing; it must return an empty list. A view that shows every token regardless of filter is not
filtering, and the inventory above cannot be trusted.

> ⚠ **The owner re-reading this does NOT close the row.** It is already OWNER-ATTESTED at that class.

## 1.5 — GitHub `staging` Environment  *(§7.1, unresolved)*

**Requirement.** Does a `staging` Environment exist, and does it carry **its own** `SUPABASE_DB_URL`?
Without it, `apply-migration.yml target=staging` cannot run.

**Instrument.** Repo → Settings → Environments (read). Record environment **names**, and for each,
the **secret names only** — **never values**, and never a value's length or prefix.

**Completeness control.** List **all** environments, not just `staging`; a production-only
configuration is itself a finding.

**Negative control.** Confirm a non-existent environment name is not listed.

## 1.6a — Cloudflare R2 bucket state

**Requirement.** `50mm` — created 2026-03-07, APAC, Standard, **Public Access Enabled**.
`50mm-staging` — created 2026-08-21. Third bucket `agentcrm` is unrelated — **do not touch.**

**Instrument.** Dashboard → R2 → bucket → Settings (read).

> ⚠ **Bucket size is context only, never a control.** It is GB-rounded and moved 1.09 → 1.12 GB
> between 08-27 and 08-29 because production is live. **It cannot detect a 0-byte object.**

**Identity control.** Record the **bucket name and account id** from the page itself for each read.

**Completeness control.** List **all** buckets in the account with name and creation date. The ledger
expects exactly three — `50mm`, `50mm-staging`, `agentcrm`. **A fourth bucket is a finding**; a
missing one is a finding. Record what is there.

**Negative control — filtering. Must produce the stated discriminating result (§0.7); an
expected 404/empty result is discriminating even though the command exits non-zero.** Request settings for a bucket name that does not exist; it must
error, not fall back to a default page.

> **Corrected after round-5 review.** The previous control required that some arbitrary setting
> *differ* between the two buckets. Two buckets may legitimately be configured identically, so that
> control could fail on correct data.

## 1.6b — `isolation-probe/` prefix searches

**Requirement.** Production `50mm`, prefix `isolation-probe/` → **zero objects.**

**Instrument.** R2 → `50mm` → Objects → "Search objects by prefix" → `isolation-probe/`.

> ⚠ **Recorded limitation that must be carried, not glossed: this is an AFTER-only measurement.**
> No before-baseline exists for run `33079091310`, and none can be created retroactively (§23.3).

**Negative control.** Search a prefix that **does** exist in the same bucket — it must return
objects. A search returning zero for everything proves nothing.

## 1.6c — Cloudflare Zero Trust posture

**Requirement.** **NEED EVIDENCE.** Never measured. Carried forward unresolved by B13 (§23.5.1).

> ⚠ **Must not be recorded as a pass.** There is no prior measurement to confirm — this is a **first**
> measurement, and until it is taken the correct entry is `BLOCKED`, not `N/A`.

**Instrument.** Zero Trust dashboard → Access → **Applications**, and → **Policies** (read).

**Completeness control (added after round-5 review — the previous version had no control at all).**
Record the **full inventory**: every Access application (name, domain/host, type) and every policy
(name, action, applied-to), with **counts**. Then state explicitly, from that inventory, whether any
entry covers `www.50mmretina.com`, `staging.50mmretina.com`, or the R2/CDN hosts. **An empty
inventory is a valid, recordable result** — "Zero Trust is not deployed for these origins" — and is
**not** the same as "not measured".

**Identity control.** Record the **account id** on the page (`a7810011a99de537a210130f86306785`).

**Negative control — filtering. Must produce the stated discriminating result (§0.7); an
expected 404/empty result is discriminating even though the command exits non-zero.** Filter the application list by a host that certainly does not exist
(e.g. `zzz-nonexistent.50mmretina.com`); it must return **no** entries. If the filter returns the
full list, the "does any entry cover X" reading above is unreliable and the row is BLOCKED.
