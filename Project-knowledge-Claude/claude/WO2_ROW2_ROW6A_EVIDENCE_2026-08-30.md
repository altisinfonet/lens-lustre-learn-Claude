# Row 2 (§1.2) and Row 6a (§1.6a) — evidence blocks

Nothing in this file closes a §25 row. Every block below is **GENERATED BY: compiler**, which by
§25.4 and §0.5 caps its class at the compiler tier regardless of how clean the measurement is.

---

## Row 2 — §1.2 Staging database policy state

    REQUIREMENT   :  Staging holds 9 RLS policies on public.ad_creative_comments; production holds
                     7. The two staging-only policies must be NAMED, not counted.
    INSTRUMENT    :  SELECT schemaname, tablename, policyname, permissive, roles, cmd
                     FROM pg_policies WHERE tablename = 'ad_creative_comments'
                     ORDER BY policyname;
                     executed read-only via mcp__Supabase__execute_sql
    TARGET        :  staging, project ref ztzutckwdhetphwghuzj
    GENERATED BY  :  compiler
    IDENTITY CTRL :  Project ref passed as an explicit, separate parameter on each call —
                     ztzutckwdhetphwghuzj (staging) vs jtdtehuqtinjxropkkcn (production).
                     THE TWO REFS DIFFER — that is §1.1's stated discriminator, satisfied.
                     Context only, never the discriminator: current_database()=postgres,
                     current_user=postgres, PostgreSQL 17.6, server_version_num=170006.
                     ⚠ current_database() reads 'postgres' on staging — expected per §1.2, not a failure.
    TIMESTAMP UTC :  2026-08-30
    SANITIZED OUT :  No credential, connection string, key or token appears in any output above.
                     server_addr was returned by the context query and is NOT reproduced here.
    COMPLETENESS  :  Unfiltered, whole-schema: staging 688 policies across 139 tables;
                     production 686 across 139 tables. Filtered count for the target table
                     agrees with the unfiltered count on both lanes (9 and 7) — the narrow
                     filter is hiding nothing.
    NEG CONTROL   :  Same query, WHERE tablename = 'zzz_does_not_exist', RUN AGAINST THE STAGING
                     REF ztzutckwdhetphwghuzj (not inherited from Row 1's production run).
                     Output: []  — zero rows. Exit: success.
                     §0.7 row: DISCRIMINATING. The reader filters; Row 2's result is not void.
    RESULT        :  PASS — staging returns 9, the expected value.
    CLASS         :  compiler-measured. NOT VERIFIED for §25 purposes (§25.4).

**Named cross-check — the two staging-only policies (named, not counted):**

| Policy name | permissive | roles | cmd | staging | production |
|---|---|---|---|---|---|
| `Ad comments follow the ad's visibility` | RESTRICTIVE | {authenticated} | SELECT | present | **absent** |
| `Banned users cannot comment on ads` | RESTRICTIVE | {authenticated} | INSERT | present | **absent** |

The other 7 are present on both lanes with matching permissive/roles/cmd: `Deleted accounts cannot
delete` (DELETE), `Deleted accounts cannot insert` (INSERT), `Deleted accounts cannot update`
(UPDATE), `Members comment as themselves` (INSERT), `Members edit their own ad comment` (UPDATE),
`Members or admins delete an ad comment` (DELETE), `Members read ad comments` (SELECT).

**Production-only policies on this table: none (0).** The delta is one-directional.

These two names correspond exactly to the ledger's description of the gap — banned users able to
comment on ads, and hidden ads' threads readable. The name-level cross-check §1.2 demands therefore
**passes**: the count match is not concealing a name mismatch.

### Row 1 (§1.1) — read only to source the names above, and its stated stop-condition checked

§1.1 warns: expected 7 **before** D-10 and 9 **after**; D-10 has not been executed; a reading of 9
now is an unauthorised change to be stopped on and recorded under §21.

**Production returned 7.** That is the pre-D-10 expected value. **No §21 condition triggered.**
Nothing further recorded under §21 by this run.

### Whole-schema extension of the completeness control

The unfiltered totals differ by exactly 2 (688 vs 686), which is *consistent with* the two named
policies being the only schema-wide difference — but consistency is not proof, and per F-13 a
cheaply-falsifiable claim should be tested rather than asserted. Tested: per-table policy counts
were compared across all tables on both lanes.

- Tables present on staging but not production: **none**. Production but not staging: **none**
  (139 tables on each lane).
- Tables whose policy count differs between lanes: **exactly one — `ad_creative_comments`,
  production 7, staging 9 (+2).**

**Stated limit of that control:** it compares *counts* per table. A same-count name substitution
inside some other table (one policy renamed or replaced, count unchanged) would not be visible to
it. Detecting that needs a full name-level diff across all 139 tables, which this run did not do.
So: **no count-level divergence exists anywhere else in `public`; name-level equivalence outside
`ad_creative_comments` is UNTESTED**, not established.

---

## Row 6a — §1.6a Cloudflare R2 bucket state

Restated per **F-6** as **PARTIAL**, and now backed by measurement rather than prose.

    REQUIREMENT   :  50mm — created 2026-03-07, APAC, Standard, Public Access Enabled.
                     50mm-staging — created 2026-08-21. Third bucket agentcrm unrelated, do not touch.
                     Ledger expects exactly three buckets.
    INSTRUMENT    :  mcp__Cloudflare_Developer_Platform__r2_buckets_list (read-only)
                     mcp__Cloudflare_Developer_Platform__r2_bucket_get   (read-only)
    TARGET        :  Cloudflare R2, account-level bucket listing
    GENERATED BY  :  compiler
    TIMESTAMP UTC :  2026-08-30
    SANITIZED OUT :  No token, secret, or credential read, revealed, rolled or edited.
                     agentcrm was enumerated by the account-level listing only — never opened,
                     never read, never modified.
    COMPLETENESS  :  All buckets in the account listed: exactly THREE.
                       50mm          created 2026-03-07T12:19:15.228Z
                       50mm-staging  created 2026-08-21T19:22:35.655Z
                       agentcrm      created 2025-12-11T13:17:22.390Z
                     No fourth bucket. None missing. Matches the ledger's expectation of three.
    NEG CONTROL   :  r2_bucket_get(name="zzz-does-not-exist-negative-control")
                     Output: 404, code 10006, "The specified bucket does not exist."
                     §0.7 row: DISCRIMINATING — it errored rather than falling back to a default
                     page. Per §0.7 an expected 404 is discriminating despite the non-zero exit.
    RESULT        :  PARTIAL
    CLASS         :  see the split below — a single class for this row would be an overclaim

**The split F-6 requires:**

| Sub-claim | Status | Basis |
|---|---|---|
| The three buckets exist, and only those three | **measured, PASS** | account listing returned exactly `50mm`, `50mm-staging`, `agentcrm` |
| `50mm` created 2026-03-07 | **measured, PASS** | `2026-03-07T12:19:15.228Z` |
| `50mm` location APAC, class Standard | **measured, PASS** | `location: APAC`, `storage_class: Standard` |
| `50mm-staging` created 2026-08-21 | **measured, PASS** | `2026-08-21T19:22:35.655Z` |
| **Bucket sizes** | **BLOCKED** | two independent reasons, below |
| `50mm` **Public Access Enabled** | **BLOCKED** | this instrument returns no public-access field; unread, therefore unverified |
| **Account id** `a7810011a99de537a210130f86306785` | **BLOCKED** | this instrument returns no account id, so the listing is not bound to that account by evidence in hand |

**Why sizes are BLOCKED, not merely unmeasured — both reasons stated:**

1. The instrument used returns no size field at all, so no size was read this run.
2. Independently, §1.6a itself rules size out as evidence: it is GB-rounded, it moved 1.09 → 1.12 GB
   between 08-27 and 08-29 because production is live, and **it cannot detect a 0-byte object.**
   Even a size that had been read could not have carried this row.

So sizes are BLOCKED on the merits, not pending a retry. Existence is measured; sizes are BLOCKED —
which is exactly the PARTIAL restatement F-6 called for.

**Context, recorded as context and not as a control:** `50mm-staging` reports `location: ENAM`
against `50mm`'s `APAC`. §1.6a specifies a location only for `50mm`, so this is not a deviation from
any stated requirement, and per the round-5 correction quoted in the pack a control requiring the
two buckets to differ (or match) on an arbitrary setting is not a valid control. Recorded so it is
on the record, ranked as context.
