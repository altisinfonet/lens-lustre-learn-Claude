# Phase 0 · 0.4 / 0.5 — candidate baseline artefacts, with provenance

**Produced by D2 on 2026-09-02, 11:56Z–11:58Z.** Two NDJSON files, generated by **PR #126's instruments, unmodified**, against a faithful staging-lane build. This is the artefact the 0.4 gate asks for — *"JSON committed under `docs/evidence/d2/baseline/` with a UTC measurement timestamp on every line"* — delivered through the transfer channel because no push authority exists on this side.

**Labelled CANDIDATE, not baseline, for one reason:** the instrument at `85fc2ea` writes the wrong unit numbers into the records (review F-1 — "P16" for one-chunk-per-language, "P21" for edge compression). I recommend the Auditor accept the *numbers* here as the Phase 0 reading and require **re-generation after F-1 is fixed** before the file is committed as the baseline the Gate Register points at. The numbers will not change; the strings will.

## Provenance — stated here because the instrument could not record it (review F-3)

| | |
|---|---|
| Source tree measured | `origin/staging` @ **`ef5d4a3790ec`** — "Auditor: sync Gate Register Revision 2 onto staging" |
| Instrument | `scripts/web-baseline.mjs` sha256 `4287dd22d657410db903fddf67fd8ef2bf12fa3a2a1b1bd567a9baea2f2894dd` (27,752 B) and `scripts/web-vitals-report.mjs` sha256 `0149fd0216bf801fdd42cd715d2ce707de74c71db293b47e3cf92cc10eb803bc` (23,442 B), both at PR #126 head **`85fc2ea`**, run unmodified |
| Build command | `npm run build` — the full chain, including `verify-html-tokens.mjs`, which passed: "1 HTML file under dist/ carry no unsubstituted VITE_ token" |
| Lane env | the **staging** lane's five public `VITE_*` values, taken verbatim from `.github/workflows/web-build.yml` lines 168–178 (D2's own file), not invented. `VITE_SITE_ORIGIN=https://staging.50mmretina.com`, `VITE_SUPABASE_PROJECT_ID=ztzutckwdhetphwghuzj`, `VITE_CDN_HOST=cdn-staging.50mmretina.com` |
| Node | v22.22.2, linux/x64 |
| Chromium for the harness | `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` via `--chromium=`, because the sandbox's preinstalled build (1194) differs from Playwright 1.62's pin (1234). Recorded in the `run` record's `chromiumPath`. **CI will use Playwright's own pinned build; the two are not the same binary and the reading may differ.** |
| `dist/` | 282 files, 7,349,662 B; 246 `.js` under `assets/` (the instrument's `summary` says `jsChunks: 247` — recorded, not resolved) |

## The readings

| Measure | Value | Class |
|---|---|---|
| Entry module `assets/index-Da-0EamU.js` | **1,609,657 B raw · 498,882 B gzip-9 · 391,184 B brotli-11** | VERIFIED (arithmetic over bytes on disk) |
| Initial payload — entry + 4 modulepreloads + render-blocking CSS, 6 files | **2,140,990 B raw · 635,688 B gzip · 509,668 B brotli** | VERIFIED |
| All assets | 7,349,662 B raw · 2,896,911 B gzip · 2,584,708 B brotli | VERIFIED |
| Language dictionaries | **one chunk**, `assets/translations.rest-CApbV07m.js`, **498,682 B raw · 111,552 B gzip · 74,415 B brotli**, carrying bn · gu · hi · mr · ta · te. Per-language bytes: **null, `blocked: shared-chunk`** — a per-language size does not exist in this build. Script-block *estimates* are present and flagged `exact: false`; hi/mr flagged `separable: false` (same script) | VERIFIED that it is null; the estimates are INFERRED by the instrument's own labelling |
| LCP, route `/`, 3 runs | **4,116 · 4,044 · 4,028 ms — median 4,044 ms** | VERIFIED **as an emulated-profile instrument reading** |
| CLS | 0 (all three runs) | VERIFIED, same caveat |
| INP≈ | 72 · 48 · 80 ms — median 72 ms, from one scripted interaction, method named in the record | VERIFIED as an approximation; **not** the web-vitals p98 |

**Compression figures are a floor, not delivery.** gzip-9 / brotli-11 computed here over bytes on disk. What the edge returns is P15's fetch.

**The vitals are an emulated Android profile.** 360×800, CPU ×4, Fast-3G shaping, in headless Chromium on a Linux container. They are a VERIFIED reading *of the instrument*. They are **not** a real-device reading, they must never be filed as one, and they are never mixed with an OWNER-ATTESTED device reading — the runbook in this delivery is how the device reading is taken.

## The gate, checked literally

296 records across the two files; **0 without both `runId` and a `measuredAtUtc` ending in `Z`.** Checked by a separate script on 2026-09-02 11:58Z, not by trusting the instrument's own claim.

## Files

| File | Bytes | sha256 |
|---|---|---|
| `web-baseline-2026-09-02T11-56-47-766Z-ed58c845.ndjson` | 129,615 | `86ef66e7ec4d733c55e4b5b3d8da6b15d6b7b162882373d7608c896ff30d9ffc` |
| `web-vitals-2026-09-02T11-57-50-102Z-dacec022.ndjson` | 7,121 | `286b5c490feff21572bfb86f77dc6938186f4e8b610b9a265c2f704e678d8bc0` |

## Disagreements recorded, not resolved

| Figure | Addendum (2026-09-01) | This artefact (2026-09-02) | Note |
|---|---|---|---|
| Entry bundle | "1.47 MB" | 1,609,657 B (entry module alone) · 2,140,990 B (initial payload) | Different definitions. The Addendum's figure came from a file-header comment, not a build. Neither is wrong; they measure different things. |
| Language chunk | 527,720 B (`translations.rest.ts`, source) | 498,682 B (built chunk) | Source vs built. Both stand. |
| Raw `<img>` count | 158 (Addendum) · **223** (Auditor at `ef5d4a37`) | not re-counted here | The Owner's instruction: recorded, not resolved. 0-D2-03 is the inventory's home; I did not add a third number. |

*D2. This file records; it approves nothing and closes nothing.*
