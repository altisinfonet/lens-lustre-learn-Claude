# Phase 0 · 0.4 — regenerated baseline artefact, corrected instrument

**Regenerated by D2 on 2026-09-02 at 12:27:35Z** with the F-1/F-3-corrected `scripts/web-baseline.mjs` from this folder. Same commit, same env, same method as the candidate filed earlier today (`claude/d2-phase0/baseline/`, EVIDENCE FILED).

## What changed between candidate and this file — and what did not

Every measured value is **identical** across all 291 records. Checked by diffing the two files with `measuredAtUtc`, `runId` and `git` stripped and the two corrected strings normalised: **one record differs, and only in the `dist` path string** (same directory, reached through a different working directory). Not one byte count, hash or classification moved.

What changed, by design:
- **F-1:** the `run` record's `compression.meaning` now says **P15**; every `language` record's `blockedReason` now says **P12**. Zero occurrences of `P16` or `P21` remain in the file (grep-checked).
- **F-3:** the `run` record now carries `git.commit = ef5d4a3790ec9f19c0ffe5ea9f4e533472ecfc95` — the tree `dist/` was built from — read by the instrument itself through the worktree's `gitdir:` pointer. The candidate had `null, null`. `branch` is `null` because the checkout is detached at that commit; that is accurate, not a defect.

## Provenance

| | |
|---|---|
| Source tree measured | `origin/staging` @ **`ef5d4a3790ec`** — now recorded **in the artefact itself** |
| Instrument | `scripts/web-baseline.mjs` in this folder, sha256 `410d79fd3423ed3c41bf8714806e39f7cceded244e4fa5d1b349b334eab2d5f6` |
| Working directory at run | the staging worktree at `ef5d4a3`, so `repoRoot` = the built tree, not the instrument's checkout |
| `dist/` | built 2026-09-02 ~11:5xZ by `npm run build` (full chain, `verify-html-tokens` green), staging lane env from `web-build.yml` lines 168–178; unchanged between candidate and regeneration |
| Node | v22.22.2, linux/x64 |

## Readings (unchanged from the candidate)

Entry module `assets/index-Da-0EamU.js` **1,609,657 B raw · 498,882 B gzip-9 · 391,184 B brotli-11** · initial payload (6 files) **2,140,990 B raw · 635,688 B gzip** · all assets 7,349,662 B · one language chunk **498,682 B raw · 111,552 B gzip** carrying bn·gu·hi·mr·ta·te, per-language bytes `null` / `blocked: shared-chunk`. 291 records, **0 unstamped**.

## File

| File | Bytes | sha256 |
|---|---|---|
| `web-baseline-2026-09-02T12-27-35-946Z-1319760d.ndjson` | 129655 | `fe476fc8890734afe84ea4cfc4b996114307f0707265c8d9bd15b75e0e078fa7` |

The 0.5 vitals artefact is **not** regenerated here: `web-vitals-report.mjs` is byte-identical to PR head (F-2 is a workflow step), so the candidate vitals file stands as filed.

*D2. Records; approves nothing; closes nothing.*
