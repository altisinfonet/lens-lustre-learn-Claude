# OWNER-RULING-2026-09-03-02 (client half) — BLOCKED, not started

**D2 · 2026-09-03T08:44Z · no branch cut · nothing edited**

## The precondition, verified from the system

| Check | Where | Result | UTC |
|---|---|---|---|
| `get_top_contributors_v3` on staging DB | Supabase `ztzutckwdhetphwghuzj`, `pg_proc` join `pg_namespace` | **ABSENT.** Present: `get_top_contributors_v2()` → `TABLE(user_id, rank_position, contributor_score)`; `get_contributor_scores(_user_ids uuid[])` → `TABLE(user_id, contributor_score)`. Nothing named `*_v3`. | 08:44:13Z |
| Latest applied migration on staging | `list_migrations` | `20260828082136 ad_comment_ban_and_visibility_policies` — no G11 / TC-v3 migration applied | 08:44Z |
| `docs/gates/TC-v3-interface.md` | every ref on `origin` (147 heads), `origin/staging` @ `69a7f87`, `origin/main` | **ABSENT** on all of them; `docs/gates/` on staging holds only `GATE_REGISTER.md` and `phase-0-kickoff.md` | 08:43:52Z (fetch) |
| `origin/staging` head | `git log -20` | `69a7f87` — unchanged since Register rev 4; no D1 commit for v3 | 08:43:52Z |

Per the order — "BLOCKED UNTIL get_top_contributors_v3 EXISTS ON STAGING … If it is absent, stop and report" — and skill rule 7 ("an interface agreed in conversation does not exist — the file exists or the unit has not started"): **stopped.**

## What the client does today (read-only, at `69a7f87`)
- `src/hooks/useTopContributors.ts:61` — `supabase.rpc('get_top_contributors_v2' as any)`; `:77` maps `contributor_score`.
- `src/components/sidebar/SidebarTopContributors.tsx:69` — renders `✦ {c.contributor_score.toLocaleString()}`.
- `src/components/ContributorScore.tsx` + `src/lib/contributorScore.ts:39` — `get_contributor_scores` (lifetime) for every other surface.
- The v2 hook header says it in its own words: v2 "ranks by … a rolling 30 UTC days, and returns the **lifetime** Contributor Score for display" — the ranking field and the displayed field are different numbers by design. That is the regression this unit exists to fix, and it is exactly why the card must read `recent_score` from v3, not a client-side sort.
- `src/integrations/supabase/types.ts` knows only `get_top_contributors_v1` (line 6844) — neither v2 nor v3 — so both are called `as any`. If v3 lands with a types regeneration, that file changes; noted per the order.
- Existing tests: `src/__tests__/topContributorsV2.test.ts` (labels "Last 30 Days", translation) — the fail-first test for this unit will sit beside it.

## Current staging data, v2 (for the descending-order check later)
`get_top_contributors_v2()` at 08:44Z: rank 1 → 764, rank 2 → 509, rank 3 → 509. Non-increasing today, but these are lifetime numbers ordered by a 30-day rank — agreement is coincidental, not guaranteed. The check the order requires is against `recent_score` from v3 and cannot be run until v3 exists.

## Ready to start the moment both exist
Branch `d2/TC-v3-home-recent-score-20260903` off `staging`; fail-first test against the current component first; hook + card only for change 1; hover-only for change 2; no touch of `Index.tsx`, `supabase/**`, `docs/gates/**`, ledger, `package.json`.

Push authority on this side: none (git proxy refuses; unchanged).
