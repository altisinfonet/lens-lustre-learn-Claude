# D2 WATCH + 5cd5ba6 build verification — 2026-09-03T09:47–09:52Z

## 1. Push authority — still refused
`git push --dry-run origin d2/P0-client-inventory-20260903` at **09:47:09Z**:
`remote: access denied by the git proxy: … not in this session's authorized repository set` → `403`. Third consecutive refusal today. No branches pushed, no PRs opened from this side.

## 2. WATCH — d2-web-vitals.yml: NO RUN, and it cannot run yet
| Check | Result | UTC |
|---|---|---|
| Runs of `d2-web-vitals.yml` | **1 — still only `33621354419`** (PR #126 synchronize, `85fc2ea`, 2026-09-02 10:49Z, pre-guard). **No run on `staging`, no run since.** | 09:47Z |
| Measured-sample count | **N/A — the F-2 guard step has never executed in CI.** `85fc2ea` predates the guard. | — |
| `.github/workflows/d2-web-vitals.yml` on `origin/staging` | **ABSENT.** Staging carries 8 workflows: android-build, apply-migration, health, security, typecheck, ui-gate, verify-schema-dependencies, web-build. | 09:47Z |
| PR #134 | **CLOSED**, base still `d2/P0-web-baseline-20260903`, **Checks (0)**. It did not retarget onto `staging`. | 09:48Z |
| `d2/P0-web-vitals-20260903` (`3435636`) in `staging`? | **NOT an ancestor** of `5cd5ba6`. The vitals harness and its workflow are not on staging. | 09:48Z |

**Finding (routing to the Auditor):** the expectation "#134 rebases onto staging automatically when #133 goes" did not hold — GitHub closed #134 when its base branch was merged, rather than retargeting. Consequence: **0.5 (the Web-Vitals harness with the F-2 guard) has not landed and its guard has never run in CI.** Both branches still exist on origin (`d2/P0-web-baseline-20260903` = `d44c485`, `d2/P0-web-vitals-20260903` = `3435636`), so the fix is to reopen #134 with base `staging` (or open a fresh PR from the same head). Not D2's action — no push authority.

## 3. staging @ `5cd5ba6` — CI
| Workflow | Run ID | Result |
|---|---|---|
| **UI gate #235** | **33740183131** | **Success**, 7m 21s, artifact `ui-sweep-screenshots` 40.1 MB (`sha256:f6d933ad…0b8cf9`) |
| Web build #395 | **33740183068** | Success, 57s — "Staging lane build" 54s; **no artifact uploaded**, so CI's own `dist/` is not retrievable |
| Typecheck #1315 | — | Success, 53s |
| Security #803 | — | Success, 34s |
Both green runs carry the same annotation: *Node.js 20 is deprecated … forced to run on Node.js 24* — the warning **F-58 fixes** (`ui-gate.yml` and `web-build.yml` still pin `node-version: 20` as well; `ui-gate.yml`/`web-build.yml` are mine and were not in F-58's scope — reporting, not widening the unit).

## 4. The bundle string — two independent readings, both with a control
Because CI uploads no `dist` artifact, the built bundle was read two ways.

**A. The deployed staging lane (the running system).** Fetched from `https://staging.50mmretina.com` in the browser, 09:52Z:
| Chunk | `AUTHORIZED SIGNATORY` | `AUTHORIZED SIGNATURE` |
|---|---|---|
| `assets/generateCertificatePdf-vHaTxtSw.js` (10,679 B) | **1** | **0** |
| `assets/AdminCertificates-qwduptzQ.js` (30,916 B) | 1 (as `Authorized Signatory`) | **0** |
Case-insensitive: signatory 2, signature **0**.

**B. Local staging-lane build of `5cd5ba6`** (`npm run build` with the public staging values from `web-build.yml`, exit 0, 09:48:34Z):
`grep -roa "AUTHORIZED SIGNATORY" dist` → **1** (`dist/assets/generateCertificatePdf-DL0Y1Br3.js`, 10,679 B — same byte length as the deployed chunk)
`grep -roa "AUTHORIZED SIGNATURE" dist` → **0**
case-insensitive: signatory **2** (adds `AdminCertificates-Dz1rR4Co.js`), signature **0**.

**The control (C-34) — the same grep on the same build of the pre-fix commit `ca8e989`:**
`AUTHORIZED SIGNATORY` → **0** · `AUTHORIZED SIGNATURE` → **1** (`dist/assets/generateCertificatePdf-C5h7-IaU.js`) · case-insensitive signature **2**, signatory **0**.
The check can fail, and does, on the unfixed input.

**Classification:** deployed reading **VERIFIED (running system)**; local build **VERIFIED (reproduction — not CI's own artefact, which does not exist)**. Real-device rendering of the PDF remains the Owner's OWNER-ATTESTED leg; not mixed in here.

## 5. TC-v3 client half
Untouched. D3's unit.
