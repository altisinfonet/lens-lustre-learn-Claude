# D2 WATCH + ad735fd build verification — 2026-09-03T10:39–10:42Z

## 0. Courier limitation recorded
0-D2-03 landed as **#136 / `bd51071`**; the file blob is **`0e0ca8b`**, byte-identical to my `d2/P0-client-inventory-20260903` (`35f23cb`) — verified locally against PR #136's head, 10:39Z. Note for the record: `bd51071` is **not yet in `origin/staging`** (head is `ad735fd`), so #136 is open, not merged, at this timestamp.

**F-58 and F-60 — courier limitation, not rejection.** The Auditor's sandbox will not upload `.github/workflows/**` or `src/__tests__/**`; both units stay **VERIFIED-pending-landing**. D2 has no push authority (three refusals today), so neither side can place those two files on origin by itself. The patches remain byte-stable and re-verified as applying on each new head: `26bbb92a…490316` (F-58) and `822c2ec2…303ec90` (F-60). Consequence to keep visible: **`typecheck.yml` still pins `node-version: 20` on staging**, and the stale assertion in `typecheckIsNotVacuous.test.ts` is still red there.

## 1. WATCH — #134 / d2-web-vitals.yml: NO RUN
| Check | Result | UTC |
|---|---|---|
| Runs of `d2-web-vitals.yml` (all branches) | **1 — still only `33621354419`** (PR #126 synchronize at `85fc2ea`, 2026-09-02 10:49Z, pre-guard). **No run against `ad735fd`. No re-run.** | 10:40Z |
| Measured-sample count | **N/A — the F-2 guard step has still never executed in CI.** | — |
| PR #134 | **Still CLOSED**, base still `d2/P0-web-baseline-20260903`, **Checks (0)** — it was not reopened or retargeted. | 10:40Z |
| `.github/workflows/d2-web-vitals.yml` on `origin/staging` @ `ad735fd` | **ABSENT** | 10:39Z |
| `d2/P0-web-vitals-20260903` (`3435636`) in staging? | **NOT an ancestor** | 10:39Z |

Unchanged from the 09:50Z report: a re-run cannot happen while #134 is closed and the workflow file is absent from staging. Both branches still exist on origin (`d44c485`, `3435636`); reopening #134 with base `staging`, or cutting a fresh PR from `3435636`, is what produces the first guarded run. Not D2's action.

## 2. staging @ `ad735fd` — CI
| Workflow | Run ID | Result |
|---|---|---|
| **Web build #397** | **33742905485** | **Success**, 56s (Staging lane build). No dist artifact uploaded. |
| UI gate #237 | 33742905520 | Success, 7m 48s |
| Typecheck #1317 | 33742905463 | Success, 1m 9s |
| Security #805 | 33742905459 | Success, 32s |

## 3. The served chunks — both counts as asked
Fetched from `https://staging.50mmretina.com` in the browser, **10:41Z**:

| Served chunk | Bytes | `Preview All Types` | `AUTHORIZED SIGNATORY` |
|---|---|---|---|
| `assets/Certificates-B8p-dYN_.js` | 19,699 | **0** ✔ expected 0 | 0 (n/a — not this chunk's string) |
| `assets/generateCertificatePdf-BHWOsheA.js` | 10,679 | 0 | **1** ✔ expected 1 |
`AUTHORIZED SIGNATURE` in both: **0**. Case-insensitive `authorized signatory` in the Certificates chunk: 0 (the label lives in the PDF and admin chunks, not here).

**Control (C-34) — the same greps on the build of the previous head `5cd5ba6`:** `dist/assets/Certificates-1po7HVlW.js` (20,121 B) → `Preview All Types` = **1**. The grep can find the string, and does, on the pre-OWNER-02 input; it returns 0 only because the button is gone.

**Local rebuild of `ad735fd`** (same public staging-lane values, `npm run build` exit 0, 10:42:30Z), as a second reading: `Certificates-Nd1Eqq-N.js` 19,727 B → `Preview All Types` **0**; `generateCertificatePdf-BqfnvM2w.js` **10,679 B** (byte-for-byte the served size) → `AUTHORIZED SIGNATORY` **1**, `AUTHORIZED SIGNATURE` **0**. Whole-dist totals: 0 / 1 / 0. The 28-byte difference on the Certificates chunk is the hashed filename inside the source-map comment, not content.

**Classification:** served readings **VERIFIED (running system)**; local rebuild **VERIFIED (reproduction)** — CI uploads no dist artifact, so its own `dist/` cannot be read.

## 4. TC-v3 client half
Untouched. D3's unit.
