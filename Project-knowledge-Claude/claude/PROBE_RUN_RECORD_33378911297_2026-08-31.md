# runbook §5.3 SECRET-ISOLATION PROBE — re-taken at the new candidate

Issued 2026-08-31 by the compiler/audit session. **Executed by the owner; verified by me from the remote and the provider.**

---

## 1. Identity

| | |
|---|---|
| Branch | `scratch/g10-53-secret-isolation-20260831` |
| Head | **`9c556b8ebe4f480b85b4b415a48cd95b1d846180`** |
| Parent | **`5ca0d256a994fcab9e5beecfae8b8513d2799446`** — the current candidate ✔ |
| Workflow run | **`33378911297`** — *"G10 secret isolation probe #2"*, `Commit 9c556b8 pushed by altisinfonet` |
| Status | **Success**, total 8s; job `probe` succeeded in 4s |
| Steps | **Set up job ✔ · Probe ✔ (1s) · Complete job ✔** — three of three green |

## 2. Pre-flight checks, taken BEFORE the run and BEFORE any damage could occur

**The owner's commit dialog was set to "Commit directly to the `staging` branch."** That was caught from his screenshot and stopped before the click. Had it gone through it would have (a) moved the release candidate, and (b) made the probe meaningless — on a lane branch the secret is *supposed* to resolve, so the result would have read backwards.

| Check | Result |
|---|---|
| `staging` unmoved through the whole operation | **`5ca0d256…9446` at 09:36:28Z and after** ✔ |
| `main` unmoved | **`b671e1fb…d674`** ✔ |
| Tags | **0** ✔ |
| Branch parented on the candidate, not on something stale | **parent = `5ca0d256`** ✔ |
| What the branch adds | **exactly one file, `+43`, `A` only. 0 M, 0 D** ✔ |
| **File integrity** — sha256 on GitHub vs what was sent | **`4da0826127e32f950cdf79750dc3cb1a054c585ebb053f573bff679f65ca42a0` both sides — BYTE-IDENTICAL** ✔ |
| Same file as the 2026-08-26 design that passed | **byte-identical** ✔ |
| Branch name matches the trigger `scratch/g10-53-secret-isolation-*` | **matches — the run fired** ✔ |
| No `environment:` key present *(its absence IS the test)* | confirmed in the committed blob ✔ |

## 3. The verdict, and exactly what it rests on

**Design closure, verified against the blob that actually ran** — not against a copy, not against a report:

| Property of the committed workflow | Consequence |
|---|---|
| exactly one step | nothing else can produce the conclusion |
| no `if:`, no `continue-on-error` | the step cannot be skipped, its failure cannot be swallowed |
| `EMPTY` → falls through → **exit 0** | the only success path |
| `NON-EMPTY` → **`exit 1`** | would have failed the run |
| no `environment:` key | the secret resolves only if a repository-level copy exists or a policy is wrong |
| trigger excludes `main` and `staging` | cannot pass for the wrong reason |

**The run's conclusion is `success` and the `Probe` step is green — so it ran, and it exited 0. The only path to exit 0 is `EMPTY`.**

**Result: `SUPABASE_DB_URL` did not resolve on a non-lane branch. G3's exit condition is met — and §4 below confirms it by observation, not only by inference.**

**This is stronger than the 2026-08-26 evidence** on two counts: the bytes on the branch that triggered the run were hashed and matched *before* the run was read, and — unlike that run — **the literal log line has been read** (§4).

## 4. THE LITERAL LINE — READ, at 2026-08-31

**The observation the ledger's standard requires has been taken.** Reached by deep-linking to the step anchor (`#step:2:1`), which forces the step open — after clicking the step, the in-page log search and the raw-logs URL had all failed.

The `Probe` step log, read verbatim from the provider:

```
Probe                                    ✓   1s
  1    ▸ Run set -euo pipefail
  12   EMPTY
```

| | |
|---|---|
| Literal output | **`EMPTY`** — step log line 12 |
| `NON-EMPTY` present anywhere in the log | **NO** |
| Step conclusion | **passed**, 1s |
| Screenshot captured | yes — retained with this record |

**`SUPABASE_DB_URL` resolved EMPTY on a non-lane branch.**

**Class: VERIFIED.** Design closure, provider conclusion, step-level pass **and the literal line**, all four. §14 HS-11 is answered in full: this is not a `success` conclusion standing in for an observation — the observation was made.

**§24.1 step 6 — SATISFIED. G3's exit condition is met.**

**Nothing secret-derived appears in the log.** The workflow prints one literal word and never the value, its length, a hash or a prefix. The screenshot contains no credential.

## 5. Cleanup — DONE, and proved rather than asserted

`git push origin --delete` equivalent performed 2026-08-31 via the Branches page. **Verified from the remote afterwards, not assumed:**

| Check the card requires | Result |
|---|---|
| `git ls-remote --heads origin \| grep secret-isolation` | **0** ✔ |
| Total remote branches, vs the pre-probe baseline of 119 | **119 — exactly restored** ✔ |
| `main` unchanged | **`b671e1fb0c5bcf145d442076c229eca888afd674`** ✔ |
| `staging` unchanged | **`5ca0d256a994fcab9e5beecfae8b8513d2799446`** ✔ |
| Tags | **0** ✔ |

**The branch-name and ref state is fully restored.**

**RESIDUE — what CANNOT be removed, stated plainly rather than claimed away:** workflow run **`33378911297`**, its logs and its job records persist permanently in the repository's Actions history. That is expected and is the evidence for this row.

**NO deployment record was created.** This design binds no `environment:`, which is precisely why it was chosen over the revision-6 variant, whose `environment:`-bound jobs would have written deployment records that cannot be deleted and could have flipped a prior deployment to `inactive`.

## 6. Standing

`main` = `b671e1fb0c5bcf145d442076c229eca888afd674` · candidate = `5ca0d256a994fcab9e5beecfae8b8513d2799446` · **tags = 0**.

Nothing merged, tagged, deployed or migrated. **§24.1 step 6 is satisfied to the level stated in §4 above and no further.**
