# BLOCKED.md — Items WO-1 Could Not Complete This Session

Per WO-1: "declare what cannot be done... do not substitute a partial method and report it as the item." Every entry below states the reason and who/what can close it. No blocked item was worked around with a lesser substitute presented as the real thing.

---

### 1. WS4 harness pack — source files absent from this environment
**What's blocked:** Running `07_ws4_reference_impl.py` (the MATCH/HEADER-ONLY/DRIFT/UNKNOWN classifier), `08_selftest.sh`, `09_rc_regression.sh`, `10_verify_pack.sh`, `11_mutation_control.sh`, `12_rc_root_mode_test.sh`, `13_make_rc_fixtures.sh`, `15_fixture_gate_test.sh`, `extract_probe_yaml.sh`. Also: producing the literal, original `06_RESULTS_TEMPLATE.md` file (a reconstruction was produced instead and is explicitly labeled as such in that file).
**Why:** Searched all 378 project docs via `project_info` and targeted `project_search` queries. The project docs (`WS4_HARNESS_REVISION7_2026-08-29.md`, `WS4_HARNESS_REVISION8_2026-08-29.md`, and their transcripts) contain SHA256 hashes and prose descriptions of these files, never their content. No local repo checkout or device connection exists in this session to source them from either. A prior session apparently built this pack inside its own container (a `/home/claude/ROUND5_PACK/` path is referenced in transcripts), which no longer exists — containers are ephemeral and that session's filesystem was destroyed.
**Who can close it:** Whoever holds the original pack files (the session/person that built REVISION7/8, or wherever they were archived outside this project's doc store) needs to supply them — either as a project doc upload or attached to a future session. Absent that, a future session with GitHub access could, in principle, reconstruct an equivalent classifier from repo source, but that would be a NEW instrument requiring its own validation (self-test, mutation control) before being trusted — not a substitute presented as the original.
**Downstream effect:** T1.5 (classification), T1.6 (per-DRIFT-case review), and the harness self-verification requirement (10_verify_pack.sh PASS=30/FAIL=0, 08_selftest.sh 56/56, 11_mutation_control.sh UNDETECTED=0) are all BLOCKED as a consequence. No WS4-derived MATCH/DRIFT figure for the full 71-function population can be produced or relied upon this session.

### 2. 138-file review
**What's blocked:** The independent 138-file review named in §25.7.2.
**Why:** Requires reading repository file contents. GitHub access is disabled for this session (confirmed by direct `curl` attempt against the GitHub API, which returned a structured refusal: "GitHub access to this repository is not enabled for this session. Use add_repo to request access." — no `add_repo`-equivalent tool exists in this session's tool set, confirmed via `ToolSearch`).
**Who can close it:** Whoever administers this session's repo access grants (add_repo or equivalent) for `altisinfonet/lens-lustre-learn-Claude`.

### 3. Independent test-suite run
**What's blocked:** Running the project's own test suite independently, as required by §25.7.2.
**Why:** Same as #2 — no GitHub access, and no local checkout exists in this cloud environment to run tests against.
**Who can close it:** Same as #2.

### 4. Full 71-function MATCH/DRIFT/UNKNOWN classification (T1.5/T1.6)
**What's blocked:** Classifying all 71 production functions against the repo baseline.
**Why:** Two independent blockers compound here: (a) no WS4 classifier (item #1), (b) no repo checkout to compare against even with a substitute classifier (items #2/#3's GitHub-access blocker applies equally here).
**Who can close it:** Both #1 and (#2's access grant) need to be resolved.
**What WAS done instead (not a substitute for the classification, but real measurement in its own right):** 7 of 71 production functions had full source independently fetched, hashed, and read directly: `submit-judge-decision`, `send-gift-credit`, `detect-ai-image`, `analyze-gallery-image`, `backfill-image-dims`, `measure-post-media`, `migrate-post-media`. Findings from direct source reading are reported in `06_RESULTS_TEMPLATE.md` Row 3, `T1_HASH_CROSS_VALIDATION.md`, and `T1.8_IMAGEDIMS_COMPARISON.md` — these are measurements of what production currently contains, explicitly not a MATCH/DRIFT verdict against repo.

### 5. §25.3 Row 4 — Cloudflare R2 API token policy/bucket scope
**Why:** No API-token listing/introspection tool exists in the available Cloudflare Developer Platform MCP tool set (checked: `d1_database_*`, `hyperdrive_config_*`, `kv_namespace_*`, `r2_bucket_create/delete/get`, `r2_buckets_list`, `search_cloudflare_documentation`, `workers_get_worker`, `workers_get_worker_code`, `workers_list`, `migrate_pages_to_workers_guide`).
**Who can close it:** Someone with direct Cloudflare dashboard or API access (outside this MCP tool set) to enumerate token scopes, or a future session with a token-management MCP tool connected.

### 6. §25.3 Row 6b — R2 `isolation-probe/` prefix search
**Why:** No object-level listing (ListObjectsV2-equivalent) or key-prefix-search tool exists in the available Cloudflare tool set. `r2_bucket_get`/`r2_buckets_list` operate at the bucket level only.
**Who can close it:** A session with an R2 object-listing tool (or direct Cloudflare dashboard/API access) connected.

### 7. §25.3 Row 6c — Cloudflare Zero Trust posture
**Why:** No Zero Trust/Access-policy tool exists in the available Cloudflare tool set.
**Who can close it:** Same as #6.

### 8. `ezbr_sha256` exact preimage
**Why:** Attempted 7 candidate reconstructions from raw `get_edge_function` source text (entrypoint alone, shared-file alone, concatenations in both orders with/without newline, JSON serialization compact/indented) for 4 sampled functions — none matched. Most likely explanation (INFERRED, not verified): the hash covers Supabase's post-bundle deployment artifact, not the pre-bundle source exposed by the API. No bundler/build-pipeline reproduction tool was available to test this hypothesis.
**Who can close it:** Supabase (documentation or support) confirming what `ezbr_sha256` is computed over, or a session with tooling to reproduce their edge-function bundling step.
**Not a hard blocker for T1.3 overall:** `ezbr_sha256` remains usable as a change-detection identity (see T1_HASH_CROSS_VALIDATION.md) — this entry blocks only the stronger claim of "independently reproduced from source."

### 9. §5.3 probe
**Not attempted, per explicit prohibition in WO-1**: "do not run the §5.3 probe — it is owner-only and runs last." Not a capability gap — a deliberate scope exclusion.

### 10. N1
**Not attempted, per explicit prohibition in WO-1**: "do not run N1 as written." Not a capability gap — a deliberate scope exclusion.

---

## Summary count
- Capability/tool-access blockers: items 1–7 (7 items)
- Partial/inferred-only blocker: item 8 (1 item)
- Deliberate scope exclusions (not blockers in the capability sense): items 9–10 (2 items)
