# G10 STEP 4.3 — PR #103 OPENED, CI GREEN, EVIDENCE CLASSIFIED

**2026-08-26. `main` NOT merged, still `b671e1f`. Candidate T unchanged at `e2e05fb`.**

---

## PR #103 — independently verified, not accepted on report

| Item | Value |
|---|---|
| PR | **#103** — "G10 4.3: arm production-lane isolation host rules" · **Open** |
| Base ← head | `main` ← `g10/4.3-arm-production-isolation` |
| Head commit | **`704ad5762f542190db61bddcf47ddeb759a613fb`** |
| Diff | **+476 / −18**, exactly **3 files** |
| `main` at verification | **`b671e1f`** — untouched |

### The strongest check: two independent constructions converged

This session prepared the change locally and never pushed it. Claude Code prepared it separately on
the owner's machine and pushed. The resulting trees were compared:

```
locally prepared tree : 54b447751308b7275c485570b8364105bdf817a2
pushed branch tree    : 54b447751308b7275c485570b8364105bdf817a2
IDENTICAL
```

**Two independent constructions producing the same tree is stronger evidence than either alone.**

### Byte-level file verification

| File | Branch blob | T blob | |
|---|---|---|---|
| `scripts/verify-bundle-isolation.mjs` | `f53f79f` | `f53f79f` | **MATCH** |
| `scripts/test-isolation-guard.mjs` | `cbae234` | `cbae234` | **MATCH** |
| `.github/workflows/web-build.yml` | diff = the reviewed diff, 1 line changed + 2 added | | **MATCH** |

No truncation, no corruption, no extra files.

---

## CI on PR #103

| Workflow | Result |
|---|---|
| **Web build / build** (run **`32982588154`**, job **`98222620258`**) | **Success, 43s** |
| Typecheck | Success, 1m |
| Security / Secret scan (full history) | Success, 6s |
| Security / Dependency vulnerabilities | Success |
| Security / This project's own security rules | Success |
| UI gate | in progress at time of writing |

Every step in the Web build job is green, including **`Bundle isolation guard (production lane)`**.

---

## Evidence classification — stated precisely, not rounded up

### ✅ VERIFIED by this session, read from the CI log

| Fact | Evidence |
|---|---|
| **Mutation harness on CI: `Mutations detected/held: 21/21`** | Read verbatim from the job log. **This closes §17-5's CI requirement**, which was previously open — the count had only been measured locally |
| `ISOLATION_FORBIDDEN_REFS: ztzutckwdhetphwghuzj` present in the job's env echo | Read verbatim — **proves the workflow change took effect on the runner** |
| `VITE_SUPABASE_URL: https://jtdtehuqtinjxropkkcn.supabase.co` | Read verbatim |
| Both guard steps invoked | `##[group]Run node scripts/test-isolation-guard.mjs` and `##[group]Run node scripts/verify-bundle-isolation.mjs` read verbatim |
| **The guard exited 0** | The step is *only* `node scripts/verify-bundle-isolation.mjs`; the guard `process.exit(1)`s on any failure. A green step therefore entails exit 0 |

### ⚠ NOT read by this session — the literal `host=` / `forbidden-hosts=` text

The raw job log redirects to a storage URL carrying a signed token in its query string. **This
environment's data-loss-prevention filter blocked every attempt to read the specific line range**,
classifying it as credential-like.

A subagent tasked with extracting it hit the same block, attempted several narrower reads, then
**correctly stopped**, reasoning that repeatedly slicing content into smaller pieces to defeat a
security filter is circumvention. **That judgment is endorsed and the attempt was not resumed.**

So the literal CI text of that one line is **corroborated three ways but not directly read here**:

1. The identical guard, identical env and identical bundle content were run locally against **`main`'s
   own build**, producing: `host=cdn.50mmretina.com present; forbidden-hosts=[cdn-staging.50mmretina.com,staging.50mmretina.com] absent`
2. The env echo in the CI log confirms the armed variables reached the runner
3. Claude Code, running on the owner's machine, read the log and reports the line is as required

**Recorded as OWNER-ATTESTED for the literal string, VERIFIED for the exit status.** It is not upgraded
to VERIFIED on the strength of corroboration.

**To close it properly:** `gh run view 32982588154 --log` on a machine with `gh`, reading the line
locally rather than through the browser.

---

## A flag raised and resolved

The subagent noted that fixtures RED-1 and RED-6 use `jtdtehuqtinjxropkkcn` — the production ref — as
the ref that *should* trigger a leak failure, and asked whether the job ran against the right lane.

**Benign, and the observation was a good one.** Those lines are the *self-test* harness
(`test-isolation-guard.mjs`), which builds synthetic fixture bundles in `/tmp/dist-*` and asserts the
guard rejects them. A fixture staging bundle containing the production ref *must* fail — that is the
test passing. They are unrelated to the production-lane run against the real `dist`. The `/tmp/dist-…`
paths in those lines distinguish them.

---

## Step 4.3 status

**Implementation complete and merged-ready. NOT merged — `main` remains `b671e1f` as instructed.**

| §17 line | Before | Now |
|---|---|---|
| §17-5 mutants held, from a run on this tree | measured locally only | **VERIFIED from CI — 21/21** |
| §17-4 both lanes armed | not satisfied | production lane now armed and green on PR #103; **still needs the on-T production-lane run** (see below) |

**§17-4 is not yet closed.** This run proves the armed guard passes on `main`'s content. §17-4 asks for
both lanes **on T**. The staging lane on T is run `32976271438`. The production-lane-on-T evidence must
come from a run whose content is T — most likely the promotion PR itself, since T's `build-production`
job is gated on `base_ref == 'main'`.

*AF-01 remains recorded and unmodified. Nothing merged, nothing on `main` changed.*
