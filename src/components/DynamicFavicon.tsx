/**
 * Favicon is intentionally static for SEO and crawler consistency.
 * Keep this component mounted as a no-op to avoid changing imports elsewhere.
 */
const DynamicFavicon = () => null;

export default DynamicFavicon;
