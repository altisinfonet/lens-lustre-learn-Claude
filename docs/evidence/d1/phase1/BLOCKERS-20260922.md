# D1 · §6 — BLOCKERS, 2026-09-22

Five. Each is recorded in the work order's §6 format. **Every other item in §4 was completed**
— a blocker on one object never stops the other 75.

---

```
BLOCKER-1
object:            the production lane itself — jtdtehuqtinjxropkkcn, and every
                   Set A object on it (claim_username(text),
                   change_custom_url(text), clear_custom_url())
action blocked:    re-deriving Set A's production ACL, running
                   PROBE_p32_setA_closed.sql on production, and confirming
                   from the production catalogue that nothing depends on the
                   public-qualified plpgsql_check names (§4.10)
why not technical: this session's Supabase connector lists exactly one project.
                   `list_projects` returns fpszggreishhuvdpkmdr (staging) and
                   nothing else, verified 2026-09-22T05:26Z. No credential,
                   connection string or token can be handled by a developer
                   session (skill §10: "never handle a connection string,
                   secret or token"), so this is not a thing D1 can work
                   around — and the D2 session of 2026-09-21 recorded the same
                   limitation independently.
decision required: attach jtdtehuqtinjxropkkcn to a session's Supabase
                   connector, or nominate who runs the two probes on
                   production. The Owner's, since the Owner holds every secret.
```

Consequence, stated rather than buried: **every production statement in this session's artefacts
is RELAYED from the Auditor's two-lane diff, not measured.** They are labelled as such at each
point of use. Set A's fail-first control (§4.5) is half satisfied — the staging half is measured,
the production half is not.

---

```
BLOCKER-2
object:            the production plpgsql_check SET SCHEMA migration prepared
                   at docs/evidence/d1/phase1/prepared/
                   P33-production-plpgsql-check-set-schema.sql.prepared
action blocked:    giving it a migration ordinal and a filename under
                   supabase/migrations/
why not technical: work order §3 and §5 — "Take an ordinal only from the range
                   the Auditor publishes in docs/gates/phase-1-kickoff.md.
                   Never self-select." That file publishes 0031-0039 and
                   0042-0099 as "free" but §2.2 of the same file requires "a
                   disjoint ordinal range PER UNIT ... before any SQL is
                   written", and no range has been allocated to
                   P33-production. Taking 0031 because it is free is exactly
                   the "first free slot" reasoning that produced the 0019
                   duplicate and the 0024 cross-lane collision; 0027's own
                   header records that reasoning in its own words.
decision required: the Auditor allocates an ordinal, or a range, to
                   P33-production.
```

The file is complete and carries its rollback. Promotion is a rename.

---

```
BLOCKER-3
object:            ordinal 20260910_0002, and
                   recompute_entry_from_tag_assignments(uuid)
action blocked:    nothing in this unit — this is a correction owed to
                   docs/gates/phase-1-kickoff.md §2, raised as a finding
why not technical: the kickoff's ordinal table records "0001-0018, 0020-0022 |
                   present on staging | closed". 0002 is NOT present, on
                   staging or anywhere: `git log --all --diff-filter=A --
                   'supabase/migrations/20260910_0002_*'` returns nothing at
                   2026-09-22T05:28Z. The file has never existed on any branch.
                   PR #274's 0027 header names it independently — "_0002 (owed
                   a re-cut, recompute_entry_from_tag_assignments)" — so a
                   second source agrees it is owed and missing.
                   The object is worse than missing: its live body on staging
                   is `BEGIN RETURN; END;`, a complete no-op, still SECURITY
                   DEFINER, still anon-executable, still called by the trigger
                   function trg_recompute_entry_after_tag_change.
decision required: the Auditor corrects the ordinal table (0002 is a THIRD
                   anomaly alongside 0019 and 0023, not a closed row), and
                   rules on the object: restore it or remove it honestly.
                   "A feature that has never worked is a decision, not a bug."
```

---

```
BLOCKER-4
object:            get_broadcast_feed(uuid[], integer, integer, text[]) — and
                   through it the two wrapper overloads
action blocked:    a disposition for all three signatures
why not technical: two separate reasons, and only the first is the Owner's.
                   (a) Owner item 2: is a signed-out feed a planned surface?
                       No current caller needs anon — useFeedQuery.ts:91 is
                       behind Feed.tsx:89's /login redirect — but the 4-arg
                       body carries an explicit
                       coalesce(auth.uid()::text,'anon') branch, so an
                       anonymous viewer is a designed-for case in the code.
                       Intent and current behaviour disagree.
                   (b) TECHNICAL, AND MINE, AND NOT FINISHED: whether the
                       body's privacy predicate is complete when auth.uid() is
                       NULL. The body is 4 668 characters; §4.3's reading
                       established the shape and the wrapper relationship, and
                       did NOT establish that the privacy filter holds for an
                       anonymous viewer. Saying so is cheaper than guessing.
decision required: (a) from the Owner. (b) from D1, in a dedicated reading of
                   that one body with a cross-member test — which is what the
                   skill requires of any definer function standing in for RLS,
                   and which this unit did not have the scope to do.
```

The ordering constraint is settled even though the disposition is not: the two wrappers are
`SECURITY DEFINER` and call the 4-arg form, so the inner call never consults the caller's grant.
**All three close together or none do.**

---

```
BLOCKER-5
object:            judging_write_decision_atomic(uuid, text, text)
action blocked:    placing it in any prospective remediation group
why not technical: it is a NEW finding and it is not on the frozen list at
                   all. P1-revocation-list.md Revision 2 clears one object and
                   defers two named groups; this function is in neither, so
                   there is no instrument that says what to do with it.
                   The body, read 2026-09-22T05:34Z, has NO identity check of
                   any kind — auth.uid() appears once, written INTO the audit
                   row as data, never compared to anything. Any holder of the
                   public anon key can append forged rows to
                   public.db_audit_logs labelled
                   'judging_write_decision_atomic' with changed_by = NULL, and
                   can distinguish 22023 / P0002 / success as an existence
                   oracle over competition_entries and v3_stage_catalog.
                   A grant closure alone leaves the body defect in place
                   behind the service_role door, and the audit trail of a
                   judging system is not a thing to close quietly.
decision required: the Auditor rules whether this is (i) folded into PR #276's
                   judging-lock body-fix unit, which is the natural home and
                   the same class of defect, or (ii) its own unit, or (iii)
                   grant-closed now and body-fixed later with the gap recorded.
                   D1 has a preference — (i) — and it is not D1's call.
```

---

## Not blockers, and deliberately not raised as such

- **No push access.** Real, and handled rather than escalated: two branches are cut, committed
  and bundled for the Owner (§4.12, `PUSH-HANDOFF.md`). The project has done this before —
  `claude/2026-09-17-P1-reconciliation-push-access-blocked-bundle-handoff.md`.
- **`docs/gates/phase-1-kickoff.md` has not landed on staging.** It exists on
  `origin/auditor/phase-1-kickoff-20260922` @ `3a0d041` and was read from there. The work order
  anticipated this ("if it has landed"). Recorded, not blocking.
- **Four merged-but-unapplied revokes.** Dispatch is owed, not authorship. The catalogue says so
  and the probes are written to fail against exactly that state.
