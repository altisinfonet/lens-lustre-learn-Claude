# TC-v3 client half · BEFORE measurement · Option B obligations

**Unit** OWNER-RULING-2026-09-03-02, client half · **Interface** `docs/gates/TC-v3-interface.md` §3.1
**Repository** `origin/staging` `c268293` (Register Rev 5) · **Branch** `d3/TC-v3-home-both-scores-20260903`
**`src/**` NOT TOUCHED. No fail-first test written — that reasoning stands and is recorded in Rev 5.**

Every line carries the UTC instant it was measured. Three obligations: **one is measured, two are not,
and this file says which is which rather than blurring them.**

---

## 0 · Precondition re-checked before anything else

`pg_proc` on staging `ztzutckwdhetphwghuzj`, Supabase MCP `execute_sql`, SELECT only, pattern
`get_top_contributors%`:

| measured (UTC) | functions present |
|---|---|
| **2026-09-03 09:17:28.555048** | `get_top_contributors_v1`, `get_top_contributors_v2` — **no v3** |

**`get_top_contributors_v3` is still absent.** Third consecutive reading (09:04:22.242475,
09:04:38.383619 production, 09:17:28.555048). D2 remains stopped on the client change.

---

## 1 · OBLIGATION 1 — longest name · **MEASURED** · and it produces a finding

### 1.1 Current top three on staging

`get_top_contributors_v2()` joined to `profiles`, measured **2026-09-03 09:17:54.86831 UTC**:

| rank | name | chars | lifetime |
|---|---|---|---|
| 1 | Yuki Tanabe | 11 | 764 |
| 2 | Tomas Vogel | 11 | 509 |
| 3 | Ari Sen | **7** | 509 |

### 1.2 The pool that *can* reach the top three — staging

`contributor_points_since(today-29)` where `score > 0`, joined to `profiles`, longest first.
Measured **2026-09-03 09:18:06.955086 UTC**:

| name | chars | 30-day score |
|---|---|---|
| **Sofia Duarte** | **12** | 331 |
| Lena Petrov · Ruben Alves · Tomas Vogel · Yuki Tanabe | 11 | 255 · 509 · 509 · 764 |
| Maya Iyer · Nadia Haq | 9 | 509 · 509 |
| Ari Sen | 7 | 509 |

Longest name in **all** staging profiles, measured **09:18:10.782482 UTC**: `50mm Retina World` (17,
the brand account), then `Nikolai Vasquez` (15, several duplicate rows).

### 1.3 The same pool on production — where the risk actually lives

Measured **2026-09-03 09:18:25.228393 UTC**:

| name | chars | 30-day score |
|---|---|---|
| **Partha Sarathi Moulik** | **21** | 1,706 |
| Shaikh Jan Mohammad | 19 | 2,267 |
| Pradipta Chowdhury · Sanjay Kumar Datta · Saptarshi Sengupta · Sri V R Narayanasw · Yeman Kalyan Modak | 18 | 331 · 1,528 · 1,477 · 5,284 · 255 |
| Tilak Chakrobarty | 17 | 764 |
| Anjandev Biswas · Manideep Naskar | 15 | 255 · 968 |

### 1.4 FINDING — staging cannot exercise the layout risk this obligation exists to catch

The interface says *"Check the longest real name on staging, not a short one."* **Staging has no long
name.** Its worst eligible case is **12 characters**; production's is **21**. Staging's worst case is
**shorter than `Amit Baran Sen` (14)**, the row §3.1 names as the tightest one — so a build that looks
perfect on staging tells us nothing about the row that will actually break.

**Seven production members eligible for the card have names of 17–21 characters.** The instruction to
verify on staging is, for this specific obligation, an instrument that cannot fail. Raised for the
Auditor: the width check needs production data — or a staging profile seeded to 21 characters, which
is D1's seeder, not D2's file.

---

## 2 · OBLIGATION 2 — before measurements at real width · **NOT MEASURED**

**The instrument is unavailable from this session, and no number below is invented to fill the gap.**

### 2.1 What was attempted, and what failed

| attempt (UTC 2026-09-03) | configuration | result |
|---|---|---|
| 09:19:36–09:20:14 | Playwright 1.56.0 + bundled Chromium, default egress | `net::ERR_CONNECTION_RESET` on both lanes, all viewports |
| 09:20:43–09:21:21 | same, `proxy: { server: 'http://127.0.0.1:41787' }`, `--ignore-certificate-errors` | `net::ERR_CONNECTION_RESET` |
| ~09:22 | `--proxy-server=…41787`, `--proxy-bypass-list=<-loopback>`, `ignoreHTTPSErrors`, `NODE_EXTRA_CA_CERTS` | `net::ERR_CONNECTION_RESET` |

Diagnosis, measured the same minute: `curl` **direct** to `www.50mmretina.com` returns **403**;
`curl -x http://127.0.0.1:41787` returns **200**. The container's only egress is that proxy, it
serves `curl`, and it resets Chromium's connections. **Stopped after three configurations** rather
than continuing to try — a fourth attempt is a rabbit hole, not a measurement.

**Consequently the following are NOT KNOWN and are owed:** rendered row height, rendered card height,
the width the name element actually receives, and the character count at which it truncates — at the
card's real width, on both lanes. No screenshots exist. **Nothing in this section may be recorded as
VERIFIED.**

### 2.2 What IS known, from source — labelled as source-derived, not rendered

Read from `src/pages/Index.tsx` @ `origin/staging` `c268293`, lines 1054–1092:

- **Truncation today is ELLIPSIS, not wrap and not overflow.** The name carries Tailwind's `truncate`
  (`overflow:hidden; text-overflow:ellipsis; white-space:nowrap`) via
  `nameClassName="text-xs truncate [font-family:var(--font-body)]"`, inside
  `<div className="flex-1 min-w-0 relative z-10">`. `min-w-0` is present, which is what lets a flex
  child shrink below its content width — without it `truncate` silently does nothing. **This is the
  one part of obligation 2 that source answers with certainty.**
- Fixed-width row furniture: medal `text-sm`, avatar `w-7 h-7` (28 px), score `shrink-0`, row
  `px-3 py-2` with `gap-2.5`, rows separated by `space-y-2`. Card is `md:col-span-4`, `p-5 md:p-6`.

### 2.3 Arithmetic, NOT a measurement — recorded so the Owner can weigh the risk early

At a 360 px viewport: card inner ≈ 360 − 40 (`p-5`) = 320; row inner ≈ 320 − 24 (`px-3`) = 296; minus
medal ≈ 18, avatar 28, three `gap-2.5` = 30, and the score block. Today's single figure `✦ 9,551` at
9 px ≈ 40 px, leaving the name ≈ 180 px ≈ 28 characters at `text-xs`. Option B's wider secondary line
`Lifetime 9,551` ≈ 52 px leaves ≈ 168 px ≈ **26 characters**.

**On that arithmetic Option B fits a 21-character name at 360 px, and rows may not grow taller at
all** — a two-line score block ≈ 24 px sits inside the height the 28 px avatar already sets. **This is
a hypothesis, not a result.** It is written down so that if the render later disagrees, the
disagreement is visible rather than quietly absorbed. **It is not evidence and must not be cited as
any part of the §3.4 gate.**

**No finding is raised against Option B.** The instruction was to report *"if any measurement shows
Option B cannot fit"* — no measurement was taken, so there is nothing to send back to OI-1. Option B
stands as frozen.

---

## 3 · OBLIGATION 3 — real mid-range Android · **NOT MEASURED**

D2 cannot hold a device, and this session has no link to one. Per §3.1 and the D2 standard, an
emulator is not the gate and a headless engine is not a device — so **no Android reading exists,
neither VERIFIED nor OWNER-ATTESTED.** It is owed, and it must be taken by the Owner on the runbook
(`docs/evidence/d2/runbook-android.md`) before and after, on the same device, or the after-reading has
nothing to be compared against.

**Anything a future session reports here without a device behind it is not this measurement.**

---

## 4 · Status of every line in this file

| class | applies to |
|---|---|
| **VERIFIED** | §0 and §1 — every name, character count and score, each through Supabase MCP `execute_sql` (SELECT only) at the stated UTC instant |
| **SOURCE-DERIVED** | §2.2 — read from `Index.tsx` at `c268293`; true of the code, not of a render |
| **NOT MEASURED** | §2.1 rendered geometry, §3 the Android reading. Instrument unavailable; owed |
| **ARITHMETIC, NOT EVIDENCE** | §2.3 |

## 5 · What is still owed before the after-reading can mean anything

1. A render of the current card at real width on both lanes — row height, card height, name width,
   truncation point — with screenshots. Needs a browser with egress this session does not have.
2. The Owner's Android before-reading on the current build.
3. A 17–21 character name reachable on staging, or agreement that the width check runs on production.

*D2 · Client & Delivery. Read-only. No `src/**` change, no SQL, no migration, no object reserved;
`supabase/**`, `scripts/db-*.mjs`, `docs/gates/**` and `docs/PROMOTION_LEDGER.md` untouched.*
