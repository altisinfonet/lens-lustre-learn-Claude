# F-89 re-land · the loop, reproduced by counting mounts

**Status: WORK IN PROGRESS. This branch currently carries F-89 restored (a revert of the
revert), so its tree is byte-identical to `bf03070` — the commit that hung deployed staging.
It MUST NOT be merged in this state.** It exists so the regression can be reproduced against a
committed, re-runnable instrument before anything is changed.

## The instrument

`mountcount.prepared.mjs` counts `UI-8006 ROUTE_NOT_FOUND` console events over a fixed window and
reports a **rate**, not a total, so a slower loop cannot pass it. It also asserts the 404 actually
rendered and that no sidebars remain, so it cannot go green on a page that never drew.

Every `NotFound` mount logs that event. Under React StrictMode a healthy mount logs **twice** per
visit. The deployed regression logged roughly **twice a second**, unbounded.

`UI-8006` is untouched — not deleted, not throttled. It is the only reason this was found, and
quietening a symptom to make a page look calmer is what Standing Rule 19 forbids.

## RED — the new in-place scene against `bf03070`'s tree

```
scene            : screen-not-found-in-place
window           : 14.4s
NotFound mounts  : 600   (healthy <= 4; StrictMode logs 2)
rate             : 41.64 / sec
404 rendered     : false
visible asides   : 2
body head        : 50mm Retina World Competitions Journal Courses Winners ⌘K AS Vote & Earn …

MOUNT CHECK: FAIL — 600 mounts in 14.4s, 404=false, asides=2
```

## Three things this reading establishes

**1. The earlier "the scene never rendered" was this loop, not a fixture gap.** When the in-place
scene was first run it showed the feed shell and no 404, with no console errors, and that was
reported as a scene that needed more work. It was not. `404 rendered: false` here, alongside 600
mounts, is the page never settling. **The evidence for this regression was on screen before the
Owner saw it and was misread as a scene bug.** That is a finding in its own right: the instrument
worked the first time and the operator did not.

**2. The harness reproduces it ~20x faster than the live site.** Deployed staging logged ~2/sec;
this logs 41.64/sec. The likely reason is that the live resolve path is paced by real network
latency while the fixture answers instantly, so the loop spins as fast as React will re-render.
Same defect, different clock — and it makes the harness the more sensitive instrument of the two.

**3. `asides=2` on this route.** The sidebars are present here on the broken code, which is
consistent with this being the route that actually carried the sidebar defect.

## Why every check passed before

On the catch-all route `NotFound` is a direct child of the `Outlet` and nothing above it
re-renders, so the loop does not occur. Both the developer's probe and the auditor's drove the
catch-all. This scene is that blindness closed.

---

# GREEN — the cause, the fix, and the numbers

## The cause, read from the diff rather than guessed

F-89 shipped the layout as a **ternary between two different tree shapes**:

```jsx
{isSidebarEligibleRoute ? (
  <div className="flex gap-8 container mx-auto">   ← sidebar branch
    <aside/> <div><AnimatePresence><PageTransition><Outlet/></…></div> <aside/>
  </div>
) : (
  <AnimatePresence><PageTransition><Outlet/></…>   ← bare branch, AnimatePresence at the top
)}
```

Those are different structures, so **every flip of the flag destroyed and recreated the whole
`Outlet` subtree.**

* **On the catch-all** that cost one extra remount and then settled, because `NotFound` holds no
  state: mount → flag true → subtree recreated → `NotFound` mounts again → flag already true →
  stable. Measured at **4 mounts** on the broken code.
* **On the single-segment route** it never settles, because the remount destroys
  **`CustomUrlProfile`** too and resets its `checking` state to `true`. So: `NotFound` unmounts →
  its cleanup sets the flag false → the shell returns → the lookup completes → `NotFound` mounts →
  flag true → subtree destroyed → `checking` resets → for ever. Measured at **600 mounts in 14.4s**.

The oscillator is the pairing of `useBareShell`'s unmount cleanup with a structural branch switch.
The flag itself was never the problem.

## The fix

One structure. The flag now changes only `className` and whether the two `<aside>` elements
render; the middle column keeps its position in the children list, so React keeps its identity and
nothing below it remounts.

## Numbers, same counter, same command

| scene | before | after |
|---|---|---|
| `screen-not-found-in-place` | **600 mounts / 14.4s (41.64/sec)**, 404 not rendered, asides=2 | **2 mounts**, 404 rendered, asides=0 |
| `screen-not-found-in-place-signed-out` | — | **2 mounts**, PASS |
| `screen-not-found` (catch-all) | 4 mounts | **2 mounts**, PASS |
| `screen-not-found-signed-out` | 4 mounts | **2 mounts**, PASS |

**2 is the healthy figure** — StrictMode's double render, exactly what the auditor measured on the
pre-F-89 deployed bundle. The catch-all dropping from 4 to 2 is independent confirmation of the
mechanism: that route had the same branch-switch remount, merely bounded.

## Everything else, because this change touches every route

```
F-89 acceptance probe, scene mode : 8 of 8 PASS (both auth states, both themes, 1280 and 360)
full suite                        : 2523 passed | 1 skipped, 0 failures
ui:gate                           : baseline diff CLEAN against all 148 recorded keys
                                    ✗ rows 12 — IDENTICAL to the F-90 baseline set
                                    baseline.json UNTOUCHED (Standing Rule 19)
                                    168 screenshots (152 + 16 from the four new 404 scenes)
```

The `baseline.json` fingerprint check is the load-bearing one here: the layout structure changed
for **every** route in the app, and all 148 recorded scene/viewport fingerprints are unchanged.

`UI-8006` is untouched throughout — not deleted, not throttled.
