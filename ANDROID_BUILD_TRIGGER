Build 1053 — 2026-08-06
- Stories are the size they should be. The round story pictures on the feed and
  on a profile were noticeably smaller than the ones people are used to
  everywhere else; they are now 80 points across on a phone and 96 on a wider
  screen, the size the owner marked on a reference screenshot.
- A story now opens FULL PAGE. Opening a story from a profile used to put it in
  a small box in the middle of the screen with no timer, while the same story
  opened from the feed filled the screen — two different experiences for the
  same photograph. Profile stories now fill the screen, with the thin progress
  bars along the top, tap the left third to go back and the right third to go
  forward, and automatic advance to the next story.
- A story stays on screen for 10 seconds, not 5. One shared setting now drives
  both places a story can be opened, so the two can never disagree again.
- You can delete your own story at any time, and you SEE it go. The delete
  button was always there and the deletion was really happening in the
  database — but the ring and the picture stayed on screen until the whole page
  was reloaded, so it looked as though nothing had happened. The story now
  disappears the moment it is deleted. And if a deletion ever genuinely fails,
  it now says so instead of falsely reporting success.
- You can tag a member in a post caption. Type @ and the first letters of a
  name while writing a post: a short list of members appears under the box with
  their picture, choose one, and their name becomes a link that opens their
  profile — the same as tagging in a comment. Only a name you actually chose
  from the list becomes a tag; a name merely typed by hand stays ordinary text,
  because tagging the wrong person is worse than tagging nobody.
- For the owner only: the members list in the admin area now shows, for each
  member, when they were last active and whether they were last on the app or
  the website. Recording started on 2026-08-05, so members who have not opened
  the site since then show a blank — a blank means "not recorded yet", never a
  guess. The app can only report itself from THIS build onward, so app members
  begin appearing after they update.
- The website address settled: 50mmretina.com now sends you to
  www.50mmretina.com, so one link no longer behaves like two different sites
  with two different sign-ins.
- Everything from 1052 and earlier. Upload 1053 INSTEAD of 1052.
- Web build 2026-08-06-1.

Build 1052 — 2026-08-05
- Post photos come back everywhere. On 2026-08-01 every feed photo was routed
  through an image-resizing service that only answers one of the site's two
  addresses; in the app and on www it failed every single time — an entire feed
  of grey placeholders on a photography site. Photos now load directly, the way
  avatars and thumbnails always did (which is why those never broke). This is
  "images are not coming", ended — measured in the owner's own browser, both
  addresses, zero placeholders.
- Saving now works in the app. Tapping download on a photo, or saving a journal
  or featured-artist article as a PDF, used to show progress and then silently
  do nothing — the app had no download manager to hand the file to. Files are
  now written properly and offered through the system Save/Share sheet. This
  build bundles the file plugin that makes it possible (the workflow change in
  this same commit).
- Emoji in comments. Typing <3, :), :(, ;) becomes a real emoji — at the
  beginning, middle or end of a comment, including a trailing <3 with nothing
  typed after it. Keyboard emoji were already stored correctly and are left
  untouched. Web addresses and ordinary text like "(see 8)" are never
  converted. 21 tests pin the rules.
- A post is a photograph: posting requires at least one photo and the caption
  is optional — and nothing ever blocks posting, commenting or reacting over a
  missing profile picture. Both rules are pinned by tests.
- You can now SEE which build you are holding: the profile sheet's bottom row
  shows the loaded version beside Logout — this build reads 1052 (1.2.2).
- A website update can no longer break a page that is already open (the
  "Something went wrong" screen right after a deploy). An open page now fetches
  the new version once, quietly, instead of failing.
- Everything from 1051 and earlier. Upload 1052 INSTEAD of 1051.
- Web build 2026-08-05-2.

Build 1051 — 2026-08-05
- Photos that fail to load now TRY AGAIN. Until now a single dropped packet — a
  tunnel, a lift, one weak moment on mobile data — replaced the photo with the
  grey placeholder permanently, and nothing ever retried it. It now waits 0.4s,
  tries again, waits 1.2s, tries once more, and only then gives up. This is the
  "images are not coming" report, and it is the main reason to upload this build.
- The picture cache no longer hides failures. When a photo could not be fetched
  it used to hand the screen an invisible blank that the phone counted as a
  SUCCESS — so nothing retried and nothing was ever written down. It now reports
  a real failure, and the app records exactly which picture died, so the next
  report arrives as an address instead of a screenshot.
- Everyone's stored pictures are refreshed once on first open (cache version
  bumped), so anything saved under the old broken rules is cleared.
- Today's Birthday now appears IN THE APP, above the feed. It existed only on a
  wide computer screen before — on every phone and tablet it was not shown at
  all. It appears only on days when somebody actually has a birthday.
- The feed opens with the three newest posts at the top, then continues with the
  wider spread that gives every photograph its turn. Same on refresh, app and
  website.
- Signing up now asks He or She. It is used for one thing only: choosing a
  fitting stand-in picture for members who have not uploaded a photo. It is
  never used for permissions or for who can see what.
- Everything from 1050 and earlier. Upload 1051 INSTEAD of 1050.
- Web build 2026-08-05-1.

Already live for ALL builds (database side, listed here for the record):
- Today's Birthday no longer misses anyone. It was reading an unordered list
  capped at 50 members, so 28 of the 68 members with a birthday on file were
  invisible on their own birthday. That cap is gone.
- Birthday notification: every accepted friend gets "Today is <name>'s Birthday"
  at 9:00 IST. Rehearsed against a real member with 21 friends — 21 notices, no
  duplicates, no email.
- The About Us page pointed twice at a logo file that does not exist, so it
  showed the grey placeholder. Corrected.

Build 1050 — 2026-08-04
- People you may know now appears in the feed itself, after the tenth post: a
  row of cards you swipe sideways, each with the photographer's picture, how
  many friends you have in common, an Add button and an X to hide it.
  This is app-only — on a computer these already sit in the right column, and
  until now people using the app never saw suggestions at all.
- The top menu bar stays put while scrolling on the website (no change in the
  app).
- Everything from 1049 and earlier.
- Web build 2026-08-04-7. PRs #62, #63.

Build 1049 — 2026-08-04
- The member's name now always wins the space. In narrow places the badge was
  taking room from the name and cutting it short; the badge now shortens itself
  instead, and shrinks to just its icon before a single letter of the name is
  lost.
- Badge text set to 8.5px.
- Everything from 1048 (readable solid badge pills in dark and light) and 1047.
  Upload 1049 INSTEAD of 1048.
- Web build 2026-08-04-5. PR #61.

Build 1048 — 2026-08-04
- Badges are readable at last. Every badge except the blue Verified one was a
  see-through tint with dark text at 7px, which failed the readability standard
  in BOTH dark and light mode for all ten colours. Each badge is now a solid
  coloured pill with white text at 10px, so it reads the same whatever is behind
  it. The blue Verified badge is untouched.
- Also covers "Featured Creator" and every other role pill, and the Admin panel's
  colour choices are now all readable by default, so a new badge cannot be
  created unreadable.
- Carries everything from 1047.
- Web build 2026-08-04-4. PR #60.

Build 1047 — 2026-08-04
- Reach and Viewed-by no longer react to anything a member does. Liking a post
  used to make them jump in the same instant, and removing the like made them go
  DOWN — which is impossible for real figures and made them look computed on the
  spot. They now depend only on the post and how old it is, so they can only ever
  move upwards, slowly, over hours and days.
- Carries everything from 1046 (no character counters) and 1045 (full 4:5 ad
  picture, fast ad updates, canonical share/referral links, native share sheet).
  Upload 1047 INSTEAD of 1045/1046.
- Web build 2026-08-04-3. PR #59.

Build 1046 — 2026-08-04
- No running character counter on any member text area (was showing "55/2200" style
  readouts on every comment box, the caption editor and the composer — owner order).
  A single red line appears ONLY when a member is over the limit, telling them how
  many characters to remove, because the send button is disabled at that point.
- Carries everything from 1045 (full 4:5 ad picture, fast ad updates, canonical
  share/referral links, native share sheet) — upload 1046 INSTEAD of 1045.
- Web build 2026-08-04-2. PR #58.

Build 1045 — 2026-08-04
- Feed ad card now shows its picture at the FULL 4:5, same size as a post photo (aspect box moved from the whole card to the media area; label no longer eats into the image).
- Admin ad changes reach screens fast: 60s settings-read cap + cache invalidation on save + mounted ad zones refetch on the save event (was up to 10 min).
- Every link that leaves the app now uses the real site origin instead of the WebView's localhost: referral link, post/profile/entry/certificate/journal shares, competition-photo copy links, and password-reset emails sent from the app.
- Share buttons in the app now open the native share sheet (the WebView has no navigator.share; before, they silently copied a dead localhost URL).
- Feed reshuffles on every refresh (DB-side, migration 20260804090000 — already live for all builds; listed here for the record).
- Web build 2026-08-04-1. PR #57.

Build 1044 - 2026-08-03

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

THIS IS THE FINAL BUILD OF THE DAY - UPLOAD THIS ONE. Supersedes 1043,
which is identical except for the item below.

NEW SINCE 1043:

MEMBERS-ONLY LINKS SEND A SIGNED-OUT VISITOR TO THE SIGN-IN PAGE
   Nine member pages (own profile, own photos, notifications, entry pages,
   journal and course editors, lessons) used to render an empty or
   misleading shell when opened signed out. They now redirect to the
   sign-in page. Verified per route in a rendered browser, before and
   after. Deliberately public pages (journal, competitions, winners,
   shared post links, the staff-ID check) are unchanged - shared post
   links were re-verified query-by-query against the live database and
   work for signed-out visitors.

ALSO ACTIVE (server-side, no build needed, already live for every build):
   Google-hosted profile pictures are no longer stored or shown. Google
   began refusing them (HTTP 503), which broke member photos at sign-in,
   at joining, and across the feed. 27 affected accounts cleaned; signup
   and login no longer write such links.

CARRIED FROM 1043: multiline comment box, visible centred send button,
   profile-completion panel fix, the logo notification icon, the 74
   repaired characters.

REGISTRATION
   Untouched. Sign-up flow has no changes in this build.

====================================================================

Build 1043 - 2026-08-03

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

THIS IS THE BUILD TO UPLOAD. It supersedes 1039-1042; every earlier build
is missing at least one of the fixes below.

CONTAINS, on top of everything in 1042:

1. INSTAGRAM-STYLE MULTILINE COMMENT BOX
   The comment field is now a real multi-line box: it grows one line at a
   time as the member types, up to five lines, then scrolls. On a phone the
   return key makes a new line and the button posts; on a desktop Enter
   posts and Shift+Enter makes a new line - the same split the big social
   apps use, chosen deliberately by the owner. The first letter of a
   comment is now capitalised automatically.

2. A SEND BUTTON MEMBERS CAN SEE AND HIT
   The send control is now a filled circular button, centred exactly on
   the line it posts from, with a 44px touch area. The first fix only
   enlarged the invisible touch zone; the owner correctly rejected a fix
   that could not be seen.

3. PROFILE COMPLETION PANEL FIXED
   The "complete your profile" panel opened a third off the left edge of
   the screen on phones, appeared on first touch and could not be
   dismissed. It is now anchored fully on-screen, opens on tap and closes
   on a second tap.

ABOUT THE BLANK-PAGE REPORT
   Could not be reproduced in the current code: all internal pages render
   at phone size with zero errors in six routes tested. The report came
   from build 1042 on a very slow connection (~30 KB/s in the screenshot).
   If it recurs ON THIS BUILD, that changes the diagnosis - report it with
   the page name.

MENTIONS
   Verified end-to-end in this code: typing @ plus letters queries the
   member list (the exact query was also proven directly against the
   backend) and the suggestion list renders on screen at phone width and
   is tappable. If the installed app still shows nothing, the suspect is
   the connection, not the feature.

REGISTRATION
   Untouched. No database change in this build.

====================================================================

Build 1042 - 2026-08-03

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

THIS IS THE BUILD TO UPLOAD. 1039, 1040 and 1041 are superseded - they were
all cut BEFORE the two fixes below, so none of them should go to the store.

1. THE NOTIFICATION MARK IS THE LOGO  (carried over from 1041)

   Push notifications showed a blank white square. Nothing was configured:
   no notification icon existed anywhere in the repository, so the messaging
   library fell back to the launcher icon - and since Android 5.0 the status
   bar flattens the small icon to its alpha mask, keeping only which pixels
   are solid. An opaque square becomes a solid white rectangle.

   The owner's logo now ships as the mark, cut separately at each of the five
   densities. The build opens the finished bundle and FAILS if the icon or
   either manifest entry is missing, so this cannot silently regress.

2. 74 CORRUPTED CHARACTERS REPAIRED  (new since 1041)

   A UTF-8 file had been read as Latin-1 and re-saved, turning single
   characters into two or three. 41 of them rendered to members as empty
   boxes:

     - every competition placement badge (trophy, medals, mentions, stars)
     - every round status icon (tick, star, cross, warning)
     - the sidebar medal icons
     - four sidebar links that should end in an arrow
     - the pinned-comment marker
     - both comment box placeholders
     - the unvote warning, which should read "2x the original reward"

   Repaired by reversing the exact byte transformation, not by retyping. A
   test now fails the build if it ever returns.

3. THE COMMENT SEND BUTTON  (new since 1041)

   Measured on production: the send button was a 24x24 px tap target. The
   minimum on Android is 48dp and on iOS 44pt, and an adult thumb is about
   45px across - so the target was smaller than the finger meant to hit it.
   People were missing it and hitting the text field instead.

   It is now 44x44. The icon is unchanged at 16px, so nothing looks
   different - there is simply something to hit. It also fires on pointer
   down as well as click, because the Android WebView can fail to deliver a
   click after a tap that closes the keyboard, and the keyboard now shows a
   Send key instead of a plain newline.

REGISTRATION
  Untouched. No database change and no change to sign-up in this build.

====================================================================

Build 1041 - 2026-08-02

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

THE NOTIFICATION MARK IS NOW THE LOGO ITSELF

  Owner's decision, taken after seeing every option at real size: use the
  logo, not a simplified mark.

  Cut from the artwork the owner supplied. The status bar keeps only the
  silhouette - which pixels are solid - and paints it in one flat colour, so
  what ships is the bright artwork of the badge: the outer ring, the 5, the
  aperture and the eye, as a single shape.

  Tuned separately at each density instead of one image resized five times,
  because the trade-off is different at every size. At 96 px and 72 px - what
  3x and 4x phones actually draw, which is most phones in use - the badge is
  legible. At 24 px it reduces to a ring and an eye; that density is rare.

  1039 shipped a drawn aperture; 1040 corrected its shape. 1041 replaces both
  with the real logo at the owner's instruction.

  The bundle-level assertion added in 1039 still runs: the build opens the
  finished bundle and fails if the icon or either meta-data entry is missing.

REGISTRATION
  Untouched. No app code, no web code and no database change in this build.

====================================================================

Build 1040 - 2026-08-02

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

THE NOTIFICATION ICON — the only change since 1037

WHAT MEMBERS SAW
  Every push arrived with a blank white square in the status bar instead of
  any mark at all.

WHY
  Nothing was configured. There was no ic_notification drawable anywhere in
  the repository, and no default_notification_icon meta-data in the manifest.
  With no small icon declared, the messaging library falls back to the
  launcher icon - and since Android 5.0 the status bar flattens the small
  icon to its alpha mask, keeping only which pixels are opaque and discarding
  every colour. The launcher icon is fully opaque, so its mask is a solid
  rectangle. That is the white square. The image was never corrupt.

WHAT CHANGED
  - A monochrome mark - the camera aperture from the centre of the app logo,
    white on a fully transparent background - at all five densities
    (24dp: 24, 36, 48, 72 and 96 px).
  - default_notification_icon and default_notification_color declared in the
    manifest, with the accent colour used to tint the icon in the shade.
  - Both are installed by the build itself, after the native project is
    generated, because that folder is not stored in the repository.

  1039 shipped an earlier glyph that read as a flower at real size. 1040
  replaces it with the classic iris, which is legible at 24dp.

HOW IT IS PROVEN, not assumed
  The build now opens the finished bundle and fails if the icon is not in it:
    - ic_notification present in the bundle resource table
    - default_notification_icon present in the bundled manifest
    - default_notification_color present in the bundled manifest
  This matters because the release build runs resource shrinking, which is
  free to drop a drawable that is only referenced from the manifest. Any
  future change that quietly strips the icon now stops at CI instead of
  reaching the store.

REGISTRATION
  Untouched. No app code, no web code and no database changed in this build.

====================================================================

Build 1037 - 2026-08-02

INTERNAL CHANGELOG. Never shown to a member. Play "What's new" stays
"Bug fixes and improvements." per the owner's standing instruction.

REGISTRATION — the one thing this build must not break, and does not
- A new member was stopped dead at the last step of signing up by
  "Upload failed / Please try another image" with no reason given. A profile
  photo is mandatory, so they could not join at all.
  Cause: the image loader rejected its promise with a DOM Event, which has no
  `.message`, so every caller printed its fallback string. The upload never
  reached the network - the browser could not decode the chosen file.
  Fixed: a real error carrying the file's type (to the console, never to the
  member), a second decode attempt via createImageBitmap that rescues files the
  <img> element rejects, and a message that says what to do next without naming
  any format or vendor.
- The signup gate itself is UNCHANGED: 18+, a profile photo on our own storage,
  an available username, a member type, and at least one interest. Each has its
  own specific message. Nothing was added to it.
- Server changes shipped today do not affect app versions already on Play:
  1033/1034 never called the new functions, the grouped-history function's
  signature is untouched, and the SELECT policy behind their notification list
  is unchanged.

NOTIFICATIONS - the system, rebuilt
- The panel that would not close. framer-motion kept the exiting node mounted:
  a 304x456 invisible rectangle over the top-right of every page, swallowing
  clicks until reload. On Android it simply stayed visible over the new page.
  Removal no longer waits on an animation; a route change, Escape, an outside
  tap and a real backdrop each close it independently.
- The chime played twice on every arrival - the bell is mounted twice, one copy
  hidden by CSS, and CSS hides without unmounting. Exactly one instance now owns
  the subscription and the sound, and leadership moves if that copy unmounts.
- One sentence everywhere. The bell used to render frozen text written by the
  database months earlier while the history page recomposed its own. Same event,
  different words. Both now come from one function.
- Push said "commented on your post" while the app said "commented on your
  photo". The push body is composed at send time now, from live data. It also
  stops naming 33 members who have deleted their accounts, and an admin can no
  longer appear under a real person's name.
- The badge stopped counting at 30. It counted a page, not the inbox; a member
  with 111 unread saw 30. It counts every unread event now, and the panel says
  how many are not on screen instead of quietly stopping.
- The bell groups like the history page - a day of reactions is one line, with
  real counts, not 40 lines.
- Tapping a new-post notification landed you on the dashboard. It opens the post.
  A reply opened an unrelated gallery. It opens the post.
- A friend request was counted twice and drawn twice. Once now. Answering one
  also clears the notification behind it.
- Push notifications can be turned off - a master switch plus per-type switches,
  including the new-photo alerts, which are the most frequent.
- A notification can be deleted. Until now nothing in the app could remove one.
- Notifications older than 90 days are cleared automatically. Anything still
  unread is kept, however old.
- The history screen is reachable from the account menu on phone and desktop.
  It used to be reachable only from inside a panel that closes itself.

Build 1036 - 2026-08-01

- Shared a photo to your wall by mistake? Open the menu on that post and choose
  Remove from my wall. It comes off your wall only; the original post and the
  photographer's copy are untouched.
- Fixed pages that sometimes opened completely blank. A page that is still
  loading now shows a loader instead of an empty screen.
- The notification bell moved to the top bar, and the light/dark switch moved
  into the profile panel where the bell used to be.
- A profile photo is required to post or comment, and an auto-filled account
  picture no longer counts as one.
- Reach and Viewed-by appear only once a post is 24 hours old.
- Older photos keep their own shape; no more empty bars around a wide photo.

Build 1035 - 2026-08-01

Profile photo policy closed properly.

- A Google account picture no longer counts as a profile photo. It was being
  filled in automatically at sign-in, so 31 of 77 members passed the mandatory
  photo rule without ever choosing a picture - and that hotlinked image often
  showed as an empty circle.
- A photo now counts only if the member uploaded it to our own storage.
  Everyone still carrying an auto-filled picture is asked for a real one on
  their next visit.
- The rule is now enforced in the database, not only in the app: posting and
  commenting are refused without a profile photo, whichever version of the app
  is installed. Nothing is deleted and nobody is signed out - reading,
  browsing, reacting and uploading a photo all keep working.

Build 1034 - 2026-08-01

Reach and Viewed-by now appear only after a post is 24 hours old. Nothing is
shown before that.

Older photos keep their own shape again. A wide photo posted before today no
longer sits inside a tall card with empty space above and below it, and where
padding is still needed the blurred edge of the photo is clearly visible on
dark images too.

build: 2026-07-28 six-features release (usernames + stored counts + photo gate + search fix, commit a465174)

build 1018 — fix(search): portal mobile sheet to body; navbar backdrop-filter collapsed fixed inset-0 (4281fae)
build 1019 — fix(search): no auto-suggestions, no phantom row highlight, tap-to-select fixed, instant tab clear (owner's 5 bug reports)
build 1020 — fix(search): post results open /post/:id (PostDetail), not /feed
build 1021 — feat: profile-only language, See translation on posts, blank-page self-heal (dd7fc24)
build 1022 — feat(search): premium search (typo-tolerant ranked RPC, hashtags, synced recents)
build 1023 — fix(search): real hashtag suggestions (typo-tolerant) + phone keyboard hardening
build 1025 — v1.2.0 release (2026-07-31). Everything since 1024 (v1.1.1, 29 Jul):
  * Feed: broadcast + never-repeat. All public posts eligible (no 48h window, no
    370-post cap), unseen-first with fewest-viewers-first fairness, recycling
    fallback so the feed never ends, no same-author back-to-back.
  * Stories (24h): public to the whole community (was follows-only), ordered
    newest-first, and the author sees a VIEW COUNT (number only, no names).
  * Feed posts: "Add friend" button on Suggested posts (sends request + follows).
  * /friends: new "Awaited" tab for received requests (accept one by one);
    "Pending" now shows only requests you sent.
  * Notifications: comment/reaction notifications open the exact post; friend
    requests open the Awaited list. Shared link map used by bell + push.
  * PUSH NOTIFICATIONS (new): device registers on login, DB trigger fires
    send-push on every notification, tap deep-links into the app, per-user
    push_* preferences, unregister on logout.
  * "Photo of the Day" renamed to "The Curated Wall" everywhere (text only).
  * Admin Gallery: free-text photo category (custom names flow to home page).
  * Fixes: first-visit blank page + sticky ?cb= param from the cache-buster;
    entry-bundle cache-bust marker so deploys can no longer serve stale code.

build 1027 — v1.2.1 (2026-07-31). Play Console recommendations + one bug fix:
  * FIX (regression from 1026): "Add friend" on a suggested post showed a raw
    Postgres error "duplicate key value violates unique constraint
    friendships_requester_id_addressee_id_key" when a friendship row already
    existed. The button is now hidden unless no friendship exists in either
    direction, and a duplicate can never surface a database error.
  * Removed the UNUSED @capacitor/camera plugin. Nothing in the app imported it,
    but it dragged in io.ionic.libs.ioncameralib, which Play named in the
    "deprecated edge-to-edge APIs" and (obfuscated) BitmapFactory downsampling
    recommendations.
  * AGP 8.9.1 -> 8.12.3 and Gradle wrapper -> 8.13 (AGP 8.12 is the minimum for
    the next item; 8.13 is the minimum Gradle for AGP 8.12).
  * Enabled android.r8.optimizedResourceShrinking=true — clears Play's
    "Optimized resource shrinking isn't enabled" recommendation.

build 1029 — v1.2.2 (2026-07-31). Bug fixes only. Build config unchanged from
1028 (AGP 8.12.3 / Gradle 8.13), which cleared 3 of Play's 4 recommendations.
  * FIX: "Add friend" on a suggested post showed a raw database error
    (duplicate key ... friendships_requester_id_addressee_id_key) when a
    friendship already existed. The button now appears only when no friendship
    exists in either direction, and a duplicate can never surface a DB error.
  * FIX: a profile with no photo now re-opens the mandatory setup screen on the
    next login / app open. The gate previously checked username and user type
    but NOT the photo, so accounts completed before the photo was required were
    never asked again.

build 1027 — v1.2.2 release (2026-07-31). Since 1026 (v1.2.1):
  * ALL FAKE ENGAGEMENT NUMBERS REMOVED. getSimulatedStats() invented view and
    "reach" figures from a hash of the item id (2,000-100,000 range) and showed
    them as real. Deleted. Numbers now come from real sources only: posts from
    feed_events via get_post_view_counts(), stories from story_views (counts
    only, never viewer names), profiles from profile_views. "Reach", "Trending"
    and "Top post" had no real source anywhere and are gone entirely rather
    than estimated.
  * Official account is FOLLOW-ONLY again. The feed Add-friend button called the
    mutation directly and bypassed the rule, so real friend requests reached the
    official account. Now enforced in three places: the feed marks admin authors
    unavailable so no button renders, the mutation refuses, and a BEFORE INSERT
    trigger on friendships rejects the row.
  * Profile photo is mandatory: accounts with no DP are gated on next open.
  * Home page: Google Play badge in the footer, duplicate copyright removed.

2026-08-01 — build 1032 (v1.2.2)
Notifications rebuilt:
- New Notifications screen: full history, grouped rows, photo thumbnails,
  relative time, Follow back without leaving the list.
- Notification list no longer closes while you scroll it.
- New alerts when someone you follow shares a post.
- 'See more' now appears on every caption that needs it.
- Picture library for feed ads with fixed placement.

2026-08-01 — build 1033 (v1.2.2)
Speed, and photos the way you shot them:
- The feed is much faster to open. It was making 106 separate requests to the
  server on every load; it now makes 46, because profiles and settings were
  being fetched over and over instead of once.
- Photos load far quicker and use a fraction of the data. Each one is now sent
  at the size your screen actually needs instead of full resolution — measured
  at about a quarter of the bytes, and in a newer format where supported.
- Reactions, comments and scrolling feel quicker as a result. They were never
  slow themselves; they were waiting behind everything else.
- YOUR PHOTO KEEPS ITS OWN SHAPE. Every upload used to be force-cropped to 4:5
  and the crop was permanent — the original never reached us. Post whatever
  shape you shot: wide, square, tall. Cropping is now optional, per photo.
- Very wide or very tall photos get a soft blurred edge instead of being cut.
- Reach and Viewed-by now appear on every post, on the same line as reactions.
- Consistent spacing under the name on every card, with or without a caption.
